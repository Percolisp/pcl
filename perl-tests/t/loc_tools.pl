# Locale tools stub for PCL tests
# Real file provides locale utilities

sub find_utf8_ctype_locale { return undef; }
sub find_locale { return undef; }
sub locales_enabled { return 0; }  # Stub: assume no locales available

1;
