#!/usr/bin/env perl

# Copyright (c) 2025-2026
# This is free software; you can redistribute it and/or modify it
# under the same terms as the Perl 5 programming language system itself.

# pl2cl - Convert Perl expressions to Common Lisp
# Usage: pl2cl [options] <file.pl>
#    or: echo '$x = 1' | pl2cl
#
# Modes:
#   (default)      Transpile to stdout
#   --bundle       Compile to single .fasl with all dependencies
#   --executable   Create standalone executable

use v5.20;
use strict;
use warnings;
use Getopt::Long;
use File::Spec;
use File::Basename;
use Cwd qw(abs_path);
use POSIX ();

# Output UTF-8 so non-ASCII chars (from \xNN escapes) are valid for SBCL's reader
binmode(STDOUT, ":utf8");
binmode(STDERR, ":utf8");

# Find Pl::Parser relative to this script, not cwd
use lib dirname(abs_path($0));
use Pl::Parser;

# v2 (Pl::Parser2 → Pl::CLForm structured emission) is the ONLY pipeline
# (default since session 273/W9; the v1 escape hatch and the whole-file
# fallback were removed at E4.1 step 2, task #242).  `PCL_V1`/`PCL_V1_FILES`
# no longer exist: anything v2 cannot lower is now a hard error, which is the
# point — a gap used to become a silent re-transpile through a second
# compiler with different semantics.  `PCL_V2=1` is still accepted as a
# harmless no-op so existing scripts keep working.
#
# `Pl::Parser` is still LOADED (v1's expression seam is called from inside
# v2); what is gone is the file-level v1 ENTRY.  Step 3 (#243) deletes the
# file-level chunks that this commit made unreachable.
require Pl::Parser2;

# (The v2 whole-file fallback lived here until E4.1 step 2.  It caught a
# "Parser2 TODO: …" and re-transpiled the file through v1 — which meant a v2
# gap silently changed compilers mid-run, and any resulting divergence was
# attributed to the program rather than to the fallback.  The gaps are now
# hard errors.  The audit side-channel that measured this (PCL_V2_AUDIT_LOG /
# PCL_V2_AUDIT_KEEP) went with it: with one pipeline there is no route to
# count.  History: docs/v1-live-share-audit.md, docs/v2-endgame-plan.md §5a.)

# The ONE parser entry.  Anything v2 cannot lower dies here, and
# the die IS the answer — there is no second compiler to retry on.  Modes are
# whitelisted rather than ignored, so an unknown mode is a loud error instead
# of a silently-dropped option.
sub parse_source {
  my ($method, @args) = @_;
  my %opts = @args[1 .. $#args];
  # `lenient_ppi` never had a v2 implementation — it only ever worked by
  # routing to v1, whose parser truncates at the first unparseable line.  The
  # flag stays ACCEPTED (runpcl/runt/sweep all pass it) and inert; what it
  # used to paper over now dies naming the file, from Parser2's PPI check.
  delete $opts{lenient_ppi};
  my %known = (eval_mode => 1, eval_pkg => 1, eval_captures => 1);
  my @unknown = sort grep { !$known{$_} } keys %opts;
  die "pl2cl: unknown transpile mode(s): @unknown\n" if @unknown;
  return pipeline_marker(v2 => Pl::Parser2->$method($args[0], %opts));
}

# T0.1 pipeline marker (docs/v2-transfer-plan.md): every transpile carries one
# leading comment line naming the pipeline that produced it, so the dialect
# mix of a corpus — and of ~/.pcl-cache — is auditable.  gen= is read from the
# runtime's *pcl-cache-generation* (the single source of truth; do NOT
# duplicate the string here — a wrong gen in the marker would be worse than
# none when debugging stale-cache problems).
my $CACHE_GEN;
sub cache_generation {
  return $CACHE_GEN if defined $CACHE_GEN;
  $CACHE_GEN = 'unknown';
  my $rt = dirname(abs_path($0)) . '/cl/pcl-runtime.lisp';
  if (open my $fh, '<', $rt) {
    while (my $line = <$fh>) {
      if ($line =~ /\(defparameter\s+\*pcl-cache-generation\*\s+"([^"]+)"/) {
        $CACHE_GEN = $1;
        last;
      }
    }
    close $fh;
  }
  return $CACHE_GEN;
}

sub pipeline_marker {
  my ($pipeline, $out) = @_;
  return $out unless defined $out;
  return ";;; pcl: pipeline=$pipeline gen=" . cache_generation() . "\n" . $out;
}

# Parse command line options
my $no_cache = $ENV{PCL_NO_CACHE} // 0;
my $cache_lisp = 0;
my $is_module = 0;      # When true, skip preamble (for dynamic module loading)
my $bundle_mode = 0;    # Compile to single .fasl
my $executable_mode = 0; # Create standalone executable
my $output_file;        # Output file for bundle/executable
my $lenient_ppi = 0;    # DEBUG: truncate at first PPI-unparseable line (test runner only)
my $eval_pkg;           # When set, emit minimal preamble for eval-string mode
my $server_mode = 0;    # Persistent transpiler server mode
# my $use_cache = 0;    # Opt-in caching (disabled by default) - COMMENTED OUT FOR NOW

GetOptions(
  'no-cache'     => \$no_cache,
  'cache-lisp'   => \$cache_lisp,
  'module'       => \$is_module,
  'bundle'       => \$bundle_mode,
  'executable'   => \$executable_mode,
  'output|o=s'   => \$output_file,
  'lenient-ppi'  => \$lenient_ppi,   # DEBUG: for test runner use only
  'eval-pkg=s'   => \$eval_pkg,      # eval "string" mode: minimal preamble for named package
  'server'       => \$server_mode,   # persistent transpiler server mode
  # 'cache'      => \$use_cache,  # COMMENTED OUT - caching disabled by default
) or die "Usage: pl2cl [options] <file.pl>\n";

# Get path to this script for runtime to use
my $script_path = abs_path($0);

# Get Perl's @INC (filter to existing directories)
my @perl_inc = grep { $_ && -d $_ } @INC;

# Build preamble to inject after (in-package :pcl)
sub build_preamble {
  my ($source_file) = @_;
  my @lines;

  # Set pl2cl path for dynamic requires
  push @lines, "(setf pcl::*pcl-pl2cl-path* #P\"$script_path\")";

  # Cache settings
  push @lines, "(setf pcl::*pcl-skip-cache* t)" if $no_cache;
  push @lines, "(setf pcl::*pcl-cache-fasl* nil)" if $cache_lisp;

  # Initialize @INC from Perl's @INC
  push @lines, ";; Initialize \@INC from Perl";
  push @lines, "(setf pcl::\@INC (make-array 0 :adjustable t :fill-pointer 0))";

  # Add source file directory first (like Perl does)
  if ($source_file && -f $source_file) {
    my $source_dir = File::Spec->rel2abs(
      (File::Spec->splitpath($source_file))[1] || '.'
    );
    push @lines, "(vector-push-extend \"$source_dir\" pcl::\@INC)";
  }

  # Add current directory
  push @lines, "(vector-push-extend \".\" pcl::\@INC)";

  # Add PCL's lib/ directory (for our stub modules like Config.pm)
  my $pcl_lib = dirname($script_path) . "/lib";
  if (-d $pcl_lib) {
    push @lines, "(vector-push-extend \"$pcl_lib\" pcl::\@INC)";
  }

  # Add Perl's @INC paths
  my @core_inc;
  push @core_inc, $pcl_lib if -d $pcl_lib;
  for my $path (@perl_inc) {
    # Escape backslashes and quotes
    $path =~ s/\\/\\\\/g;
    $path =~ s/"/\\"/g;
    push @lines, "(vector-push-extend \"$path\" pcl::\@INC)";
    push @core_inc, $path;
  }

  # Record the "built-in" module paths (PCL lib/ shims + system perl's libs).
  # They play the role of perl's compiled-in default @INC: user code that
  # REPLACES @INC (`BEGIN { @INC = '../lib' }` in t/run/*) must still resolve
  # core modules, whose PCL equivalents live in these dirs.
  push @lines, "(setf pcl::*p-core-inc-dirs* (list "
    . join(" ", map { "\"$_\"" } @core_inc) . "))";

  # Switch to main package for user code (Perl's default package)
  # main uses :cl and :pcl so runtime functions are accessible
  push @lines, ";; Switch to main package (Perl's default for code without 'package' statement)";
  push @lines, "(p-defpackage :main)";
  push @lines, "(in-package :main)";

  return join("\n", @lines);
}

# Build MINIMAL preamble for eval-string mode.
# Only switches to the named package — all other state (@INC, *pcl-pl2cl-path*,
# etc.) is already live in the running SBCL image.
sub build_eval_preamble {
  my ($pkg_name) = @_;
  # $pkg_name is the Perl package name (e.g. "main", "Foo", "Bar::Baz").  Apply
  # the same single-vs-multi-segment designator rule as Parser::_cl_pkg_designator
  # so the eval'd code lands in the SAME CL package the rest of the codegen uses:
  # single-segment names stay bare (reader case-folds, :main -> MAIN, :Foo -> Foo),
  # multi-segment names are pipe-quoted to preserve '::' and case.
  my $desig = ($pkg_name =~ /::/) ? ":|$pkg_name|" : ":$pkg_name";
  return "(p-defpackage $desig)\n(in-package $desig)";
}

sub _run_server {
  # Persistent transpiler server: read requests from stdin, write CL to stdout.
  # Character-based protocol (UTF-8 on both sides, lengths = char counts):
  #   Request:  "<pkg>\n<captures>\n<char-count>\n<perl-code: exactly char-count chars>"
  #   Response: "ok\n<char-count>\n<cl-code: exactly char-count chars>"
  #          or "err\n<char-count>\n<error: exactly char-count chars>"
  # <captures> is the space-separated list of the caller's in-scope lexical
  # names (the eval capture alist's keys), possibly empty.  Only ONE decision
  # reads it — #296-B1, "is this exception-partition name the caller's `my $a`
  # or the dynamic special" — and the runtime keys its eval cache on the same
  # list, because it is compiler INPUT.
  binmode(STDIN,  ':encoding(utf-8)');
  binmode(STDOUT, ':encoding(utf-8)');

  # LIVENESS (task #273, ruled s397).  This loop looks at STDIN only BETWEEN
  # requests, so a server whose client SBCL is SIGKILLed while a transpile is
  # in flight never sees the EOF: it is reparented (to init, or to the
  # session's subreaper -- `systemd --user` on a desktop login, which is why
  # a PPID==1 test in the callers never fired) and grinds on holding
  # gigabytes (s396: two orphans at 4.8 GB and 4.6 GB for half an hour).  The
  # one process that KNOWS the client is gone is this one, so it asks: a
  # once-a-second tick compares getppid() with the client's pid, and a
  # changed answer means the reply would go nowhere.  Safe signals deliver
  # the tick between perl ops, so it fires inside a long transpile too (worst
  # case: after the current op -- a long regex match -- completes); the
  # blocking reads below are EINTR-safe (PerlIO retries after the handler).
  # _exit, not exit: nothing to flush, no partial reply to a dead pipe.
  my $client = getppid();
  $SIG{ALRM} = sub { POSIX::_exit(0) if getppid() != $client; alarm 1 };
  alarm 1;

  while (1) {
    my $pkg = <STDIN>;
    last unless defined $pkg;
    chomp $pkg;

    my $caps_str = <STDIN>;
    last unless defined $caps_str;
    chomp $caps_str;
    my @caps = split ' ', $caps_str;

    my $len_str = <STDIN>;
    last unless defined $len_str;
    chomp $len_str;
    my $len = $len_str + 0;

    my $code = '';
    my $got = read(STDIN, $code, $len);
    last unless defined $got && $got == $len;

    my $cl = eval {
      my $output = parse_source('parse_code', $code,
                                       eval_mode => 1, eval_pkg => $pkg,
                                       eval_captures => \@caps);
      my $preamble = build_eval_preamble($pkg);
      $output =~ s/\(in-package :pcl\)/$preamble/;
      $output
    };

    my ($status, $body);
    if ($@) {
      $status = 'err';
      $body   = "$@";
    } else {
      $status = 'ok';
      $body   = $cl;
    }

    print STDOUT "$status\n" . length($body) . "\n$body";
    STDOUT->flush;
  }
}

# Get runtime content for bundling
sub get_runtime_content {
  my $script_dir = dirname(abs_path($0));
  my $runtime_path = "$script_dir/cl/pcl-runtime.lisp";

  open my $fh, '<', $runtime_path
    or die "Cannot read runtime: $runtime_path: $!";
  local $/;
  my $content = <$fh>;
  close $fh;

  return $content;
}

# Bundle mode: compile main file + all dependencies into single output
sub do_bundle {
  my ($source_file, $output_file, $executable) = @_;

  # Default output name
  unless ($output_file) {
    my $base = basename($source_file, '.pl', '.pm');
    $output_file = $executable ? $base : "$base.fasl";
  }

  my $lisp_file = "$output_file.lisp";

  say STDERR "Bundling: $source_file -> $output_file";

  # Transpile the main file (dependencies are loaded at run time via p-use).
  # E4.1 step 1: bundle mode used to call Pl::Parser->parse_file directly — the
  # one v1-ONLY bypass left in pl2cl.  It now goes through the same entry as
  # every other file transpile, so the gate flip in step 2 covers it too.
  my $transpiled = parse_source('parse_file', $source_file);

  # Inject preamble
  my $preamble = build_preamble($source_file);
  $transpiled =~ s/(\(in-package :pcl\))/$1\n$preamble/;

  # Build the bundle
  open my $out, '>', $lisp_file
    or die "Cannot write $lisp_file: $!";

  # 1. Runtime first
  print $out ";; === PCL Runtime ===\n";
  print $out get_runtime_content();
  print $out "\n\n";

  # 2. Transpiled code (includes all dependencies via 'use')
  print $out ";; === Transpiled: $source_file ===\n";
  print $out $transpiled;
  print $out "\n";

  close $out;

  say STDERR "  Written: $lisp_file";

  if ($executable) {
    # Create standalone executable
    # NOTE: one --eval = ONE expression for SBCL, hence the progn.
    my $save_code = qq{
      (progn
      (load "$lisp_file")
      (sb-ext:save-lisp-and-die "$output_file"
        :toplevel (lambda ()
                    (handler-case
                      (sb-ext:exit :code 0)
                      (error (e)
                        (format *error-output* "Error: ~A~%" e)
                        (sb-ext:exit :code 1))))
        :executable t))
    };

    say STDERR "  Creating executable...";
    system('sbcl', '--noinform', '--non-interactive',
           '--eval', $save_code);

    if ($? == 0) {
      say STDERR "  Created: $output_file";
      # Clean up intermediate file
      unlink $lisp_file;
    } else {
      die "Failed to create executable (sbcl exit code: $?)\n";
    }
  } else {
    # Compile to FASL - load the file first (which loads ASDF/cl-ppcre), then compile
    say STDERR "  Compiling to FASL...";
    system('sbcl', '--noinform', '--non-interactive',
           '--load', $lisp_file,
           '--eval', qq{(compile-file "$lisp_file" :output-file "$output_file")},
           '--eval', '(sb-ext:exit)');

    if ($? == 0) {
      say STDERR "  Created: $output_file";
    } else {
      die "Failed to compile (sbcl exit code: $?)\n";
    }
  }
}

if ($server_mode) {
  _run_server();
  exit 0;
}

my $code;
my $source_file;

# Handle bundle/executable modes
if ($bundle_mode || $executable_mode) {
  die "Bundle/executable mode requires a source file\n" unless @ARGV && -f $ARGV[0];
  do_bundle($ARGV[0], $output_file, $executable_mode);
  exit 0;
}

if (@ARGV && -f $ARGV[0]) {
  $source_file = $ARGV[0];
  my $output = parse_source('parse_file', $source_file,
    ($lenient_ppi ? (lenient_ppi => 1) : ()));

  if ($is_module) {
    # Module mode: skip full preamble (@INC etc. are already live in the running SBCL),
    # but still inject the default package switch so subs are defined in :main, not :pcl.
    # Any explicit 'package Foo' in the module will override this immediately.
    $output =~ s/(\(in-package :pcl\))/$1\n(p-defpackage :main)\n(in-package :main)/;
  } else {
    my $preamble = build_preamble($source_file);
    $output =~ s/(\(in-package :pcl\))/$1\n$preamble/;
  }

  print $output;
}
elsif (@ARGV) {
  # Treat arguments as code (no @INC init needed for one-liners)
  $code = join(' ', @ARGV);
  print parse_source('parse_code', $code);
}
elsif (!-t STDIN) {
  # Read from stdin
  local $/;
  $code = <STDIN>;
  my $output = parse_source('parse_code', $code,
                 (defined $eval_pkg ? (eval_mode => 1, eval_pkg => $eval_pkg) : ()));

  if (defined $eval_pkg) {
    # eval-string mode: REPLACE (in-package :pcl) with just the package switch.
    # @INC, *pcl-pl2cl-path* etc. are already live in the running SBCL.
    my $preamble = build_eval_preamble($eval_pkg);
    $output =~ s/\(in-package :pcl\)/$preamble/;
  } else {
    # Normal stdin: inject full preamble after (in-package :pcl)
    my $preamble = build_preamble(undef);
    $output =~ s/(\(in-package :pcl\))/$1\n$preamble/;
  }

  print $output;
}
else {
  say "Usage: pl2cl [options] <file.pl>";
  say "   or: pl2cl '\$x = 1;'";
  say "   or: echo '\$x = 1;' | pl2cl";
  say "";
  say "Modes:";
  say "  (default)       Transpile to stdout";
  say "  --bundle        Compile to single .fasl with runtime + all dependencies";
  say "  --executable    Create standalone executable (bonus feature!)";
  say "";
  say "Options:";
  say "  -o, --output    Output file (default: <input>.fasl or <input>)";
  say "  --no-cache      Disable module caching (also: PCL_NO_CACHE=1)";
  say "  --cache-lisp    Cache .lisp instead of .fasl (for debugging)";
  exit 1;
}

print "\n";
