# Questions worth asking — the meta list (s493, 2026-09-20)

The questions that found the most in s491–s493 changed WHO is looking or WHAT is looked at, not the amount of effort.  This file keeps the list and each question's status; update it when one of them is worked.  Scratch paths are on the development machine; the task numbers are the durable record.

USER (s493, 2026-09-20): "what should I ask about, to make PCL work better (closer to Perl)?" → then "Save your list of questions, this meta level was good."  Checked-in copy: `docs/questions-worth-asking.md`.  The pattern: the questions that paid off changed WHO is looking or WHAT is looked at, not the amount of effort ("how many central features fail?" → the everyday battery; "find more obvious angles" → the s493 test-running angles (DECIDED `## s493`)).  The suites answer the questions they were built for.

1. **"Here are my own scripts — which of them run?"**  The USER's Perl is on this machine (~600k lines: `~/old2/{myP,P,old}`, `~/Dropbox/{perl,P,attorg,…}`); NEVER EXECUTE it unasked (side effects; Dropbox holds private files — touch only .pl/.pm/.t, never print contents).  Done s493, read-only: a module-usage table (`~/pcl-agent-scratch/s493/q1/modules.tsv`: Carp 394 files, Test::More 345, Data::Dumper 319, Smart::Comments 203 (a SOURCE FILTER), Getopt::Long 131, File::Spec 119, Moo 97, DBI 87, Data::Dump 77, FindBin 69, Dumpvalue 66, Dancer 66, LWP 59 …) = the REAL priority list for shim work; and a seeded 528-file compile-acceptance sample (`q1/accept-uc.log`): **510 of 528 (96.6 %) transpile clean**; the 18 misses = `dies_ok {…}`/`lives_ok {…}` block-form calls x4 (Test:: prototypes skipped = #478), smartmatch `~~` x4 (ruled refusal — the USER`s own code uses it), `pairwise {…} @a, @b` x2, `method NAME {…}` x2 (Method::Signatures::Simple, a keyword module; 22 files use it), `state($a, $b, $c);` list declaration DIES x2, one file-lexical capture refusal, 3 probably-invalid old files.  NEXT: ask the USER which scripts are safe to RUN (or run their `.t` files) for a true pass rate.
2. **"Which mechanisms keep producing the bugs?"**  Rough keyword census of tasks #1000+ (`~/pcl-agent-scratch/s493/q2/cluster.pl`; 614 tasks): NAME RESOLUTION/scoping/packages 96 filed / 72 open = the biggest producer; CONTEXT 17 filed but 10 SILENT (highest silent ratio; found three separate times in two days: #2004 #2090 #2102); regex engine 29 / 12 silent; `local` 15 / 13 open.  NEXT: a careful per-task classification (Sonnet job) → decide whether context and name resolution get a STRUCTURAL fix (context computed ONCE as a node attribute; the bind-once symbol table of var-handling-review-s379 Direction A).
3. **"What does perl die on that PCL lets through?"**  perldiag's run-time fatals walked (`q3/fatals.pl`, 67 cases): 50 agree, **16 perl-dies / PCL-lives** (strict refs never enforced, read-only modification, INVALID REGEX at run time, `@$undef` rvalue, false `require`, bless non-ref) → **#2103**.
4. **"What breaks only at scale or over time?"**  Partly done: the N-vs-4N battery (#2098 quadratic `.=`/shift/unshift; recursion ~10k frames #1929).  NOT RUN: the ENDURANCE battery — fd exhaustion from unclosed lexical handles in a loop (#2006(b) predicts "Too many open files" at ~1000), 1M-iteration `eval {}`/`die`/`local`/string-eval loops (handler/binding/eval-cache growth), 1M-line file, 1M-key hash, RSS at checkpoints via /proc/self/status, fork+wait ×1000 (zombies), piped opens ×2000.
5. **"What does it look like when it goes wrong?"**  NOT RUN: ~20 failure scenarios, perl vs pcl, measuring stderr LINE COUNT, whether Lisp leaks into the user's face (`SB-`, `Backtrace`, `#<`), and exit status: uncaught die (±newline, object), runtime error, syntax error, missing module, missing script file, SIGINT/SIGTERM/SIGPIPE (`pcl x.pl | head -1`), heap/stack exhaustion.  Seen s493: an uncaught error = 30 lines of SBCL backtrace; a compile-phase hang survives `timeout`'s SIGTERM.
6. **"Which of our numbers flatter us?"**  Sweep 96.5 % vs core-module t/ 68 % vs battery 66 %.  Idea: every README claim becomes a checked assertion that fails when it stops being true in EITHER direction (the `@_`-aliasing bullet was stale for who knows how long).
7. **"If another Perl programmer tried this next week, which ten fixes matter most?"**  Forces ranking by (how ordinary) × (silent?) instead of by suite rows.
8. **"How would a user ever find out PCL gave a wrong answer?"**  Product idea: `pcl --check script.pl` runs perl alongside and diffs — every user becomes a differential tester.

