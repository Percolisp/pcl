# file	P_ok	P_notok	C_ok	C_notok	status	sig	[P=PERL, C=PCL]
# s441a (Opus 5, task #463 item 2) — TWO ROWS EDITED BY HAND, both because a
#   DROP became real code: prefix `++`/`--` on a scalar DEREF (`++${"23::foo"}`)
#   used to take the whole statement with it.
#
#     op/universal.t   C_ok 61 -> 79, C_notok 48 -> 59, drops 1 -> 0
#     uni/method.t     rows unchanged at 28/21, drops 2 -> 0
#
#   universal.t's drop is line 112, `++${"23::foo"};`, whose only job is to
#   autovivify the package `23` — every row after it that asks whether "23" can
#   a method depended on it, and the file died there.  It now runs on to
#   aborted-forms:6 ("No SUPER::isa found from RTN::B"), 18 rows further.  The
#   11 new C_notok are rows the file never reached before, and they fail for
#   ONE pre-existing cause each (the UNIVERSAL::isa loop at line 106 and the
#   glob/IO `can` rows) — nothing that passed stopped passing.
#   uni/method.t's two drops are the same shape in a BEGIN block; the file
#   still aborts at exactly the same place with exactly the same signature, so
#   only its drops column moves.  Both measured in the parallel pass AND alone
#   (#366 serial re-run: 79/59 both ways).
# s438i (Opus 5, task #450) — ONE ROW EDITED BY HAND: op/glob.t C_ok 14 -> 13,
#   C_notok 4 -> 5.  perl's glob returns a pattern with no METACHARACTER as
#   ITSELF (`glob("/nope-xyz")` is "/nope-xyz"), and PCL went to the filesystem
#   unconditionally and answered empty; #450 fixes that, and #450 also gives
#   PCL perl's WORD model (a pattern is a whitespace-separated list).
#
#   The row that moves is 18, "Make sure the presence of the CORE::GLOBAL::glob
#   typeglob does not affect whether File::Glob::csh_glob is called".  It
#   compares `$output1 eq $output2` from TWO separate `eval q{ glob(q(./"TEST"))
#   }` call sites, and it was PASSING ON NOTHING: both sides were undef, because
#   that pattern has no wildcard and matched nothing.  With the values real, two
#   pre-existing gaps become visible and the row fails HONESTLY:
#     #489  the scalar-context iterator is keyed by PATTERN, not by CALL SITE,
#           so the second call answers undef where perl answers the value again;
#     #490  glob does not strip csh quotes, so the value is `./"TEST"` where
#           perl gives `./TEST`.
#   Nothing that was testing anything stopped passing.  Measured alone and in
#   the parallel pass, both 13/5.  io/pvbm.t read 20/8 in both passes and 23/5
#   ALONE again — the SEVENTH time — and is NOT edited.
# s438 (Opus 5) — the scan is 528 FILES, not 523, and ONE ROW EDITED BY HAND.
#   `--all` used to drop the five BEGIN-@INC "need-harness" files from the dir
#   scan silently (comp/line_debug.t, op/goto.t, op/lex.t, op/require_errors.t,
#   run/dtrace.t), which is how they came to have rows nothing could refresh
#   (spliced by hand in s431; printed as a hole from both sides in s434).  All
#   five were measured by naming them and all five produce a verdict, so they
#   join the scan (fable-answers-s437 §2 ask 1(b)).  Four of the five match
#   their s431 row to the row.  The fifth:
#
#     op/require_errors.t   C_notok 70 -> 68   (C_ok unchanged at 3)
#
#   and it is PRE-EXISTING and ATTRIBUTED, not a mover of this session: 3/70 on
#   a 4356e77 worktree and on 9138404 (s434), 3/68 on f702da3 (s435) and on
#   d0b52e9 (HEAD) — so THE FLIP is the cause, measured, not inferred.  The two
#   rows are TAP 55 "should fail to load" and 56 "check the second attempt also
#   fails"; both were `not ok` before and are `(missing)` now, because a drop in
#   a module this file requires DIES when reached and takes the rest of its
#   top-level form with it.  Nothing that PASSED was lost.  That is the flip's
#   own documented price, and this row is the first place it could be seen —
#   which is the argument for closing the never-refreshed hole.
#   io/pvbm.t read 20/8 in the parallel AND the #366 serial pass and 23/5 ALONE
#   again (the FIFTH time) — NOT edited.  io/open.t contention, uni/variables.t
#   the known unstable TIMEOUT flood.
# s437 (Fable review) — ONE ROW EDITED BY HAND: op/utf8cache.t DIFF 2/0 ->
#   TIMEOUT 2/0.  C_ok is unchanged; the STATUS moved because the #467 recovery
#   load (s434) carries the file past its first dying form into the "quadratic
#   pos" test (`"\x{100}" x 1000000` + `while ($x =~ /./g) { pos($x) }`), which
#   PCL runs QUADRATICALLY (task #477: 100k chars 4 s, 200k 15.7 s, perl 1M in
#   0.09 s — the match is the cost, not pos()).  Measured TIMEOUT 2/0 in the
#   parallel pass, in #366's serial re-run and ALONE on a quiet machine, three
#   times; it is not a timeouts.tsv allowance case because no allowance short of
#   ~400 s reaches the next row — the fix is #477, after which the row returns
#   to DIFF by EDIT.  io/pvbm.t read 20/8 in the parallel AND serial pass again
#   and 23/5 alone (the fourth time) — NOT edited.
# s436 part 2 (Q3 = #456 half (b) / #469, the PHASE MODEL) — 6 ROWS EDITED BY
#   HAND.  Every section's COMPILE phase (defs, BEGINs, captured use/require)
#   now precedes every section's RUN phase, as perl's own model has always
#   been, so a sub or a BEGIN written after a `package` switch is in place
#   before earlier run-time code needs it.  This population GAINED:
#
#     op/gmagic.t     C_ok   4 -> 30   (+26)   the file's helper packages are
#                                              defined before the code that
#                                              calls them
#     op/gv.t              130 -> 134  (+4)
#     uni/gv.t              51 -> 55   (+4)
#     comp/hints.t          15 -> 16   (+1)
#     op/sort.t            181 -> 182  (+1)    the perl-tests twin of the
#                                              bug-36430 row (see pass-baseline)
#     op/cond.t     C_notok   0 -> 1   the quarantined 20k-ternary file; C_ok
#                                      unchanged at 4, XDIFF either way
#   NET +36 C_ok, and NOTHING LOST.  Each of the five gainers was measured
#   alone on a `f332682` worktree (the commit before the change) and on HEAD,
#   and the base reproduces the blessed value exactly — so all five are this
#   change, not drift.
#
#   NOT EDITED, and worth knowing why: io/pvbm.t and io/open.t both read low in
#   the parallel pass, and io/pvbm.t read low AGAIN in the runner's own #366
#   serial re-run (20/8 vs its blessed 23/5) — but three later runs of it
#   ALONE, on both HEAD and the base worktree, give 23/5 every time.  A
#   fresh_perl-driving file can lose rows even to the "serial" re-run when the
#   machine is otherwise busy, so #366's serial verdict is a strong signal, not
#   a proof.  s435 reached the same conclusion about the same file.
# s436 (Opus 5, Q2 leg 4) — 13 ROWS EDITED BY HAND, never re-blessed: the price
#   of the s435 announce->DIE flip on this population.  A dropped statement now
#   DIES when reached, and this runner loads with recovery (#467), so the loss
#   unit is the enclosing TOP-LEVEL FORM — one file-level `my` wraps the rest of
#   the file in a single `let`, which is why ONE drop can cost 25 rows.  Ruled
#   accept, no narrowing (DECIDED s435); a loss out of proportion PROMOTES its
#   drop's owner task, it does not bend the flip.
#
#   Each row below was measured THREE times — once in s435's `--all --quick`
#   pass, twice here with `--jobs 1` over exactly these 13 files — and every
#   count agreed.  The 10 files the runner re-ran ALONE (#366) all came back
#   "REAL MOVE (both runs agree)".
#
#     file                    C_ok        cause (drops in the file)
#     op/substr.t             377 -> 347  2 lvalue-substr drops, census family 1
#     op/method.t             124 ->  99  1 indirect-object drop, owner #399
#     op/universal.t           79 ->  61  1 `++${"N::foo"}` drop  -- 18 rows for
#                                         ONE drop: owner PROMOTED
#     op/sub_lval.t            67 ->  50  35 lvalue-sub drops (registered absence)
#     comp/parser.t            65 ->  49  5 drops, the family-6 deliberate
#                                         parse-error torture rows
#     op/signatures.t         920 -> 915  1 lvalue drop; XDIFF -> DIFF (see below)
#     op/split.t              213 -> 212  0 drops: a fresh_perl CHILD drop (#472)
#     op/stash.t               48 ->  47  0 drops: same, child program
#     re/reg_eval.t             2 ->   1  0 drops: same; XDIFF -> DIFF
#     op/lexsub.t / op/try.t / run/runenv_randseed.t  C_ok FLAT, 1 / 2 / 5 rows
#                                         VANISHED (the fail count fell) — rows
#                                         that used to run ON a dropped
#                                         statement's undef
#     io/pvbm.t                23 ->  23  NO MOVE.  s435's `--all --quick` read
#                                         it as -3; three serial runs give the
#                                         blessed 23/5.  It was CONTENTION, and
#                                         the row is re-stated here unchanged.
#   TOTAL over the 13: C_ok 2017 -> 1903, i.e. **-114** (s435's -117 counted
#   io/pvbm.t's contention).
#
#   TWO STATUS MOVERS, both XDIFF -> DIFF, and both are DELIBERATE: a registered
#   file must diverge on EXACTLY its blessed row multiset (#185), and the flip
#   changed WHICH rows diverge — op/signatures.t gains 5 unregistered diverging
#   rows, re/reg_eval.t 1.  They are NOT re-blessed into
#   docs/perl-suite-expected-rows.tsv: those rows are lost to an aborted form,
#   not to the registered not-supported reason, and blessing them would hide the
#   flip's price behind an "expected divergence" label.  The DIFF is the debt.
#   (Contrast s434, where six files went XDIFF -> DIFF because they REACHED more
#   rows, and re-blessing was right.)
#
#   THE SIGNATURE COLUMN CHANGED SHAPE in the same commit.  Since the flip the
#   commonest aborted-form condition starts "PCL: statement not supported at
#   /home/.../perl-N.N.N/t/op/substr.t line N: ..." and the directory prefix
#   alone overran the 90-character budget, so every drop-caused abort came back
#   as ".../perl-N.N.N/t/o" — naming nothing, which is the one thing the
#   signature exists to do.  run-perl-suite.pl now collapses a DEEP absolute
#   path to its basename (two or more components, only where a path can start),
#   so these rows read "at substr.t line N: bar = "XXX"; -- PCL: Can't modify...".
#   Cosmetic by construction: read_snapshot compares status + C_ok + C_notok and
#   never $sig.  The ~40 OTHER rows that carry a path still show the pre-s436
#   text; they refresh at the next full bless.
# s434 (Opus 5, task #467) — THE WHOLE SNAPSHOT WAS RE-BLESSED, in one measured
#   `--all --jobs 4` pass, because THE RUNNER CHANGED, not PCL.  This runner now
#   loads the emitted CL with `pcl::p-load-with-recovery` — one top-level form at
#   a time, continuing past an uncaught die in a single form — which is what
#   sweep-perl-tests.pl has always done.  Nothing under Pl/, cl/ or lib/ moved
#   (generation stays v2-177), so not one row here is a compiler change.
#
#   WHY: measured s432, the same compiler change (#456 half (a)) cost the sweep
#   ONE row and this runner 94 — op/method.t 96/30 -> 44/7, op/sort.t 181/24 ->
#   142/9, op/lexsub.t 9/8 -> 6/6 — because a plain `--load` ends the file at the
#   first uncaught die.  A per-file count from here was therefore not comparable
#   to one from the sweep for any change that makes something die, and the
#   difference was the RUNNER.  Task #467; the rule is in
#   docs/test-infrastructure.md.
#
#   WHAT MOVED (each file re-run ALONE where the two runs disagreed — task #366;
#   the 22 movers over the runner's own cap were re-run alone in a second
#   `--jobs 1` pass):
#     * 52 files GAINED rows — every one a file that used to die mid-way.  The
#       largest: op/filetest.t 36 -> 434 rows, op/tr.t 94 -> 314, op/lexsub.t
#       12 -> 139, op/sub_lval.t 70 -> 194, op/method.t 51 -> 151, uni/fold.t
#       0 -> 1160, re/subst.t 0 -> 272, op/write.t 0 -> 42, op/leaky-magic.t
#       0 -> 71, uni/variables.t 1567 -> 29182 — that one A/B'd against the
#       `4356e77` worktree (old runner: DIFF 1248/319), so its flood of honest
#       failures after the aborted form IS this change; the count is a stopwatch
#       reading too, since the file now runs to the timeout.  TOTAL
#       C_ok over the population: 74803 -> 77128 (+2325 passing rows this runner
#       could not see before).
#     * 116 files changed only their SIGNATURE: a recovered die leaves no
#       "Unhandled …" header, so `crash:…` became `aborted-forms:N: <condition>`
#       (the runner counts recovered forms and refuses to call such a file OK).
#     * 2 files changed only their STATUS, and BOTH ARE PRE-EXISTING, with their
#       cause measured (the s421 rule: "pre-existing" is WHEN, not WHY).  A
#       `4356e77` worktree — the OLD `--load` runner — gives re/overload.t
#       TIMEOUT 3/0 and re/regexp_noamp.t TIMEOUT 796/109, i.e. exactly what this
#       runner now reports.  Their blessed DIFF rows were stale: both files sit
#       at the 90 s cutoff, so which side of it they land on is a stopwatch
#       reading, not a verdict about PCL.
#     * NO file lost rows.  The `--all --jobs 4` pass showed six #326 hang-set
#       files down 300-400 rows (re/regexp*.t) and op/utf8cache.t / re/pat_psycho.t
#       / io/pvbm.t moved; every one of them was re-run ALONE twice and came back
#       at or above its blessed value (re/regexp.t 795/110 vs a blessed 793/112;
#       io/pvbm.t 23/5 exactly; re/pat_psycho.t 11/0 exactly; op/utf8cache.t back
#       to DIFF 2/0).  Those were CONTENTION.  Note what that means for the six
#       hang-set rows: the file never finishes, so its C_ok is A STOPWATCH READING
#       (how far it got in 90 s), not a measurement of PCL — the s325/#195 note
#       below.  READ NO REGRESSION INTO THEM from a count alone.
#     * re/pat_psycho.t kept its rows (11/0) and lost its `control-stack-exhausted`
#       signature — ALSO PRE-EXISTING: the `4356e77` worktree with the old runner
#       reports the same 11/0 with no signature.  (The tempting explanation, that
#       per-form `eval` has a shallower host stack than `load`, is NOT what the
#       A/B says; the blessed signature was simply older than the tree.)
#     * SIX registered files (io/shm.t, mro/basic.t, mro/basic_utf8.t,
#       mro/next_skip.t, mro/next_skip_utf8.t, op/current_sub.t) went XDIFF ->
#       DIFF because they now REACH rows they used to die before, so their #185
#       row baselines no longer matched.  docs/perl-suite-expected-rows.tsv was
#       re-blessed for those six ONLY (`--bless-rows` over exactly those files;
#       1952 -> 1956 rows: io/shm.t 1 -> 22, op/current_sub.t 1 -> 15, both
#       next_skip 10 -> 11, and mro/basic.t 54 -> 33 + mro/basic_utf8.t 40 -> 28,
#       which SHRANK because rows that used to diverge now agree with perl).  The
#       five registered files that were already DIFF before this session
#       (comp/hints.t, mro/inconsistent_c3.t, mro/next_ineval_utf8.t,
#       mro/package_aliases.t, re/pat_re_eval.t) were deliberately NOT blessed.
#       Re-measured after the bless: all six back to XDIFF, twice, alone.
#   Files that do NOT die mid-way are unchanged to the row (verified before/after
#   on base/rs.t and op/join.t, and by the 5 need-harness rows below being
#   untouched).
#
#   NOT re-blessed here: the five rows for files `--all` does not run (see the
#   s431 block below) — they are need-harness files, so no default run measures
#   them, and this runner now PRINTS that as a named discrepancy at the end of
#   every full-scan run.  op/lex.t's census drop is why this run reports 76
#   drops against a 77-drop perl-t census; nothing is missing.
# s432 (Opus 5, #456 half (a): a CALLED p-declare-sub stub now runs the package's
#   AUTOLOAD, else DIES).  EIGHTEEN rows re-measured and spliced; each was
#   re-run ALONE (task #366: parallel and serial agree on all 18) and A/B'd
#   against a `bc02000` worktree, which splits them cleanly:
#
#   FOUR are CAUSED by this change (they differ between the two trees):
#     op/method.t   96/30 -> 44/7   the file already crashed (undef-fn:main::pl-new);
#                                   a stub call now dies EARLIER, so the plain
#                                   `--load` this runner uses ends the file sooner.
#     op/sort.t    181/24 -> 142/9  same shape, same crash signature.
#     op/lexsub.t     9/8 -> 6/6    same; the crash is now PCL's own
#                                   "Undefined subroutine &main::d called."
#     op/gmagic.t     0/0 -> 1/0    +1: the die lets one row emit before the crash.
#   All 94 lost rows sit AFTER the dying form in three files that never ran to
#   completion.  The perl-tests sweep lost exactly ONE row for the same change
#   because IT loads with p-load-with-recovery and this runner does not — the
#   asymmetry is task #467, not PCL behaviour.
#
#   FOURTEEN are PRE-EXISTING (byte-identical on both trees): the snapshot rows
#   predated their cause and nobody had re-run them.  op/state.t, op/switch.t,
#   op/smartmatch.t, op/coreamp.t, op/defer.t, op/tie_fetch_count.t and the four
#   class/*.t files went to XDIFF/TRANSPILE-FAIL with the s415 Track A refusal
#   registrations; op/index.t (49/13 -> 94/13), op/avhv.t (38/2 -> 39/1),
#   mro/package_aliases.t (XDIFF -> DIFF) and mro/package_aliases_utf8.t
#   (66/32 -> 73/40) had drifted with no row edited since.
# s431 (Opus 5) — FIVE files had NO ROW in this snapshot at all: comp/line_debug.t,
#   op/goto.t, op/lex.t, op/require_errors.t, run/dtrace.t.  Not quarantined, not
#   registered anywhere, simply absent — so a regression in any of them could never
#   read as a mover (the #176 family: an unmeasured file's rows vanish invisibly).
#   Found while pricing the announce->DIE flip, which needed op/lex.t's row.
#   Measured s431 on 54e2d80 (parallel + serial) and spliced with their FIRST
#   measurement, so none of them is a mover.  op/goto.t TRANSPILE-FAILs on the
#   #314 refusal ("my-lexical 'count' spans a package boundary"); run/dtrace.t is
#   NOTAP under perl itself, which says nothing about PCL.  Their rows carry a
#   `# s431 first measurement` marker.
# s427 row edits — THREE rows, two of them this session's and one PRE-EXISTING
#   drift.  The companion leg was 18 files (op/ re/ io/ comp/ uni/ mro/, the
#   dirs this batch touches), run A/B against a 753ecab worktree; the A/B diff
#   is exactly these rows and nothing else.  Each mover was re-run ALONE by the
#   #366 rule and both runs agreed.
#     op/glob.t   13/4  -> 14/4   task #415: `ok <~>, '~ works'` (line 110) was
#                 a DROPPED statement.  `<X>` is a readline only for a
#                 filehandle (perlop's whitelist); `<~>` globs, PPI's `<` `~`
#                 `>` split after a list-operator Word is rebuilt now that `~`
#                 counts as a glob metacharacter, and p-glob expands a leading
#                 tilde.  Drops 1 -> 0 (census row removed).
#     io/argv.t   23/30 -> 27/26  task #415, the same rule: `<<>>` (perl 5.22's
#                 double diamond) is a READLINE, and PPI hands it over as one
#                 Readline token whose inner text is `<>`.  The old blacklist
#                 rule emitted an unbound CL symbol `<>` — the file's own
#                 failure note in this snapshot — and a whitelist without that
#                 case would have globbed the string "<>" silently.  Four rows
#                 gained.
#     uni/gv.t    s427 measured 41/40 -> 53/28 as PRE-EXISTING (a 753ecab
#                 worktree measures 53/28 too, and the s427 tree is
#                 byte-identical to its base on that file).  SUPERSEDED by the
#                 s423 bullet below, which re-blessed the row to 50/31 from a
#                 463a8f8 base with its own A/B — a newer base and a measured
#                 cause, so that number stands and this one is only the record
#                 that s427 did not move it.
# s423 row edits — SIXTEEN rows in the uni/ + mro/ legs, all from task #418 (a
#   non-ASCII name is now spelled |…| everywhere, so it is the SAME symbol on
#   both sides of every seam).  Each mover was re-run ALONE (#366) on this tree
#   AND on a 463a8f8 base worktree; the two runs agree row for row.  FIFTEEN are
#   this session's; the sixteenth is PRE-EXISTING (see the last bullet).
#     uni/method.t          0/0   -> 23/9    the file used to die at the first
#                  method call on a fullwidth class; 32 rows now RUN.
#     uni/caller.t          1/3   -> 7/12    likewise; 4 rows -> 19.
#     uni/package.t         1/0   -> 8/10    likewise; 1 row -> 18.
#     uni/select.t          0/0   -> 1/4     likewise; 0 rows -> 5.
#     uni/readline.t        3/1   -> 4/1     the bareword filehandle fix.
#     mro/overload_c3_utf8.t   0/0 -> 7/0    now FULLY PASSING; its
#     mro/recursion_c3_utf8.t  1/7 -> 8/0    "utf8 package-name variant"
#     mro/recursion_dfs_utf8.t 1/7 -> 8/0    registrations were REMOVED from
#                  docs/perl-suite-expected.tsv (their non-utf8 twins always
#                  passed — the divergence WAS the name spelling).
#     mro/method_caching_utf8.t 10/0 -> 21/7  \  still XDIFF, but 18 registered
#     mro/basic_utf8.t         10/10 -> 13/12 |  rows stopped diverging;
#     mro/isa_c3_utf8.t        19/15 -> 21/13 |  docs/perl-suite-expected-rows
#     mro/isa_dfs_utf8.t       19/15 -> 21/13 /  .tsv re-blessed (18 removed,
#                  0 added — a pure narrowing of what each reason excuses).
#     mro/next_ineval_utf8.t   0/0  -> 0/1    the file now produces a row at
#                  all (it used to die before its first assertion).
#     mro/package_aliases_utf8.t 72/34 -> 73/40  +1 ok and +6 rows RUN.
#     uni/gv.t             53/28 -> 50/31   THE ONE LOSS, and it is three
#                  ACCIDENTAL passes: rows 226-228 are perl's
#                  `local *Ｊ = *Ｊ; *Ｊ = sub{}` idiom, and PCL's `local *NAME`
#                  loses the glob's scalar/array/hash slots — a PRE-EXISTING
#                  bug that is identical for ASCII names (task #433, probed on
#                  the base worktree).  Before #418 the fullwidth glob and the
#                  fullwidth cells were DIFFERENT symbols, so the broken
#                  glob-local could not reach them; now it can, and the file
#                  behaves exactly like its ASCII twin.  Same family as the
#                  s418 bless.t / split.t un-drops.
#     uni/parser.t         23/35 -> 26/32   PRE-EXISTING: the 463a8f8 base
#                  worktree measures 26/32 too, so the SNAPSHOT was stale (an
#                  earlier session moved it and did not splice).  Recorded here
#                  so the next A/B does not re-derive it.
# s424 row edits — TWO rows moved, both because of this session (task #423, the
#   glob VALUE vs glob REFERENCE distinction; a 12-file op/+io/ leg, each mover
#   re-run ALONE by the #366 rule, both runs agreeing):
#     op/gv.t      49/48  -> 61/36   s419d's regression recovered (a glob value
#                  in a scalar stringifies *main::foo again, so the file's
#                  s///, tr/// and ref() rows pass) plus eleven rows past it —
#                  ref($glob) is "" and ref(\$glob) is GLOB as perl says.
#                  A/B on an a2ac578 worktree measures 49/48.
#     op/substr.t 376/24  -> 377/23  the `\substr does not coerce its glob arg
#                  just yet` row (`my $x = *foo; is ref \$x, 'GLOB'`) — the
#                  same row perl-tests/substr.t gained in the sweep (374->375).
#   The other ten files in the leg (op/ref, op/magic, op/stash, op/select,
#   op/reset, op/readline, op/gmagic, io/open, io/argv, io/defout) are
#   IDENTICAL to this snapshot.
# s422 row edit — ONE row, signature only.  re/pat.t 1263/4 C:0/0 keeps its
#   numbers and LOSES its crash signature: task #419 made the emitted CL valid
#   UTF-8 (the eight >U+10FFFF literals are now `(p-unrepresentable-char N)`
#   forms that read and die on evaluation), so `sb-c::input-error-in-load` is
#   gone and the file LOADS.  It still yields no TAP because SBCL's DEFAULT
#   1 GB dynamic space is exhausted mid-run and the process dies hard, so the
#   runner records an EMPTY signature and "PCL produced no TAP output".
#   MEASURED the same session with `--dynamic-space-size 3072` (a
#   measurement-only PCLSbcl edit, reverted): 225/140 — 365 rows are behind
#   the heap — then `crash:simple-error: Failed to match at …/re/pat.t line N`.
#   Both walls and the runner-level decision they need are task #424.
#   uni/variables.t is the only other file whose emission #419 changed and it
#   does NOT move (1248/319 on a a2ac578 worktree and after): it dies on
#   `unbound:$}` before `load-as-source` ever reads the wide literal's form.
# s421 (Fable review) — a full --all --quick --jobs 4 run: 15 files differ from
#   this snapshot, every one re-run ALONE (all REAL by the runner's own test).
#   Ten are XDIFF 0/0 refusals: the six Track-A files s420 already lists, plus
#   class/class.t, class/inherit.t, class/phasers.t, class/threads.t (the s417c
#   `class NAME ;` refusal, #399 half b) — rows LEFT as the pre-refusal record,
#   same convention.  op/utf8cache.t TIMEOUT 2/0 = the registered contention
#   non-move.  FOUR files GAINED rows, A/B'd on a 47e0750 (pre-s420) worktree:
#     comp/parser.t   63/111 -> 65/110  one row IS s420's (#413: test 85
#                     `is CORE::print::foo, 43` — a package-QUALIFIED sub
#                     declaration now registers; 64/111 at 47e0750); the other
#                     +1 pre-dates s420 — cause not attributed, next measurement
#                     = bisection by row description over s396–s419 worktrees.
#     comp/colon.t    14/11 -> 15/10   pre-existing at 47e0750 (gain; same note)
#     io/fflush.t      2/2  ->  3/1    pre-existing at 47e0750 (gain; same note)
#     run/fresh_perl.t 55/36 -> 56/35  pre-existing at 47e0750: the registered
#                     "fdopen from a system descriptor…" row no longer diverges
#                     (STALE) — that ONE row deleted from perl-suite-expected-rows.tsv.
# s420 row edits.  ONE row moved because of this session: uni/gv.t 38/37 ->
#   41/40 (task #410 — eight `$NONASCII{…}` statements stopped dropping, so
#   six more rows run; +4 passing on the A/B, the base worktree measures
#   37/38).  Everything else that differed from this snapshot in the uni/,
#   mro/, re/ and op/ legs was verified PRE-EXISTING by running the same file
#   on a 47e0750 worktree, and is spliced here with that verdict:
#     op/bless.t     111/5  -> 111/6   (s418 #408, honest fail after the un-drop)
#     op/split.t     210/9  -> 213/6   (s418 #407)
#     op/filetest.t   16/6  ->  25/11  (s417 #372, stacked filetests)
#     op/bop.t       249/246-> 251/246
#     op/gv.t         50/47 ->  49/48
#     op/rt119311.t    4/1  ->   4/2
#     op/sprintf2.t 1525/13 -> 1679/25
#     mro/package_aliases_utf8.t 48/26 -> 72/34  (registered *rows-unstable*)
#   The six XDIFF-0/0 files (op/coreamp.t, op/defer.t, op/smartmatch.t,
#   op/state.t, op/switch.t, op/tie_fetch_count.t) are the Track A REFUSALS
#   (#371/#399/#400) and their rows are left as the pre-refusal record.
#   re/pat.t stays 0/0: the file does not LOAD at all — one `"\x{4000000}"`
#   literal is emitted as the 6-byte pre-2003 extended UTF-8 form, which is
#   not valid UTF-8, so SBCL's reader fails the whole file (task #419).  Its
#   five #412 drops are gone but nothing can observe that yet.
# s419 row edits: opbasic/concat.t 248/6 -> 249/5 (task #402 -- the [perl #124160]
#   interpolation-'.'-overload row now passes; serial+parallel agree).
#   re/pat_advanced.t 927/751 -> 950/730: PRE-EXISTING drift since this s392
#   snapshot, verified by running the file on a ba9045f worktree (same 950/730
#   before the s419 runtime change) -- attributed to the s404-era repairs, not
#   to s419.  re/overload.t, re/pat_psycho.t, re/speed.t report TIMEOUT at the
#   90s default with row counts IDENTICAL to their spliced --timeout-300
#   measurements (status word only; rows unchanged).
# taken-at: 582667e (full --all --jobs 6 run, s392, task #304); the 8 TIMEOUT
#   rows re-measured in the same tree with --timeout 300 and spliced in (the
#   s325/#195 method — a TIMEOUT row's C_ok is only "how far it got before
#   the cutoff", so it is not comparable across different --timeout values).
# AUDITED, not blessed blind: the 44 C_ok decreases vs the s323e snapshot each
#   have a verdict in docs/perl-suite-audit-s392.md.  Rows this snapshot
#   records as LOWER than reality, with their owning task, are listed there —
#   do not read a decrease here as "expected" without checking that file.
# s393 (#314 F-A1 fixed): opbasic/cmp.t re-measured and spliced —
#   TRANSPILE-FAIL -> 12078 ok / 0 not-ok, its pre-E4.1-flip value to the
#   digit.  The other 17 files of the #314 TRANSPILE cluster were re-run in
#   the same tree and are UNMOVED (their own six families); op/decl-refs.t
#   still reads 42 exactly.  So F-A1 was one file, as the audit said — and
#   that one file was 96% of the loss.
# s393 (#313 fixed): mro/basic_utf8.t and mro/next_edgecases_utf8.t re-measured
#   in the fixed tree and spliced in — both were recorded at their CRASHED
#   value ("Cannot proclaim a macro variable special"), the debt the audit
#   said was owed back.  basic_utf8 9 -> 10, next_edgecases_utf8 0 -> 9, and
#   both return to XDIFF.  That clears the audit's LIVE CRASH entry; the
#   #314 (TRANSPILE cluster) and #315 (14 undiagnosed) rows still stand.
# NOTAP = PERL produced no TAP (row not comparable; says nothing bad about PCL)
# XDIFF = expected divergence, docs/perl-suite-expected.tsv (a blessed not-supported.md gap)
# FIXTURE = harness artifact, docs/perl-suite-fixture.tsv (the MEASUREMENT differs, not PCL)
# NOT-RUN with QUARANTINED = deliberately not run this session; UNMEASURED, never passing
# s395 (#314 F-B fixed): `our NAMES <non-assignment tail>` lowers -- op/inccode.t
#   and op/repeat.t re-measured in the fixed tree and spliced.  op/repeat.t
#   TRANSPILE-FAIL -> 47 ok / 3 not-ok (the 3 are @_-aliasing / lvalue-cx /
#   `(1) x ~1`, all unrelated to the declaration).  op/inccode.t
#   TRANSPILE-FAIL -> 1 ok: it COMPILES now and dies at test 2 on the
#   coderef-in-@INC feature, which is that whole file (task #321).
# s395 (#314 F-A2 fixed): attributes on a variable declaration are stripped
#   (and announced) instead of refusing the file -- op/attrs.t and
#   uni/attrs.t re-measured in the fixed tree and spliced.  op/attrs.t
#   TRANSPILE-FAIL -> 28 ok / 61 not-ok, uni/attrs.t -> 8 / 26.  The
#   remaining not-oks are the ATTRIBUTE PROTOCOL itself (MODIFY_*_ATTRIBUTES
#   dispatch, the compile error perl raises for an unconsumed attribute, and
#   attributes::get) -- task #322, docs/not-supported.md.
# s395 (#314 single: the brace spelling of a punctuation variable):
#   re/pat_rt_report.t TRANSPILE-FAIL -> 2431 ok / 39 not-ok.  Four assertions
#   spelling @+ / @- as `@{+}` / `@{-}` inside a pattern held the whole 2513-row
#   file; `$#-` / `$#+` (one Magic token to PPI, not an ArrayIndex) was the
#   second half.  The file now runs to the `(?{ CODE })` block at line 343 and
#   dies there with a control-stack exhaustion -- a deliberate non-support
#   crashing instead of announcing, task #324, worth ~82 more rows.
# s395 (the four fillers): rows re-measured in a FULL --all run and spliced.
#   op/packagev.t 5 -> 198 (#319 version::is_strict/is_lax), op/select.t 0 -> 3
#   (#317 plan args flatten), re/regex_sets.t 1 -> 4 and now XDIFF plus
#   re/script_run.t XDIFF (#320).  The #316 glob-case fix reached FIVE files
#   the task had not predicted: uni/parser.t 17 -> 23, op/gv.t 47 -> 50,
#   uni/gv.t 36 -> 38, op/stash.t 46 -> 48, uni/stash.t 41 -> 43,
#   op/substr.t 375 -> 376 -- anything that prints or compares a glob.
# NOT spliced from that run: comp/require.t and the six re/regexp*.t files,
#   which came back TIMEOUT-shaped (~793 -> ~350) because the run shared the
#   machine with a second suite invocation and a timing measurement.  A
#   TIMEOUT row is not comparable (DECIDED s390/s392): comp/require.t
#   re-measured alone gives its snapshot value EXACTLY in every case:
#   comp/require.t 909 (--timeout 300), re/regexp.t 793 and re/regexp_noamp.t
#   794 (--timeout 400).  Those six re/regexp*.t files and comp/require.t have
#   no docs/perl-suite-timeouts.tsv row -- the #176 hole, still open.
# s396 (#325/#327/#329 refaliasing + the n-at-a-time foreach): FIVE rows
#   re-measured in the fixed tree and spliced.  op/const-optree.t
#   TRANSPILE-FAIL -> 86/62 and op/for-many.t TRANSPILE-FAIL -> 63/8 are the
#   two features landing; op/aassign.t 169 -> 173 is the refaliasing block in
#   that file; re/opt.t is TRANSPILE-FAIL -> XDIFF (it transpiles now and needs
#   re::optimization, a not-supported entry).  op/substr.t 376/23 -> 376/24 is
#   NOT a regression: at s395 it CRASHED before its last test, and the
#   refaliasing fix lets it reach test 400 and JUDGE it (failing on 4-arg
#   substr as an lvalue, task #209) -- one more assertion evaluated, C_ok
#   unchanged.  Verified against a worktree at 6fa8cb9.
# s396 NOT spliced, deliberately: comp/require.t and the six re/regexp*.t are
#   TIMEOUT-shaped and this run was contended (op/cond.t's solo phase held
#   5.4 GB).  comp/require.t returns its snapshot 909/835 exactly once given
#   the timeouts.tsv allowance added this session; the six are task #326.
# s398 (#153 FOLD chunk 3 + the `(LIST)[i]{k}` retag fix): ONE row moved --
#   op/ref.t 195 -> 196 (the arrow-less hash-subscript-after-a-list-slice
#   row, "hash deref from list slice w/o ->", was a PARSE-ERROR drop and now
#   evaluates).  Everything else UNMOVED in a full --jobs 8 run (emission was
#   byte-identical to main over the whole suite for the fold itself);
#   mro/package_aliases_utf8.t 48 -> 39 is its registered *rows-unstable*
#   noise (serial re-run 72/34); the six re/regexp*.t + re/overload.t
#   TIMEOUT-shaped as always at the 90s default (#326).
# s399 (#323, the three t/test.pl warning helpers made honest): FOUR rows
#   re-measured in this tree and spliced.  warning_is / warning_like /
#   warnings_like used to run the code and pass() unconditionally (the #202
#   class), so every row asserting a WARNING was manufactured.  With the real
#   t/test.pl bodies in the stub, the rows are honest and RED, and the cause is
#   the same for all of them: PCL emits no warnings-gated diagnostic
#   (docs/not-supported.md "Warnings-gated diagnostics are absent"; owner #221).
#     op/assignwarn.t  116/0 -> 96/20   (Use of uninitialized value, per op)
#     op/hashassign.t  309/0 -> 305/4   (Odd number of elements / Reference
#                                        found where even-sized list expected)
#     op/numify.t       32/0 -> 21/11   (Argument "..." isn't numeric)
#     op/utf8decode.t  644/42 -> 620/90 (malformed-UTF-8 warnings; note the
#                                        row count now MATCHES perl's 710 --
#                                        the old stub emitted 1 row where perl
#                                        emits 2, which is what produced the
#                                        "PCL's TAP numbering is offset for 592
#                                        rows" note this file used to carry.
#                                        Its 4 "Got expected Unicode
#                                        characters" fails are PRE-EXISTING,
#                                        verified in a worktree at f8ffd56.)
#   UNMOVED, verified in the same run: op/time.t 72/0 (its warning_is rows
#   expect NO warning), op/inc.t 60/15, op/split_unicode.t 107/40, re/subst.t
#   (crash, unchanged).
# s399 (#324, the harness): tools/run-perl-suite.pl was the ONLY runner without
#   --control-stack-size, so the companion suite ran PCL on SBCL's 2 MB default
#   while the gate, the sweep and ./runpcl all use 512 MB.  Four files
#   "crashed with control-stack-exhausted" in this measurement and nowhere
#   else.  Flag added; the two files measurable at the DEFAULT timeout are
#   re-measured and spliced:
#     re/pat_rt_report.t  2431/39 CRASH -> 2454/56, runs to the end
#     op/utf8cache.t         0/2  CRASH ->    2/0
#   The other two now RUN instead of crashing but are SLOW, so they need a
#   docs/perl-suite-timeouts.tsv decision before their rows are comparable
#   (measured at --timeout 300: re/pat_psycho.t 11/0, re/speed.t 1/0; both
#   TIMEOUT at the 90 s default).  Their rows are left as they were, still
#   carrying the crash signature, deliberately — see task #324.
#   VERIFICATION OF THE FLAG IS PARTIAL: a --all --jobs 4 run was stopped by
#   the user at 391 of 521 files; those 391 differ from this snapshot in
#   exactly ONE row, mro/package_aliases_utf8.t, which is the registered
#   *rows-unstable* file reading its known serial value (72/34).  The
#   remaining ~130 (re/, run/, uni/) are UNVERIFIED against the flag.
# s400 (#324): VERIFICATION COMPLETED — the remaining 135 files (re/, run/,
#   uni/) were run with the flag.  125 read this snapshot exactly.  Nine are
#   TIMEOUT-shaped and not comparable across runs (DECIDED s390/s392): the six
#   re/regexp*.t + re/overload.t (task #326), plus re/pat_psycho.t and
#   re/speed.t, which no longer crash and therefore now RUN the pathological
#   patterns they exist to time (the deliberate #324 consequence above).
#   The tenth, re/pat_advanced.t, MOVED and is spliced below — it is NOT the
#   stack flag: measured at PCL_SUITE_STACK_MB=2 (SBCL's old default) it gives
#   the same 927/751, and bisected over the s399 commits it changes at 6f04839,
#   the #323 warning helpers.
# s400 (#323 census CORRECTION): 927/751, was 937/732.  s399 declared the
#   #323 population CLOSED at eight companion files; it is ELEVEN.  The census
#   used a plain `grep -l`, which prints NOTHING for a file grep decides is
#   binary — and perl's regex test files are full of control bytes (the same
#   trap CLAUDE.md documents for .tsv, `grep -a`).  The three missed files are
#   re/pat_advanced.t, re/pat.t and re/reg_mesg.t; only the first produces rows
#   (re/pat.t crashes at load, re/reg_mesg.t dies on display_rx), and its move
#   is the same cause as the other four: warning_like now compares the warning
#   instead of pass()ing, and PCL emits no warnings-gated diagnostic
#   (not-supported.md "Warnings-gated diagnostics are absent", owner #221).
#   The 19 new not-oks are the `qr/(?g-o)/ warns` family and "Ignoring zero
#   length \N{} in character class warning"; +9 rows because the real helper
#   emits one row per expected warning where the stub emitted one per call.
# s400 (#207, which_perl): NO ROW MOVED.  cl/pcl-test.lisp's which_perl
#   returned a LITERAL perlbrew perl path; it now returns $^X, which the
#   runtime derives, and that resolves to the same interpreter here — the fix
#   is machine-portability (#278), not behaviour.
#   MEASURED BUT NOT LANDED (task #348, USER-decided s400): pointing which_perl
#   at $PCLPERL, so its children run PCL like fresh_perl_*/runperl already do
#   (#90).  All 19 companion callers were run that way — seventeen unmoved,
#   op/closure.t 267/3 -> 235/27 (honest: PCL's nested-sub-captures-foreach
#   gap, task #347), and run/cloexec.t 16/6 -> HANG at its first backtick
#   child, still hanging at --timeout 400 (task #346).  The sweep half:
#   perl-tests/closure.t OK 272 -> PARTIAL 240/28.  Two measurement HOLES for
#   ~56 vacuous rows made honest is the wrong trade while #176/#204 stand, so
#   the switch waits for #346/#347.
# s405 (#346 CLOSED): the run/cloexec.t hang above was #358 — PCL's
#   `open FH, "<&=N"` on a CLOSED descriptor built a stream on it (perl fails
#   EBADF), and the first read then retried EBADF forever.  The child of that
#   file exists to open an fd it was NOT given, so it spun.  With the runtime
#   fix the file reads 16/6 under a PCL child — IDENTICAL to its real-perl-child
#   row here, so #348's cloexec half is unblocked (#347 still stands).  The
#   fd LEAK fixed in s404l is a second, independent cause that also had to go:
#   `load` held the transpiled .lisp open, so fd 3 and 4 were valid in every
#   child.
# s405 (#340, op/try.t 0 -> 23/5): perl 5.34's try/catch/finally is
#   implemented — one Compound arm + the p-try macro + a PPI repair (finally is
#   not in PPI's try statement and its orphan swallowed the next statement,
#   ppi-upstream-bugs.md §18).  The five remaining rows are all OTHER families,
#   probed: two are the file-level lvalue-sub refusal (`fscalar = 123` — the
#   file's one drop, census row edited), one is caller() reporting the
#   generated .lisp file/line rather than the Perl ones (identical inside and
#   outside try — probed), one is the experimental-feature compile warning
#   (#221, warnings-gated diagnostics are absent), and one asserts perl's
#   parse-error TEXT for `catch` without (VAR), which principle 9 puts out of
#   scope.  Left DIFF rather than registered XDIFF so a real try regression
#   still shows.
# s405: SIX rows spliced after an `--all --quick` run compared file by file
#   against this snapshot (18 of 523 differed; the other 12 are --quick's 11
#   NOT-RUN files and mro/package_aliases_utf8.t, the registered rows-unstable
#   one).  Three are real movements, three are label-only:
#     op/ref.t          196/45 -> 197/44   (below, the s404l blank-line fix)
#     re/pat_re_eval.t  461 -> 462 failing (s404's #351/#354 — a statement that
#                                          used to VANISH now reports honestly)
#     io/shm.t          TRANSPILE -> XDIFF (0 rows either way: the file now
#                                          transpiles and reaches its registered
#                                          IPC::SysV crash; s404's repairs)
#     op/assignwarn.t, op/hashassign.t, op/numify.t: DIFF -> XDIFF, counts
#                                          IDENTICAL — registered in
#                                          perl-suite-expected.tsv after this
#                                          snapshot was taken.
# s405 (#347, op/lexsub.t TRANSPILE -> DIFF 6/8): narrowing the
#   nested-named-sub capture refusal (a PROMOTED lexical is legitimately
#   captured — the hoisted sub and the in-place code share the one cell) made
#   this file transpile.  It is the ONLY file in either population whose
#   emission the fix moves (emission-ab 638: 637 SAME / 1 DIFF / 1 RCDIFF), and
#   its 8 drops are `my sub` shapes — task #337's territory (census row added).
#   The fix's real target is invisible here: op/closure.t builds its programs
#   and runs them in a CHILD, so its row moves only when the child is PCL —
#   235/27 (s400) -> 267/3, i.e. the row it already had with perl children.
# s405 (op/ref.t 196/45 -> 197/44): the s404l blank-line fix in
#   tools/pclperl-for-tests — a PCL child no longer prints 56 empty stderr
#   lines, so one exact-output comparison in a file that spawns 23 children
#   matches.  The same +1 appears in the sweep half (perl-tests/ref.t 190 ->
#   191 passing).
# s406 (#348 LANDED — which_perl's children now run PCL): ONE row moved, and
#   it moved for a reason worth reading twice.
#     io/crlf_through.t  726/216 DIFF -> 942/0 OK
#   That file is `$main::use_crlf = 1; do './io/through.t'`, and through.t
#   pipes its data through a which_perl child.  With a REAL perl child the 216
#   failures were PCL's `:crlf` layer disagreeing with perl's; with a PCL child
#   both ends agree — so the row now says "PCL is self-consistent", not "PCL
#   matches perl".  Attributed with one measurement: `PCL_FRESH_PERL=real`
#   reproduces 726/216 exactly.  The gap itself is UNCHANGED and still owned by
#   task #139 (probed again s406: `>:crlf` writes 61 0a where perl writes
#   61 0d 0a; `<:crlf` returns 610d0a where perl returns 610a — the layer is a
#   no-op in both directions), and io/crlf.t still measures it directly
#   (16408/16421, 13 rows).  The other 18 companion callers did not move at
#   all: the 19 were run before and after and their failure logs are
#   byte-identical.
# s406 (measurement hygiene, NOT a row change): an `--all --quick --jobs 4`
#   run compared against this snapshot showed 36 differing rows, of which 22
#   were CONTENTION — every one of them re-run alone reproduced the snapshot
#   value exactly (op/lc.t, op/magic.t, op/ref.t, op/sprintf2.t, op/stash.t,
#   op/print.t, op/splice.t, op/stash_parse_gv.t, …).  A file that spawns
#   fresh_perl/runperl children loses rows when the machine is busy, and #348
#   made the run busier (19 files now spawn PCL children too).  The rule the
#   next session should follow: a companion row that moved is not a finding
#   until it has been re-run ALONE.  The remaining 14 = 11 --quick NOT-RUN,
#   mro/package_aliases_utf8.t (registered rows-unstable), op/utf8cache.t
#   (DIFF 2/0 -> TIMEOUT 2/0, same counts) and io/crlf_through.t above.
# s408 (#337, op/const-optree.t 86/62 -> 90/58 and DIFF -> XDIFF): `my sub` /
#   `state sub` are now LEXICALS (a scope-unique rename), so the four
#   "retval of my sub …" rows pass.  Those four were the file's only non-readout
#   divergences and are why it stayed UNEXPLAINED for nine sessions; a fresh
#   per-row read of the 58 that remain (28 inlinable + 25 :method readouts,
#   5 RT 134138) met the all-or-nothing bar, so the file is REGISTERED in
#   docs/perl-suite-expected.tsv with both reasons and its rows blessed.
#   Confirmed by a SOLO re-run, not just the --quick run.  The same run's only
#   other movers were mro/package_aliases_utf8.t (registered rows-unstable —
#   72/34 this time, 54/34/34 before) and op/utf8cache.t (TIMEOUT under load,
#   DIFF 2/0 alone): contention, per the s406 rule above, not findings.
# s408 (#363, an eval-mode DROP now DIES into $@; #375, the `qq {…}` delimiters):
#   FOURTEEN rows spliced, every one the SAME family — an `eval STRING` whose text
#   PCL cannot lower now reports $@ instead of silently returning undef, so a row
#   that asserted "no error" (or that read undef as the right answer) is now
#   honest.  WINS: op/cmpchain.t 1476 -> 1521 (+45) and op/signatures.t 891 -> 912
#   (+21) — both are `eval "<invalid perl>"` rows asserting $@ is set, which PCL
#   could not do before; both were REGISTERED, so their rows were re-blessed and
#   they are XDIFF again with 66 fewer diverging rows.  op/magic.t 167 -> 168 and
#   op/eval.t 129 -> 130 are #375's (a `qq < … >` string had the delimiters in it).
#   LOSSES, all false passes: op/smartmatch.t 143 -> 44 is the big one — that file
#   does `$res = eval $tstr; if ($@ ne '') { fail }`, so 99 unsupported `~~`
#   expressions were silently dropped, $@ stayed empty and undef happened to
#   satisfy the "does not match" assertion.  Same shape: op/dor.t -3, op/glob.t -3
#   ("eval'ed a glob"), op/postfixderef.t -2, op/exec.t -2, comp/parser.t -2,
#   op/yadayada.t -1, comp/retainedlines.t -1, op/packagev.t -1, op/smartkve.t
#   (+2 fails, same passing).  Each mover was RE-RUN ALONE (s406 rule);
#   op/utf8cache.t's TIMEOUT did not reproduce and is contention, not a move.
# s408b (#374, the keyword-named lexical sub): op/lexsub.t 6/8 -> 7/10.  `if` is
#   in ExprToCL's %RUNTIME_NAMES, so a bareword one lowered to the p-if MACRO
#   with whatever arity it had — `my $x = if if if` (legal perl when a sub is
#   named `if`, which this file asserts) emitted a zero-argument `(p-if)` whose
#   MACROEXPANSION error killed the file at load.  A statement keyword is never
#   a function, so the funcall generator refuses it and the statement is a
#   counted DROP (census 10 -> 14); with no crash-form left the file runs
#   further and stops at a different blocker (undef-fn:main::pl-F).  Emission
#   IDENTICAL everywhere else: corpus-diff 111, emission-ab over perl's t/
#   559 SAME / 1 DIFF (this file).
# s409 (Fable review of s408): two rows edited by hand from a --quick --all run
#   re-run ALONE (both agree).  op/signatures.t 912/334 -> 920/326: #374 half (a)
#   turned the eight STATEMENT-KEYWORD `sub ($x = if, $y) {}` evals into an
#   error (a keyword is never a function -> counted drop -> #363 dies into $@),
#   so the eight `<kw> does not swallow trailing comma` rows now pass; they were
#   registered rows, re-blessed with --bless-rows (STALE cleared, XDIFF again).
#   op/sub.t 51/14 -> 25/6 DIFF: #368's rule-12 die (`__SUB__` inside an ANON
#   sub) is reached at op/sub.t:214 (`sub { ...; CORE::__SUB__->() }->()`, the
#   [perl #122845] closure-recursion test) and aborts the file — 26 passing rows
#   lost to a die that replaced a silent 0.  Not measured in s408 (only
#   op/current_sub.t was re-run after #368).  Owner: task #378 (implement anon
#   __SUB__ as a self-reference rewrite; recovers the rows).
# s410 (#378 shipped): op/sub.t 25/6 -> 52/13 DIFF, spliced in.  #368's die is
#   gone -- an anon sub's `__SUB__` is now a source-level self-reference, so
#   the file no longer aborts at line 214.  It comes back ONE ROW BETTER than
#   the 51/14 it had before #368, because the [perl #122845] row now PASSES:
#   with the old no-op lambda there was no recursion at all, so $ok stayed 0.
#   Measured twice (parallel and serial, both 52/13, task #366's re-run).
#   The other two files that differed from the snapshot in the same run are
#   NOT moves: op/utf8cache.t TIMEOUT 2/0 in parallel and DIFF 2/0 alone (the
#   contention this file is already noted for above), and
#   mro/package_aliases_utf8.t is the registered rows-unstable one.
# s418 (#401 eval half shipped): op/sub.t 52/13 -> 53/12 DIFF, spliced in.
#   The s415 CORE::state normalisation had made the file TRANSPILE-FAIL
#   ("Parser2 TODO: state $x in named sub (string eval)" -- the honest die
#   for what had been a silent mis-parse); with the state cell riding the
#   eval-site capture alist (_eval_state_captures) the file transpiles
#   again and comes back ONE ROW BETTER than its s410 snapshot, because
#   the `eval q/$var/ in named sub in predeclared sub` row now PASSES.
#   Measured parallel and serial, both 53/12 (task #366 re-run agrees).
# s431 first measurement (see the s431 note in the header)
# s431 first measurement (see the s431 note in the header)
# s431 first measurement (see the s431 note in the header)
# s430 (#374(b) keyword-named lexical sub, B3.3): op/lexsub.t 7/10 -> 9/8 — the
#   `our sub if() {42} my $x = if if if` rows (lines 73, 76: 'lexical subs (even
#   our) override all keywords', '…from other packages…') now run and PASS; the
#   `state`/`my sub if` twins (lines 211/214, 578/581) un-drop too but sit
#   behind the file's abort (undef-fn:main::pl-F, unchanged).  Serial-confirmed
#   REAL MOVE by tools/run-perl-suite.pl op/lexsub.t; census 12 -> 4.
# s428 (#411 elided-arrow call, B3.1): op/ref.t 197 -> 198 — `(sub{})[0]()` (line
#   668) no longer DROPS; base b70ccaf measured 197/44 (matches this snapshot,
#   no drift), current 198/44, serial-confirmed REAL MOVE.  The other three
#   #411 movers do NOT change their row counts: op/closure.t (267/3) and
#   op/lexsub.t (7/10) un-drop statements bounded by an earlier failure, and
#   op/current_sub.t is 0/0 on BOTH trees (crashes on __SUB__, #378) — the drop
#   removal is honest (0 PARSE ERRORs emitted) but adds no row.
# s431 first measurement (see the s431 note in the header)
# s431 first measurement (see the s431 note in the header)
base/cond.t	4	0	4	0	OK		0
base/if.t	2	0	2	0	OK		0
base/lex.t	120	0	0	0	TRANSPILE	TRANSPILE-FAIL	-1
base/num.t	56	0	56	0	OK		0
base/pat.t	2	0	2	0	OK		0
base/rs.t	41	0	26	15	DIFF		0
base/term.t	7	0	7	0	OK		0
base/translate.t	257	0	257	0	OK		0
base/while.t	4	0	4	0	OK		0
class/accessor.t	10	0	0	0	XDIFF	TRANSPILE-FAIL | not-supported.md §Perl 5.38 class/field/method — deferred to a future version (surface syntax; desugars onto existing package/CLOS/p-sub machinery)	-1
class/class.t	10	0	0	0	XDIFF	TRANSPILE-FAIL | not-supported.md §Perl 5.38 class/field/method — deferred to a future version (surface syntax; desugars onto existing package/CLOS/p-sub machinery)	-1
class/construct.t	10	0	0	0	XDIFF	TRANSPILE-FAIL | not-supported.md §Perl 5.38 class/field/method — deferred to a future version (surface syntax; desugars onto existing package/CLOS/p-sub machinery)	-1
class/destruct.t	4	0	0	0	XDIFF	TRANSPILE-FAIL | not-supported.md §Perl 5.38 class/field/method — deferred to a future version (surface syntax; desugars onto existing package/CLOS/p-sub machinery)	-1
class/field.t	33	0	0	0	XDIFF	TRANSPILE-FAIL | not-supported.md §Perl 5.38 class/field/method — deferred to a future version (surface syntax; desugars onto existing package/CLOS/p-sub machinery)	-1
class/inherit.t	19	0	0	0	XDIFF	TRANSPILE-FAIL | not-supported.md §Perl 5.38 class/field/method — deferred to a future version (surface syntax; desugars onto existing package/CLOS/p-sub machinery)	-1
class/method.t	8	0	0	0	XDIFF	TRANSPILE-FAIL | not-supported.md §Perl 5.38 class/field/method — deferred to a future version (surface syntax; desugars onto existing package/CLOS/p-sub machinery)	-1
class/phasers.t	3	0	0	0	XDIFF	TRANSPILE-FAIL | not-supported.md §Perl 5.38 class/field/method — deferred to a future version (surface syntax; desugars onto existing package/CLOS/p-sub machinery)	-1
class/threads.t	0	0	0	0	XDIFF	TRANSPILE-FAIL | not-supported.md §Perl 5.38 class/field/method — the file also needs a threaded perl (NOTAP before s415; it now refuses at transpile like its siblings, task #371)	-1
class/utf8.t	4	0	0	0	XDIFF	TRANSPILE-FAIL | not-supported.md §Perl 5.38 class/field/method — deferred to a future version (surface syntax; desugars onto existing package/CLOS/p-sub machinery)	-1
cmd/elsif.t	4	0	4	0	OK		0
cmd/for.t	16	0	13	3	XDIFF	not-supported.md §Internals::* — aborts on Internals::stack_refcounted (survey row 2026-06-23; tests before the abort match perl)	0
cmd/mod.t	15	0	15	0	OK		0
cmd/subval.t	36	0	27	3	DIFF	unbound:F; aborted-forms:4: The variable F is unbound.	0
cmd/switch.t	18	0	18	0	OK		0
comp/bproto.t	16	0	10	6	DIFF		0
comp/cmdopt.t	44	0	44	0	OK		0
comp/colon.t	25	0	15	10	DIFF		0
comp/decl.t	9	0	6	0	DIFF		0
comp/filter_exception.t	4	0	2	2	DIFF		0
comp/final_line_num.t	1	0	0	0	DIFF		1
comp/fold.t	35	0	25	10	DIFF	aborted-forms:2: The function main::pl-INSTALL is undefined.	0
comp/form_scope.t	14	0	0	2	DIFF		0
comp/hints.t	31	0	16	13	DIFF	NOT fully registered: 3 unregistered diverging row(s): #15; tied empty hint hash is copied to inner scope; tied hint hash is copied to inner scope | not-supported.md §Lexical compile-time hints — $^H/%^H are inert globals, no compile-time scoping/restore (user bless 2026-07-28)	0
comp/line_debug.t	25	0	1	24	DIFF		0
comp/multiline.t	6	0	6	0	OK		0
comp/opsubs.t	36	0	31	5	DIFF		0
comp/our.t	7	0	0	0	TRANSPILE	TRANSPILE-FAIL	-1
comp/package.t	14	0	8	6	DIFF	aborted-forms:1: Error while parsing arguments to defmacro p-scalar-=: too many elements in (main::$a $ 'b)	0
comp/package_block.t	7	0	0	7	DIFF		0
comp/parser.t	191	0	49	49	DIFF	undef-fn:main::pl-FOO; aborted-forms:1: PCL: statement not supported at parser.t line N: "${; =pod =cut }"; -- Bug. Fell through. 	5
comp/parser_run.t	70	0	29	41	DIFF	unbound:$00	0
comp/proto.t	216	0	47	14	DIFF	aborted-forms:1: Undefined subroutine &main::N called at (eval N) line N. 	0
comp/redef.t	20	0	1	19	DIFF		0
comp/require.t	1750	0	909	835	DIFF		0
comp/retainedlines.t	109	0	18	91	DIFF	undef-fn:main::pl-foq	0
comp/term.t	23	0	23	0	OK		0
comp/uproto.t	32	0	13	21	DIFF		0
comp/use.t	87	0	42	45	DIFF	undef-fn:main::pl-true	0
comp/utf.t	4216	0	12	4204	DIFF		0
io/argv.t	52	1	27	26	DIFF		0
io/binmode.t	9	0	9	0	OK		0
io/bom.t	3	0	0	3	DIFF		0
io/closepid.t	3	0	0	0	DIFF	aborted-forms:1: Execution of a form compiled with errors. Form: (p-open $fhN "-|" $perl "-e" "sleep N")	0
io/crlf.t	16421	0	16412	6	DIFF	aborted-forms:1: The function PerlIO::pl-get_layers is undefined.	0
io/crlf_through.t	942	0	942	0	OK		0
io/data.t	4	0	0	0	DIFF		0
io/defout.t	22	0	21	1	XDIFF	not-supported.md §format / write report formatting — t7 $- (FORMAT_LINES_LEFT) is only nonzero after a real write(); $- is a static 0 because formats are not implemented	0
io/dup.t	29	0	4	0	DIFF		0
io/eintr.t	0	0	0	0	NOTAP		0
io/eintr_print.t	0	0	0	0	NOTAP	aborted-forms:1: Can't locate loadable object for module Time::HiRes in @INC	0
io/errno.t	16	0	16	0	OK		0
io/errnosig.t	1	0	0	1	DIFF		0
io/fflush.t	7	0	3	1	DIFF	aborted-forms:1: The function main::pl-_create_runperl is undefined.	0
io/fs.t	61	0	56	2	DIFF	aborted-forms:1: Could not delete the file "/tmp/pcl-test-N-N": Is a directory	0
io/getcwd.t	0	0	0	0	NOTAP		0
io/inplace.t	8	0	2	6	DIFF		0
io/iofile.t	3	0	1	2	DIFF		0
io/iprefix.t	2	0	0	2	DIFF		0
io/layers.t	55	0	0	0	DIFF	aborted-forms:1: The variable STDIN is unbound.	0
io/msg.t	18	0	0	0	XDIFF	aborted-forms:6: Can't locate loadable object for module IPC::SysV in @INC | not-supported.md §DynaLoader / XS binary extensions — IPC::SysV is XS; needs the pclxs bridge (docs/xs-shim-design.md). The generic bugs behind the old crash (labeled-loop goto, empty %SIG) were fixed s316.	0
io/nargv.t	7	0	11	2	DIFF		0
io/open.t	188	0	108	13	DIFF	aborted-forms:1: The variable STDOUT is unbound.; reaped 1 orphan(s)	1
io/openpid.t	10	0	8	2	DIFF		0
io/paragraph_mode.t	80	0	80	0	OK		0
io/perlio.t	46	2	22	0	DIFF	aborted-forms:1: Error opening #P"/tmp/nKNTIilTqW/t/": Is a directory	0
io/perlio_fail.t	15	0	6	9	DIFF		0
io/perlio_leaks.t	8	4	0	0	DIFF	aborted-forms:1: No such file or directory at /home/bernt/perlN/perlbrew/build/perl-N.N.N/perl-N.N.N/t/io/p	0
io/perlio_open.t	10	0	0	0	DIFF	aborted-forms:3: Execution of a form compiled with errors. Form: (p-open	0
io/pipe.t	27	0	8	0	DIFF	aborted-forms:1: Couldn't write to #<sb-sys:fd-stream for "descriptor N" {NCNFN}>: Broken pipe	0
io/print.t	24	0	23	1	DIFF		0
io/pvbm.t	28	0	23	5	DIFF		0
io/read.t	2	0	2	0	OK		0
io/say.t	13	0	13	0	OK		0
io/scalar.t	128	0	96	32	DIFF		0
io/scalar_ungetc.t	19	0	1	1	DIFF	aborted-forms:1: Can't locate object method "ungetc" via package "main" at - line N. 	0
io/sem.t	22	0	0	0	XDIFF	aborted-forms:2: Can't locate loadable object for module IPC::SysV in @INC | not-supported.md §DynaLoader / XS binary extensions — IPC::SysV is XS; needs the pclxs bridge (docs/xs-shim-design.md). The generic bugs behind the old crash (labeled-loop goto, empty %SIG) were fixed s316.	0
io/semctl.t	2	0	0	0	DIFF		0
io/shm.t	21	0	0	2	XDIFF	aborted-forms:8: Can't locate loadable object for module IPC::SysV in @INC | not-supported.md §DynaLoader / XS binary extensions — IPC::SysV is XS; needs the pclxs bridge (docs/xs-shim-design.md). The generic bugs behind the old crash (labeled-loop goto, empty %SIG) were fixed s316.	0
io/socket.t	45	0	0	0	DIFF		0
io/socketpair.t	8	0	0	0	DIFF		0
io/tell.t	36	0	26	0	DIFF	aborted-forms:1: The path #P"/tmp/nKNTIilTqW/t/>/tmp/pcl-test-N-N" does not exist.	0
io/through.t	942	0	942	0	OK		0
io/utf8.t	62	0	13	14	DIFF	aborted-forms:1: The function bytes::pl-length is undefined.	0
mro/basic.t	66	0	34	23	XDIFF	aborted-forms:2: Inconsistent hierarchy during CN merge of 'ISACLEAR'  | not-supported.md §mro — C3-only: DFS orders + get_mro 'dfs' default assertions	0
mro/basic_01_c3.t	4	0	0	1	XDIFF	aborted-forms:3: Can't locate object method "hello" via package "Diamond_D" at - line N.  | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/basic_01_c3_utf8.t	4	0	0	1	XDIFF	aborted-forms:3: Can't locate object method "hèllò" via package "Ｄiᚪၚd_D" at - line N.  | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/basic_01_dfs.t	4	0	0	1	XDIFF	aborted-forms:3: Can't locate object method "hello" via package "Diamond_D" at - line N.  | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/basic_01_dfs_utf8.t	4	0	0	1	XDIFF	aborted-forms:3: Can't locate object method "hèllò" via package "Ｄiᚪၚd_D" at - line N.  | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/basic_02_c3.t	10	0	3	3	XDIFF	aborted-forms:4: Can't locate object method "C_or_D" via package "Test::A" at - line N.  | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/basic_02_c3_utf8.t	10	0	3	3	XDIFF	aborted-forms:4: Can't locate object method "ƈ_or_Ḋ" via package "텟ţ::ଅ" at - line N.  | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/basic_02_dfs.t	10	0	3	3	XDIFF	aborted-forms:4: Can't locate object method "C_or_D" via package "Test::A" at - line N.  | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/basic_02_dfs_utf8.t	10	0	3	3	XDIFF	aborted-forms:4: Can't locate object method "ƈ_or_Ḋ" via package "텟ţ::ଅ" at - line N.  | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/basic_03_c3.t	4	0	0	1	XDIFF	aborted-forms:3: Can't locate object method "O_or_D" via package "Test::A" at - line N.  | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/basic_03_c3_utf8.t	4	0	0	1	XDIFF	aborted-forms:3: Can't locate object method "ᴼ_or_Ḋ" via package "텟ţ::ଅ" at - line N.  | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/basic_03_dfs.t	4	0	0	1	XDIFF	aborted-forms:3: Can't locate object method "O_or_D" via package "Test::A" at - line N.  | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/basic_03_dfs_utf8.t	4	0	0	1	XDIFF	aborted-forms:3: Can't locate object method "ᴼ_or_Ḋ" via package "텟ţ::ଅ" at - line N.  | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/basic_04_c3.t	1	0	0	1	XDIFF	not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/basic_04_c3_utf8.t	1	0	0	1	XDIFF	not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/basic_04_dfs.t	1	0	0	1	XDIFF	not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/basic_04_dfs_utf8.t	1	0	0	1	XDIFF	not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/basic_05_c3.t	2	0	0	1	XDIFF	aborted-forms:1: No SUPER::foo found from Diamond_D | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/basic_05_c3_utf8.t	2	0	0	1	XDIFF	aborted-forms:1: No SUPER::ᕘ found from Ｄiᚪၚd_D | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/basic_05_dfs.t	2	0	0	1	XDIFF	aborted-forms:1: No SUPER::foo found from Diamond_D | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/basic_05_dfs_utf8.t	2	0	0	1	XDIFF	aborted-forms:1: No SUPER::ᕘ found from Ｄiᚪၚd_D | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/basic_utf8.t	53	0	26	18	XDIFF	aborted-forms:2: Inconsistent hierarchy during CN merge of 'ᛁ앛ଌᛠ'  | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/c3_with_overload.t	7	0	7	0	OK		0
mro/c3_with_overload_utf8.t	7	0	7	0	OK		0
mro/complex_c3.t	12	0	11	0	XDIFF	aborted-forms:1: Can't locate object method "method" via package "next" (perhaps you forgot to load "next"? | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/complex_c3_utf8.t	12	0	11	0	XDIFF	aborted-forms:1: Can't locate object method "method" via package "next" (perhaps you forgot to load "next"? | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/complex_dfs.t	11	0	9	2	XDIFF	not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/complex_dfs_utf8.t	11	0	9	2	XDIFF	not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/dbic_c3.t	1	0	1	0	OK		0
mro/dbic_c3_utf8.t	1	0	1	0	OK		0
mro/dbic_dfs.t	1	0	0	1	XDIFF	not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/dbic_dfs_utf8.t	1	0	0	1	XDIFF	not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/inconsistent_c3.t	1	0	0	1	DIFF	NOT fully registered: 1 unregistered diverging row(s): ... got the right error with an inconsistent hierarchy | not-supported.md §mro + §error-text — C3 merge-failure error wording differs	0
mro/inconsistent_c3_utf8.t	1	0	1	0	OK		0
mro/isa_aliases.t	13	0	10	3	XDIFF	not-supported.md §symbol-table hashes — *Foo:: = *Bar:: package aliasing (stash writes)	0
mro/isa_aliases_utf8.t	12	0	10	2	XDIFF	not-supported.md §symbol-table hashes — *Foo:: = *Bar:: package aliasing (stash writes, utf8)	0
mro/isa_c3.t	35	0	22	13	XDIFF	not-supported.md §symbol-table hashes + §mro — *ISA glob manipulation / ISA magic internals	0
mro/isa_c3_utf8.t	34	0	21	13	XDIFF	not-supported.md §symbol-table hashes + §mro — *ISA glob manipulation / ISA magic internals (utf8)	0
mro/isa_dfs.t	34	0	21	13	XDIFF	not-supported.md §mro — DFS ordering spec tests (C3-only by design)	0
mro/isa_dfs_utf8.t	34	0	21	13	XDIFF	not-supported.md §mro — DFS ordering spec tests (C3-only by design, utf8)	0
mro/isarev.t	24	0	7	17	XDIFF	not-supported.md §mro — get_isarev needs live stash enumeration (returns [] provisionally)	0
mro/isarev_utf8.t	24	0	7	17	XDIFF	not-supported.md §mro — get_isarev needs live stash enumeration (returns [] provisionally, utf8)	0
mro/method_caching.t	36	0	21	15	XDIFF	not-supported.md §mro — perl method-cache semantics (CLOS has no cache to invalidate)	0
mro/method_caching_utf8.t	28	0	21	7	XDIFF	not-supported.md §mro — perl method-cache semantics (utf8)	0
mro/next_NEXT.t	4	0	1	0	XDIFF	aborted-forms:3: Can't locate object method "next::method" via package "Fuz" at - line N.  | not-supported.md §mro — next::method family (Tier 3, deferred: docs/mro-plan.md)	0
mro/next_NEXT_utf8.t	4	0	1	0	XDIFF	aborted-forms:3: Can't locate object method "next::method" via package "Fᶽ" at - line N.  | not-supported.md §mro — next::method family (Tier 3, deferred, utf8)	0
mro/next_edgecases.t	12	0	9	3	XDIFF	not-supported.md §mro — next::method family (Tier 3, deferred: docs/mro-plan.md)	0
mro/next_edgecases_utf8.t	12	0	9	3	XDIFF	not-supported.md §mro — next::method family (Tier 3, deferred, utf8)	0
mro/next_goto.t	4	0	0	0	XDIFF	aborted-forms:4: The function next::pl-method is undefined. | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/next_goto_utf8.t	4	0	0	0	XDIFF	aborted-forms:4: The function next::pl-method is undefined. | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/next_inanon.t	2	0	0	0	XDIFF	aborted-forms:2: Can't locate object method "method" via package "next" (perhaps you forgot to load "next"? | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/next_inanon_utf8.t	2	0	0	0	XDIFF	aborted-forms:2: Can't locate object method "method" via package "next" (perhaps you forgot to load "next"? | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/next_ineval.t	1	0	0	1	XDIFF	not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/next_ineval_utf8.t	1	0	0	1	DIFF	NOT fully registered: 1 unregistered diverging row(s): method resolved inside eval{} | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/next_method.t	5	0	0	1	XDIFF	aborted-forms:4: Can't locate object method "hello" via package "Diamond_D" at - line N.  | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/next_method_utf8.t	5	0	0	1	XDIFF	aborted-forms:4: Can't locate object method "헬ฬ" via package "Diӑmond_D" at - line N.  | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/next_skip.t	10	0	0	2	XDIFF	aborted-forms:8: Can't locate object method "method" via package "next" (perhaps you forgot to load "next"? | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/next_skip_utf8.t	10	0	0	2	XDIFF	aborted-forms:8: Can't locate object method "method" via package "next" (perhaps you forgot to load "next"? | not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/overload_c3.t	7	0	7	0	OK		0
mro/overload_c3_utf8.t	7	0	7	0	OK		0
mro/overload_dfs.t	7	0	7	0	OK		0
mro/package_aliases.t	54	0	102	46	DIFF	aborted-forms:2: Can't locate object method "womp" via package "Childclass" at - line N.  | NOT fully registered: 66 unregistered diverging row(s): *extra* '; *extra* '; *extra* '; +63 more | not-supported.md §symbol-table hashes — glob package aliasing + isarev	0
mro/package_aliases_utf8.t	52	0	73	40	XDIFF	aborted-forms:1: Can't locate object method "ວmᑊ" via package "C힐dᒡl았" at - line N.  | rows NOT checked (*rows-unstable*) | not-supported.md §symbol-table hashes — glob package aliasing + isarev (utf8).  ROWS NOT CHECKED (*rows-unstable*, s336): measured over three runs this file's divergence is nondeterministic in TWO ways — the number of PCL-only TAP rows after the crash varied 54 / 34 / 34, and one run came back TIMEOUT instead of DIFF.  The test tortures the stash with growing "인ንʵ::"-style names, so hash order decides where PCL gives up; the row set is not a baseline.  The other 107 registered files ARE row-checked.	0
mro/pkg_gen.t	7	0	0	0	XDIFF	aborted-forms:2: The function mro::pl-get_pkg_gen is undefined. | not-supported.md §mro — mro::get_pkg_gen method-cache generation counters	0
mro/pkg_gen_utf8.t	7	0	0	0	XDIFF	aborted-forms:2: The function mro::pl-get_pkg_gen is undefined. | not-supported.md §mro — mro::get_pkg_gen method-cache generation counters (utf8)	0
mro/recursion_c3.t	8	0	8	0	OK		0
mro/recursion_c3_utf8.t	8	0	8	0	OK		0
mro/recursion_dfs.t	8	0	8	0	OK		0
mro/recursion_dfs_utf8.t	8	0	8	0	OK		0
mro/vulcan_c3.t	1	0	0	1	XDIFF	not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/vulcan_c3_utf8.t	1	0	0	1	XDIFF	not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/vulcan_dfs.t	1	0	0	1	XDIFF	not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
mro/vulcan_dfs_utf8.t	1	0	0	1	XDIFF	not-supported.md §mro — C3-only, no mro:: API / next::method (provider planned: docs/mro-plan.md)	0
op/64bitint.t	425	0	425	0	OK		0
op/aassign.t	188	1	173	16	DIFF		0
op/alarm.t	5	0	5	0	OK	reaped 12 orphan(s)	0
op/anonconst.t	7	0	1	6	DIFF		0
op/anonsub.t	14	1	1	5	DIFF		0
op/append.t	13	0	13	0	OK		0
op/args.t	23	0	22	1	XDIFF	not-supported.md §DESTROY called by garbage collector — t23 is the ONLY failing row (22/1 vs perl 23/0): after `delete $_[0]` in a sub, the blessed arg's DESTROY must fire when the enclosing block drops the last reference. Probe s319: PCL agrees on t21/t22 (flag stays 0 inside f and after f) and only misses the scope-exit DESTROY — refcount-deterministic destruction, which PCL does not have.	0
op/arith2.t	9	0	9	0	OK		0
op/array.t	195	0	169	26	DIFF		0
op/assignwarn.t	116	0	96	20	XDIFF	rows NOT checked (*rows-unstable*) | not-supported.md section "Warnings-gated diagnostics are absent (`use warnings` is not modelled)" — ALL 20 diverging rows (96/20 vs perl 116/0) are one assertion shape: `warning_is { $x OP= … } 'Use of uninitialized value'` for each compound assignment operator, plain and tied. PCL emits no warnings-gated diagnostic; owner task #221. The rows became visible in s399 when task #323 stopped the t/test.pl helper manufacturing a pass. ROW CHECK OPTED OUT (*rows-unstable*, measured s399): the file iterates `keys %should_warn`, so BOTH sides emit the rows in a per-process random order and the description-multiset pairing reports a different missing/extra split every run (81 then 73 diverging rows in two consecutive runs). The COUNTS are stable.	0
op/attrhand.t	4	0	0	0	DIFF	aborted-forms:1: Failed to transpile /home/bernt/perlN/perlbrew/perls/perl-N.N.N/lib/N.N.N/Attribute/Handle	0
op/attrproto.t	48	0	17	28	DIFF		0
op/attrs.t	157	2	37	67	DIFF	aborted-forms:2: The function attributes::pl-_guess_stash is undefined.	0
op/auto.t	47	0	47	0	OK		0
op/avhv.t	40	0	39	1	DIFF		0
op/bless.t	116	2	111	6	DIFF		0
op/blocks.t	25	1	16	10	DIFF		0
op/bool.t	8	0	8	0	OK		0
op/bop.t	510	0	251	246	DIFF	aborted-forms:1: Invalid type 'P' in pack 	0
op/caller.t	111	1	28	84	DIFF		0
op/catch.t	12	0	10	2	DIFF		0
op/chars.t	34	0	34	0	OK		0
op/chdir.t	44	0	42	2	FIXTURE	Task #172 (s319): the shadow t/ SYMLINKS op/, and getcwd(3) returns the PHYSICAL path, so the test's `"$Cwd/op"` can never equal the post-chdir cwd. perl runs in the real tree, PCL in the shadow. Probed identical in both: bare chdir() honouring HOME *and* LOGDIR, delete $ENV{}, cwd() tracking a chdir, rel2abs(curdir), and chdir-into-a-symlink resolving to the physical path. Nothing in PCL is wrong; the two sides run in different trees.	0
op/chop.t	148	0	96	4	DIFF		0
op/chr.t	45	0	38	7	XDIFF	not-supported.md §Unicode semantics differences — ALL 7 failing rows (38/7 vs perl 45/0) are two blessed subsections. t10-13 are the `### use bytes` block (t/op/chr.t lines 28-31: under the pragma `chr(-1)` must be the single byte "\xFF"; PCL has no byte view, so it keeps "\x{FFFD}"). t40-42 are `### Code points above U+10FFFF` (lines 80-82: chr(0x110000)/0x1FFFFF/0x200000 in perl's extended UTF-8) — measured s319/s320: SBCL's char-code-limit is 1114112, so a CL string physically cannot hold them and (code-char #x110000) yields no character. Blessed in fable-answers-s318.md §11; task #173.	0
op/closure.t	281	1	273	3	DIFF	aborted-forms:1: The function main::pl-ff is undefined.	0
op/cmpchain.t	1750	0	1521	229	XDIFF	not-supported.md §Error compatibility for invalid Perl input (CLAUDE.md §9, verbatim example) — ALL 274 failing rows (1476/274 vs perl 1750/0) are the SAME assertion: `is eval("sub { \$a <=> \$b <=> \$c }"), undef, "… non-associative"`, i.e. invalid Perl must FAIL TO COMPILE. PCL is a transpiler for working Perl, not a validator, so it compiles the chain and returns a coderef (probe s319). Counted every row: 274 of 274 say "non-associative", none assert a value. The legitimately CHAINABLE comparisons (`$a == $b == $c`) work correctly in PCL — verified — so this is exclusively the rejection category, task #149 :principle9.	0
op/concat2.t	4	0	4	0	OK		0
op/cond.t	5	0	4	1	XDIFF	reaped 1 orphan(s) | not-supported.md §pathological-nesting — 20k-deep ternary eval: quadratic PExpr transpile + SBCL control-stack; DEFERRED post-R1 (user s309)	0
op/const-optree.t	148	0	90	58	XDIFF	not-supported.md §Readouts of perl's own internals -> "Constant-folding / inlinability and the :method attribute readout" (53 of 58 rows: 28 "… is/is not inlinable", 25 "… has (no) :method attribute" — both are diagnostics ABOUT an optree PCL does not build) AND §Error compatibility for invalid Perl input / CLAUDE.md §9 (the other 5: "… now throws exception (RT 134138)", where perl REJECTS `sub () { $x }` for a lexical modified elsewhere and PCL compiles it). REGISTERED s408, after #337 fixed the 4 "retval of my sub …" rows that were the file's real fix target and kept it UNEXPLAINED — a fresh per-row read of all 58 was the precondition the s399 note set.	0
op/context.t	8	0	7	1	DIFF		0
op/coreamp.t	778	0	0	0	XDIFF	TRANSPILE-FAIL | not-supported.md §`given`/`when` / smart match (`~~`) — `CORE::given`; the file also carries lvalue-sub drops (§Lvalue subroutines)	-1
op/coresubs.t	1109	0	0	0	TRANSPILE	TRANSPILE-FAIL	-1
op/cproto.t	254	0	71	183	DIFF		0
op/crypt.t	6	0	5	1	XDIFF	not-supported.md §Unicode semantics differences — t6 is the ONLY failing row (5/1 vs perl 6/0): `crypt` writing to a utf8 target must turn the target's UTF-8 flag OFF. Probe s319: PCL's crypt VALUE is correct (t5 passes); only `utf8::is_utf8($x)` differs, because PCL has no per-scalar UTF-8 flag (CL strings are always Unicode).	0
op/current_sub.t	22	0	8	3	XDIFF	aborted-forms:3: invalid number of arguments: N | not-supported.md §__SUB__ — current-sub reference DEFERRED (needs dynamic *current-sub* threaded through the calling convention; roadmap)	0
op/dbm.t	5	0	0	0	DIFF		0
op/decl-refs.t	402	0	42	165	DIFF	aborted-forms:2: ParserN TODO: state outside a block-level declaration at (eval N) line N. 	0
op/defer.t	28	0	0	0	XDIFF	TRANSPILE-FAIL | not-supported.md §`defer { … }` blocks — implementable, scheduled with Option B phase 2 Track C (task #371)	-1
op/defined.t	5	0	5	0	OK		0
op/defins.t	27	0	27	0	OK		0
op/delete.t	56	0	53	3	DIFF		0
op/die.t	25	1	5	15	DIFF		0
op/die_except.t	12	0	12	0	OK		0
op/die_exit.t	17	0	17	0	OK		0
op/die_keeperr.t	24	0	15	9	DIFF		0
op/die_unwind.t	13	0	8	5	DIFF		0
op/do.t	71	0	66	7	XDIFF	not-supported.md §Error compatibility for invalid Perl input (CLAUDE.md §9) + §DESTROY called by garbage collector — ALL 7 failing rows (66/7 vs perl 71/0) are two blessed categories, established row-by-row s321 after the TAP re-alignment (task #177) showed the old number-join had blamed two rows that PASS. (a) perl t63-t66 + PCL's 2 EXTRA rows: one `foreach my $mode (qw(subname("arg") subname() $subref("arg") $subref()))` loop asserting `$@ =~ /\Asyntax error/`. `do SUBNAME(LIST)` was VALID Perl before 5.20 and meant call-the-sub; PCL keeps that historical semantics, so the assertions fail AND each test's `fail()` guard fires (those are the 2 PCL-only rows — the same divergence, not a second defect). Ruled no-fix in fable-answers-s318.md §4, task #158 :principle9. (b) perl t68 "RT 124248": `f(do { 1; !!(my $x = bless []) })` requires the blessed temp to be DESTROYed at do-block exit (FREETMPS) before f() runs — refcount-deterministic destruction, which PCL does not have; same family as op/args.t t23. NB t67 (delete-helem copied) and t70 ($@ false on do dir) PASS — probed s321 — and t71 ($! is EISDIR on do dir) was FIXED s321 (p-do now carries the OS errno into $!).	0
op/dor.t	34	0	29	5	DIFF		0
op/dump.t	0	0	0	0	NOTAP		0
op/each.t	65	0	35	12	DIFF	aborted-forms:1: Can't locate loadable object for module this module in @INC	0
op/each_array.t	65	0	65	0	OK		0
op/eval.t	169	0	130	38	DIFF	undef-fn:main::pl-ARRAY	0
op/evalbytes.t	9	0	5	4	DIFF		0
op/exec.t	41	0	23	2	DIFF	aborted-forms:1: Couldn't execute "lskdfj": No such file or directory	0
op/exists_sub.t	19	0	18	1	XDIFF	not-supported.md §Error compatibility for invalid Perl input (CLAUDE.md §9) — t19 is the ONLY failing row (19/1 vs perl 19/0): `eval 'exists &t5()'` must set $@ =~ /not a subroutine name/. PCL DOES reject the invalid form — probe s319 gives "invalid number of arguments: 1" — so only the MESSAGE TEXT differs, and exact fatal-error text is not a goal (user 2026-07-28). Task #149 category :principle9.	0
op/exp.t	31	0	31	0	OK		0
op/fh.t	8	0	5	3	DIFF		0
op/filehandle.t	4	0	1	1	DIFF	aborted-forms:1: IO::Seekable::getpos missing at /home/bernt/perlN/perlbrew/perls/perl-N.N.N/lib/N.N.N/File	0
op/filetest.t	436	0	183	251	DIFF	aborted-forms:1: The function main::pl-f is undefined.	1
op/filetest_stack_ok.t	136	0	71	65	DIFF		0
op/filetest_t.t	7	0	0	0	DIFF	aborted-forms:1: Error opening #P"/dev/tty": No such device or address	0
op/flip.t	14	0	12	2	DIFF		0
op/for-many.t	71	0	63	8	DIFF		0
op/for.t	148	1	0	0	TRANSPILE	TRANSPILE-FAIL	-1
op/fork.t	28	0	1	0	DIFF		0
op/fresh_perl_utf8.t	1	0	0	1	DIFF		0
op/getpid.t	0	0	0	1	NOTAP	aborted-forms:1: Can't locate object method "new" via package "threads" at - line N. 	0
op/getppid.t	8	0	0	0	DIFF	aborted-forms:1: Execution of a form compiled with errors. Form: (p-pipe (vector $r $w))	0
op/glob.t	18	0	13	5	DIFF		0
op/gmagic.t	78	0	30	3	DIFF	aborted-forms:5: Can't call method init on an undefined value	0
op/goto.t	132	2	0	0	TRANSPILE	TRANSPILE-FAIL	-1
op/goto_xs.t	11	0	10	1	DIFF	aborted-forms:1: The function common-lisp:nil is undefined.	0
op/grent.t	3	0	3	0	OK		0
op/grep.t	77	0	71	6	DIFF		0
op/groups.t	4	0	4	0	OK		0
op/gv.t	304	0	134	65	DIFF	undef-fn:main::pl-fred; aborted-forms:3: Died at gv.t line N. 	3
op/hash-clear-placeholders.t	9	0	0	0	DIFF	aborted-forms:3: Can't locate loadable object for module this module in @INC While evaluating the form star	0
op/hash-rt85026.t	1	0	0	0	DIFF	aborted-forms:2: Can't locate loadable object for module this module in @INC	0
op/hash.t	494	0	8	7	DIFF	aborted-forms:1: Can't locate loadable object for module this module in @INC	0
op/hashassign.t	309	0	305	4	XDIFF	not-supported.md section "Warnings-gated diagnostics are absent (`use warnings` is not modelled)" — ALL 4 diverging rows (305/4 vs perl 309/0): `Odd number of elements in hash assignment` and `Reference found where even-sized list expected`, each in its `%h = …` and its `($s, %h) = …` spelling. Owner task #221; visible since s399 (#323).	0
op/hashwarn.t	20	0	5	15	DIFF		0
op/heredoc.t	138	0	32	106	DIFF	undef-fn:main::pl-EOF	0
op/hexfp.t	125	0	110	15	DIFF		0
op/inc.t	93	0	60	15	DIFF		0
op/inccode-tie.t	89	0	0	0	DIFF	aborted-forms:1: PCL: unsupported in string eval: multiple package sections at (eval N) line N. 	0
op/inccode.t	89	0	13	28	DIFF	aborted-forms:4: The value #<function (lambda (&rest %_args) :in	0
op/incfilter.t	153	0	0	0	DIFF	aborted-forms:2: Can't locate loadable object for module Filter::Util::Call in @INC	0
op/index.t	415	0	94	13	DIFF	aborted-forms:1: The function main::pl-formline is undefined.	0
op/index_thr.t	0	0	0	0	NOTAP	aborted-forms:1: This Perl not built to support threads 	0
op/infnan.t	1112	0	1106	6	DIFF		0
op/int.t	19	0	19	0	OK		0
op/isa.t	14	0	14	0	OK		0
op/join.t	43	0	39	4	DIFF		0
op/kill0.t	9	0	6	3	DIFF		0
op/kvaslice.t	38	0	22	16	DIFF		0
op/kvhslice.t	39	0	18	20	DIFF		0
op/lc.t	2716	0	2683	33	DIFF		0
op/leaky-magic.t	71	0	65	6	DIFF	aborted-forms:16: Can't locate tests.pm in @INC (@INC contains: /home/bernt/perlN/perlbrew/build/perl-N.N.N/	0
op/length.t	49	0	35	12	DIFF		0
op/lex.t	53	0	13	39	DIFF	unbound:%0	1
op/lex_assign.t	353	0	350	3	DIFF		0
op/lexsub.t	156	0	76	62	DIFF	undef-fn:main::pl-foo; aborted-forms:16: Undefined subroutine &main::d called. 	4
op/lfs.t	17	0	0	0	DIFF		0
op/list.t	0	0	0	0	NOT-RUN	QUARANTINED: task #160 — 10 GB SBCL blowup OOM-kills the run	-1
op/local.t	316	3	300	19	DIFF	undef-fn:main::|pl-main::|	0
op/localref.t	64	0	63	1	XDIFF	not-supported.md §DESTROY called by garbage collector — t64 needs refcount-deterministic DESTROY at local-restore	0
op/lock.t	5	0	0	0	DIFF	aborted-forms:5: invalid number of arguments: N	0
op/loopctl.t	66	1	63	4	DIFF		0
op/lop.t	47	0	47	0	OK		0
op/lvref.t	199	2	0	0	TRANSPILE	TRANSPILE-FAIL	-1
op/magic-27839.t	2	0	2	0	OK		0
op/magic.t	207	1	168	39	DIFF		0
op/method.t	163	0	99	26	DIFF	undef-fn:main::pl-new; aborted-forms:3: PCL: statement not supported at method.t line N: is((method $obj "a","b","c"), "method,a,b	1
op/mkdir.t	17	0	17	0	OK		0
op/multideref.t	65	0	42	10	DIFF	undef-fn:main::pl-local; aborted-forms:1: invalid number of arguments: N	0
op/my.t	59	0	52	7	DIFF		0
op/my_stash.t	9	0	4	3	DIFF	aborted-forms:2: The function Foo::pl-runperl is undefined.	0
op/mydef.t	2	0	0	2	DIFF		0
op/negate.t	48	0	48	0	OK		0
op/not.t	24	0	20	4	XDIFF	not-supported.md §Interned boolean constants (!0 / !1 identity) — t21/t22 "not 0/1 is read-only" need the shared read-only scalar; PCL returns a fresh mutable value per call	0
op/numconvert.t	1446	0	0	0	DIFF		0
op/numify.t	32	0	21	11	XDIFF	not-supported.md section "Warnings-gated diagnostics are absent (`use warnings` is not modelled)" — ALL 11 diverging rows (21/11 vs perl 32/0) are `numifying '…' trigger a warning`, i.e. perl's `Argument "…" isn't numeric`. The VALUE each string numifies to is asserted separately and PCL passes those. Owner task #221; visible since s399 (#323).	0
op/numify_chkflags.t	14	0	0	0	DIFF	undef-fn:main::pl-Dump; aborted-forms:1: Can't locate loadable object for module this module in @INC	0
op/oct.t	79	0	79	0	OK		0
op/or.t	13	1	11	3	DIFF		0
op/ord.t	38	0	38	0	OK		0
op/overload_integer.t	2	0	2	0	OK		0
op/override.t	36	0	3	17	DIFF	aborted-forms:2: Failed to transpile /home/bernt/perlN/perlbrew/perls/perl-N.N.N/lib/N.N.N/xN_N-linux/Confi	0
op/pack.t	0	0	0	0	NOT-RUN	QUARANTINED: task #160 — never measured; was queued behind op/list.t when it OOMed	-1
op/packagev.t	307	0	197	110	DIFF		0
op/pos.t	33	0	15	15	DIFF		0
op/postfixderef.t	128	0	95	26	DIFF	aborted-forms:3: The function main::pl-foo is undefined.	0
op/pow.t	268	0	268	0	OK		0
op/print.t	3	0	2	1	XDIFF	not-supported.md §Unicode semantics differences — t3 is the ONLY failing row (2/1 vs perl 3/0): reading MALFORMED (overlong) UTF-8 through a `<:utf8` in-memory handle and printing it must pass the bytes through unchanged. Probe s319: PCL emits c3 81 c2 af … where perl emits c1 af …, i.e. it holds the scalar as CHARACTERS and re-encodes on output — no per-scalar UTF-8/byte flag (CL strings are always Unicode). NB the open itself was ALSO broken until s319 (task #171, `<:utf8` was rejected outright); this row is what remains after that fix.	0
op/protowarn.t	12	0	4	8	DIFF		0
op/push.t	32	0	32	0	OK		0
op/pwent.t	2	0	2	0	OK		0
op/qr.t	37	0	23	14	DIFF		0
op/quotemeta.t	60	0	58	2	DIFF		0
op/rand.t	263	0	263	0	OK		0
op/range.t	162	0	155	7	DIFF		0
op/read.t	2116	0	2116	0	OK		0
op/readdir.t	16	0	13	3	DIFF		0
op/readline.t	35	1	25	7	DIFF	aborted-forms:1: The function PerlIO::pl-get_layers is undefined.	0
op/recurse.t	28	0	27	1	DIFF	crash:sb-kernel::binding-stack-exhausted: Binding stack exhausted.	0
op/ref.t	257	0	198	44	DIFF		1
op/refstack.t	0	0	0	0	NOTAP		0
op/repeat.t	50	0	47	3	DIFF		0
op/require_37033.t	10	0	4	2	DIFF	aborted-forms:2: Can't locate test_use_N.pm in @INC (@INC contains: /home/bernt/perlN/perlbrew/build/perl-N	0
op/require_errors.t	73	0	3	68	DIFF		0
op/require_gh20577.t	9	0	4	5	DIFF		0
op/require_override.t	10	0	7	3	DIFF		0
op/reset.t	45	0	25	19	DIFF		0
op/reverse.t	25	0	25	0	OK		0
op/rt119311.t	22	0	9	2	DIFF	aborted-forms:2: Can't find label test at /home/bernt/perlN/perlbrew/build/perl-N.N.N/perl-N.N.N/t/op/rtN.t	0
op/runlevel.t	24	0	0	0	DIFF		0
op/select.t	9	0	3	6	DIFF		0
op/setpgrpstack.t	3	0	3	0	OK		0
op/sigdispatch.t	29	0	24	3	DIFF	crash:sb-sys:interactive-interrupt: Interactive interrupt at #xNBNEANEBB.	0
op/signame_canonical.t	15	0	0	0	DIFF		0
op/signatures.t	1245	1	915	296	DIFF	aborted-forms:1: PCL: statement not supported at signatures.t line N: f() = "X"; -- PCL: Can't modify non-l | NOT fully registered: 5 unregistered diverging row(s): No warnings emitted within scope of no warnings "experime...; Signatured subroutine permits attributed scalar; `\&SUB` warns of experimental @_; +2 more | not-supported.md §Error compatibility for invalid Perl input (CLAUDE.md §9) + §Error message text and format — 357 of 360 residual rows (s316o; state-in-default fixed s316r): 262 "X does not swallow trailing comma" + t939/947/1219-1222 etc. are `isnt $@ ""` on evals that must FAIL to parse (invalid-syntax detection); 56 exact-error/arity-message text; 19+4 experimental-@_ / warning-line fidelity; t1/2/4/5 = §Signature syntax is read as a signature even with the feature off. All former fix-target rows are FIXED: t151/152/156 parenless list-op swallow (t017, s316s) + t021-family. t276-296 state-in-default re-init FIXED (task #134, s316r).	1
op/sigsystem.t	4	0	0	0	XDIFF	aborted-forms:1: Can't locate loadable object for module Time::HiRes in @INC | not-supported.md §DynaLoader / XS binary extensions — Time::HiRes is XS; needs the pclxs bridge. Before s316 this file scored OK only because empty %SIG made `skip ... if not exists $SIG{CHLD}` fire, so PCL emitted 4 skip-oks against perl 4 oks — a false pass.	0
op/sleep.t	4	0	4	0	OK		0
op/smartkve.t	18	0	3	15	DIFF		0
op/smartmatch.t	353	0	0	0	XDIFF	TRANSPILE-FAIL | not-supported.md §`given`/`when` / smart match (`~~`) — infix `~~`, removed in perl 5.42 (task #371)	-1
op/sort.t	205	0	182	23	DIFF	undef-fn:main::pl-revers	0
op/splice.t	34	0	34	0	OK		0
op/split.t	219	0	212	7	DIFF		0
op/split_unicode.t	147	0	107	40	DIFF		0
op/sprintf.t	559	0	534	25	DIFF		0
op/sprintf2.t	1704	0	1679	25	DIFF		0
op/srand.t	10	0	8	2	DIFF		0
op/sselect.t	23	0	17	6	DIFF		0
op/stash.t	54	1	47	7	DIFF	undef-fn:main::pl-SUPER	0
op/stash_parse_gv.t	5	0	5	0	OK		0
op/stat.t	108	3	0	0	DIFF	aborted-forms:1: The function main::pl-is_linux_container is undefined.	0
op/stat_errors.t	638	0	305	333	DIFF		0
op/state.t	166	0	0	0	XDIFF	TRANSPILE-FAIL | not-supported.md §`given`/`when` / smart match (`~~`) — one `given` block in an otherwise supported file; the refusal costs the file's rows, ruled with task #400	-1
op/study.t	43	0	43	0	OK		0
op/studytied.t	14	0	13	1	DIFF		0
op/sub.t	64	1	53	12	DIFF		0
op/sub_lval.t	211	0	50	49	DIFF	unbound:@119797; aborted-forms:11: PCL: statement not supported at sub_lval.t line N: get_st = N; -- PCL: Can't modify non-lv	35
op/substr.t	400	0	347	7	DIFF	aborted-forms:2: PCL: statement not supported at substr.t line N: bar = "XXX"; -- PCL: Can't modify non-lva	2
op/substr_thr.t	0	0	0	0	NOTAP	aborted-forms:1: This Perl not built to support threads 	0
op/svflags.t	16	0	0	0	DIFF	aborted-forms:2: Can't locate loadable object for module this module in @INC	0
op/svleak.t	156	0	0	0	TRANSPILE	TRANSPILE-FAIL	-1
op/switch.t	197	0	0	0	XDIFF	TRANSPILE-FAIL | not-supported.md §`given`/`when` / smart match (`~~`) — removed in perl 5.42; PCL refuses the file as perl does (task #371, Option B phase 2 Track A)	-1
op/symbolcache.t	8	0	4	3	DIFF	aborted-forms:1: The function main::pl-removed is undefined.	0
op/sysio.t	45	0	45	0	OK		0
op/taint.t	0	0	0	0	TRANSPILE	TRANSPILE-FAIL	-1
op/threads-dirh.t	0	0	0	1	NOTAP	aborted-forms:1: This Perl not built to support threads 	0
op/threads.t	0	0	2	19	NOTAP	aborted-forms:11: This Perl not built to support threads ; reaped 4 orphan(s)	0
op/tie.t	94	1	0	0	DIFF		0
op/tie_fetch_count.t	343	0	0	0	XDIFF	TRANSPILE-FAIL | not-supported.md §`given`/`when` / smart match (`~~`) — one infix `~~` statement; refused (task #371)	-1
op/tiearray.t	75	0	36	39	DIFF		0
op/tiehandle.t	67	0	12	32	DIFF		0
op/tiehash.t	27	0	20	7	DIFF		0
op/time.t	72	0	72	0	OK		0
op/time_loop.t	2	0	2	0	OK		0
op/tr.t	317	0	269	45	DIFF	aborted-forms:1: PCL: unsupported: a single generated top-level form of N chars exceeds the N-char limit (i	0
op/tr_latin1.t	2	0	1	1	DIFF		0
op/try.t	28	0	23	3	DIFF	aborted-forms:1: PCL: statement not supported at try.t line N: fscalar = N; -- PCL: Can't modify non-lvalue	1
op/undef.t	88	0	32	5	DIFF	undef-fn:main::pl-tcp; aborted-forms:1: The function main::pl-z is undefined.	0
op/universal.t	142	2	79	59	DIFF	aborted-forms:6: No SUPER::isa found from RTN::B	0
op/unlink.t	6	0	4	2	DIFF	aborted-forms:1: Couldn't unlink tempdir '/tmp/pcl-test-N-N': Directory not empty at /home/bernt/perlN/perl	0
op/unshift.t	19	0	19	0	OK		0
op/upgrade.t	64	0	64	0	OK		0
op/utf8cache.t	16	0	2	0	TIMEOUT	timeout	1
op/utf8decode.t	710	0	620	90	DIFF		0
op/utf8magic.t	6	0	2	4	DIFF		0
op/utfhash.t	99	0	91	0	DIFF		0
op/utftaint.t	0	0	72	17	NOTAP		0
op/vec.t	78	0	73	5	DIFF		0
op/ver.t	54	0	47	7	XDIFF	undef-fn:main::pl-gethostbyaddr | not-supported.md §use bytes / §use vN default warnings / §Control-character glob names — tests 21/23/25 (bytes view), 52 (Mordor), 54 (vN warnings); test 51 is the OPEN defelem arg-aliasing gap (task #131), a fix target	0
op/waitpid.t	1	0	1	0	OK		0
op/wantarray.t	28	0	27	1	DIFF		0
op/warn.t	32	1	19	14	DIFF		0
op/while.t	26	0	20	6	DIFF		0
op/write.t	636	0	18	24	DIFF	unbound:OUT; aborted-forms:19: The variable OUT is unbound.	1
op/yadayada.t	34	0	20	13	DIFF	undef-fn:main::pl-A	0
opbasic/arith.t	183	0	183	0	OK		0
opbasic/cmp.t	13280	0	12078	0	DIFF		0
opbasic/concat.t	254	0	249	5	DIFF		0
opbasic/magic_phase.t	7	0	0	5	DIFF		0
opbasic/qq.t	30	0	30	0	OK		0
re/alpha_assertions.t	2279	0	1784	74	DIFF	aborted-forms:2: end of file on #<dynamic-extent string-input-stream (unavailable) from ";;; pcl:..."> at (	0
re/anyof.t	1187	0	0	0	DIFF	aborted-forms:2: Can't locate loadable object for module Storable in @INC	0
re/begin-once.t	2	0	1	1	DIFF		0
re/bigfuzzy_not_utf8.t	1	0	0	1	DIFF		0
re/charset.t	8328	0	2776	2776	DIFF		0
re/fold_grind_8.t	213	0	0	0	DIFF		0
re/fold_grind_T.t	0	0	0	0	NOTAP		0
re/fold_grind_a.t	31	0	0	0	DIFF		0
re/fold_grind_aa.t	103	0	0	0	DIFF	reaped 1 orphan(s)	0
re/fold_grind_d.t	31	0	0	0	DIFF		0
re/fold_grind_l.t	113	0	0	0	DIFF		0
re/fold_grind_u.t	213	0	0	0	DIFF		0
re/keep_tabs.t	14	0	14	0	OK		0
re/no_utf8_pm.t	1	0	1	0	OK		0
re/opt.t	639	17	0	0	XDIFF	aborted-forms:1: The function re::pl-optimization is undefined. | not-supported.md §Readouts of perl's own internals: B:: optree inspection, re::optimization, XS::APItest — re::optimization reports perl's regex-COMPILER decisions (minlen/anchored/floating/stclass); cl-ppcre is a different optimizer, so there is no perl answer to report	0
re/overload.t	87	0	3	0	TIMEOUT	timeout	0
re/pat.t	1263	4	0	0	DIFF		3
re/pat_advanced.t	1678	0	950	730	DIFF		4
re/pat_advanced_thr.t	0	0	0	0	NOTAP	aborted-forms:1: This Perl not built to support threads 	0
re/pat_psycho.t	15	0	11	0	DIFF		0
re/pat_psycho_thr.t	0	0	0	0	NOTAP	aborted-forms:1: This Perl not built to support threads 	0
re/pat_re_eval.t	527	0	64	462	DIFF	NOT fully registered: 15 unregistered diverging row(s): hairy backslashes plain blank [9\'] =~ /^9\\\'/; hairy backslashes plain blank [9\\'] =~ /^9\\\\'/; hairy backslashes plain blank [9\\\'] =~ /^9\\\\\\'/; +12 more | not-supported.md §regex-code-blocks — whole file exercises (?{...})/(??{...}) (196 uses); cl-ppcre has no mid-match callback hook	0
re/pat_re_eval_thr.t	0	0	0	0	NOTAP	aborted-forms:1: This Perl not built to support threads 	0
re/pat_rt_report.t	2513	1	2454	56	DIFF		0
re/pat_rt_report_thr.t	0	0	0	0	NOTAP	aborted-forms:1: This Perl not built to support threads 	0
re/pat_special_cc.t	9	0	9	0	OK		0
re/pat_special_cc_thr.t	0	0	0	0	NOTAP	aborted-forms:1: This Perl not built to support threads 	0
re/pat_thr.t	0	0	0	0	NOTAP	aborted-forms:1: This Perl not built to support threads 	0
re/pos.t	8	0	8	0	OK		0
re/qr-72922.t	14	0	14	0	OK		0
re/qr.t	4	0	3	1	DIFF		0
re/qr_gc.t	2	0	0	2	DIFF		0
re/qrstack.t	1	0	1	0	OK		0
re/recompile.t	48	0	0	48	DIFF		0
re/reg_60508.t	1	0	0	1	DIFF		0
re/reg_email.t	13	0	0	0	DIFF		0
re/reg_email_thr.t	0	0	0	0	NOTAP	aborted-forms:1: This Perl not built to support threads 	0
re/reg_eval.t	8	0	1	7	DIFF	NOT fully registered: 1 unregistered diverging row(s): RT \#129199: | not-supported.md §regex-code-blocks — all 8 cases drive (?{...})/(??{...}) through fresh_perl_is; same family as pat_re_eval.t	0
re/reg_eval_scope.t	48	1	0	0	TRANSPILE	TRANSPILE-FAIL	-1
re/reg_fold.t	6786	105	0	0	DIFF	aborted-forms:2: Can't locate loadable object for module Storable in @INC PCL: Internals::SvREADONLY: a SCA	0
re/reg_mesg.t	3348	0	0	0	DIFF	aborted-forms:1: The function main::pl-display_rx is undefined.	0
re/reg_namedcapture.t	2	0	0	2	DIFF		2
re/reg_nc_tie.t	37	0	14	18	DIFF	undef-fn:|Tie::Hash::NamedCapture|::pl-STORE; aborted-forms:5: The function |Tie::Hash::NamedCapture|::pl-FETCH is undefined.	0
re/reg_nocapture.t	25	0	14	11	DIFF		0
re/reg_pmod.t	88	0	58	30	DIFF		0
re/reg_posixcc.t	2560	0	1544	7646	DIFF		0
re/regex_sets.t	96	0	4	84	XDIFF	not-supported.md §Regex extended character classes — cl-ppcre has no (?[ … ]); perl-only, so task #71 does NOT lift it	0
re/regex_sets_compat.t	2167	2	1600	258	DIFF	aborted-forms:2: end of file on #<dynamic-extent string-input-stream (unavailable) from ";;; pcl:..."> at (	0
re/regexp.t	2167	2	795	110	TIMEOUT	timeout	0
re/regexp_noamp.t	2169	0	796	109	TIMEOUT	timeout	0
re/regexp_nonull.t	2167	2	0	0	DIFF		0
re/regexp_normal.t	2169	0	1850	8	DIFF	aborted-forms:2: end of file on #<dynamic-extent string-input-stream (unavailable) from ";;; pcl:..."> at (	0
re/regexp_notrie.t	2167	2	795	110	TIMEOUT	timeout	0
re/regexp_qr.t	2167	2	795	110	TIMEOUT	timeout	0
re/regexp_qr_embed.t	2167	2	795	110	TIMEOUT	timeout	0
re/regexp_qr_embed_thr.t	0	0	202	1656	NOTAP	aborted-forms:3: The function main::pl-useithreads is undefined.	0
re/regexp_trielist.t	2167	2	795	110	TIMEOUT	timeout	0
re/regexp_unicode_prop.t	1110	0	778	332	DIFF		0
re/regexp_unicode_prop_thr.t	0	0	0	0	NOTAP	aborted-forms:1: This Perl not built to support threads 	0
re/rt122747.t	3	0	3	0	OK		0
re/rxcode.t	42	0	26	15	DIFF		0
re/script_run.t	185	0	0	185	XDIFF	not-supported.md §Regex script-run assertions — cl-ppcre has no (*script_run:…); LIFTED by the PCRE2 backend (task #71)	0
re/speed.t	59	0	1	0	TIMEOUT	timeout	0
re/speed_thr.t	0	0	0	0	NOTAP	aborted-forms:1: This Perl not built to support threads 	0
re/stclass_threads.t	0	0	5	1	NOTAP		0
re/subst.t	281	0	194	78	DIFF	aborted-forms:3: The value #<hash-table :TEST equal :COUNT N {NDN}> 	2
re/substT.t	0	0	0	0	NOTAP		0
re/subst_amp.t	13	0	0	13	DIFF		0
re/subst_wamp.t	281	0	0	0	DIFF		0
re/uniprops01.t	39811	0	0	0	XDIFF	aborted-forms:1: Could not run lib/unicore/TestProp.pl: No such file or directory at /home/bernt/perlN/perl | not-supported.md §Unicode — \p{} property classes in cl-ppcre (plan: docs/unicode-property-regex-plan.md)	0
re/uniprops02.t	39961	0	0	0	XDIFF	aborted-forms:1: Could not run lib/unicore/TestProp.pl: No such file or directory at /home/bernt/perlN/perl | not-supported.md §Unicode — \p{} property classes in cl-ppcre (plan: docs/unicode-property-regex-plan.md)	0
re/uniprops03.t	39793	0	0	0	XDIFF	aborted-forms:1: Could not run lib/unicore/TestProp.pl: No such file or directory at /home/bernt/perlN/perl | not-supported.md §Unicode — \p{} property classes in cl-ppcre (plan: docs/unicode-property-regex-plan.md)	0
re/uniprops04.t	39759	0	0	0	XDIFF	aborted-forms:1: Could not run lib/unicore/TestProp.pl: No such file or directory at /home/bernt/perlN/perl | not-supported.md §Unicode — \p{} property classes in cl-ppcre (plan: docs/unicode-property-regex-plan.md)	0
re/uniprops05.t	17394	0	0	0	XDIFF	aborted-forms:1: Could not run lib/unicore/TestProp.pl: No such file or directory at /home/bernt/perlN/perl | not-supported.md §Unicode — \p{} property classes in cl-ppcre (plan: docs/unicode-property-regex-plan.md)	0
re/uniprops06.t	31724	0	0	0	XDIFF	aborted-forms:1: Could not run lib/unicore/TestProp.pl: No such file or directory at /home/bernt/perlN/perl | not-supported.md §Unicode — \p{} property classes in cl-ppcre (plan: docs/unicode-property-regex-plan.md)	0
re/uniprops07.t	29260	0	0	0	XDIFF	aborted-forms:1: Could not run lib/unicore/TestProp.pl: No such file or directory at /home/bernt/perlN/perl | not-supported.md §Unicode — \p{} property classes in cl-ppcre (plan: docs/unicode-property-regex-plan.md)	0
re/uniprops08.t	30492	0	0	0	XDIFF	aborted-forms:1: Could not run lib/unicore/TestProp.pl: No such file or directory at /home/bernt/perlN/perl | not-supported.md §Unicode — \p{} property classes in cl-ppcre (plan: docs/unicode-property-regex-plan.md)	0
re/uniprops09.t	29848	0	0	0	XDIFF	aborted-forms:1: Could not run lib/unicore/TestProp.pl: No such file or directory at /home/bernt/perlN/perl | not-supported.md §Unicode — \p{} property classes in cl-ppcre (plan: docs/unicode-property-regex-plan.md)	0
re/uniprops10.t	26334	0	0	0	XDIFF	aborted-forms:1: Could not run lib/unicore/TestProp.pl: No such file or directory at /home/bernt/perlN/perl | not-supported.md §Unicode — \p{} property classes in cl-ppcre (plan: docs/unicode-property-regex-plan.md)	0
re/user_prop_race_thr.t	0	0	0	3	NOTAP	aborted-forms:1: This Perl not built to support threads 	0
run/cloexec.t	22	0	16	6	DIFF		0
run/dtrace.t	0	0	0	0	NOTAP		0
run/exit.t	17	0	14	3	DIFF		0
run/fresh_perl.t	91	0	56	35	XDIFF	unbound:H | not-supported.md §Error message text and format — 36 residual rows after s316e (user-blessed 2026-07-28): ~30 expect perl's exact fatal-death text (method-on-undef wording, "BEGIN failed--compilation aborted", read-only enforcement); rest = invalid-perl detection (CLAUDE.md §9) + one (?{}) case (§regex-code-blocks). The semantic gaps this file exposed (hole aliasing, defelem @_, Perl-4 tick, &$ref call) were all FIXED s316d/s316e — 55/91 rows match.	0
run/locale.t	44	0	0	0	DIFF		0
run/noswitch.t	3	0	2	1	DIFF		0
run/runenv.t	106	0	0	0	TRANSPILE	TRANSPILE-FAIL	-1
run/runenv_hashseed.t	278	0	0	0	DIFF	aborted-forms:1: Failed to extract seed: at /home/bernt/perlN/perlbrew/build/perl-N.N.N/perl-N.N.N/t/run/ru	0
run/runenv_randseed.t	9	0	0	4	DIFF	aborted-forms:1: PCL: statement not supported at test.pl line N: open(my $f, '<', $out) ? <$f> // '' : '' -	0
run/script.t	3	0	3	0	OK		0
run/switch0.t	1	0	0	1	DIFF		0
run/switchC.t	15	0	4	11	DIFF		0
run/switchDx.t	0	0	0	0	NOTAP		0
run/switchF.t	2	0	0	1	DIFF		0
run/switchF1.t	5	0	5	0	OK		0
run/switchF2.t	5	0	5	0	OK		0
run/switchI.t	4	0	0	4	DIFF		0
run/switchM.t	5	0	2	3	DIFF		0
run/switcha.t	2	0	0	1	DIFF		0
run/switchd-78586.t	2	0	0	2	DIFF		0
run/switchd.t	21	0	2	19	DIFF	undef-fn:main::pl-ARRAY	0
run/switches.t	140	0	6	99	DIFF	aborted-forms:1: sbcl: Failed to open 'tmpswitches.bak': No such file or directory at /home/bernt/perlN/per	0
run/switchn.t	3	0	1	1	DIFF		0
run/switchp.t	3	0	1	0	DIFF		0
run/switcht.t	0	0	9	4	NOTAP		0
run/switchx.t	8	0	0	3	DIFF		0
uni/attrs.t	35	0	8	26	DIFF		0
uni/bless.t	84	0	83	1	DIFF		0
uni/caller.t	18	0	7	12	DIFF		0
uni/chomp.t	446	0	446	0	OK		0
uni/class.t	12	0	0	12	DIFF		0
uni/eval.t	5	0	5	0	OK		0
uni/fold.t	19128	104	928	232	DIFF	aborted-forms:3: Can't locate loadable object for module Storable in @INC While evaluating the form startin	0
uni/goto.t	4	0	2	2	XDIFF	undef-fn:common-lisp:nil | not-supported.md §Error message text and format (CLAUDE.md §9; exact fatal-error text is not a goal — user 2026-07-28) — BOTH failing rows (2/2 vs perl 4/0) are `like $@, qr/Goto undefined subroutine &main::…/`, i.e. error-MESSAGE matches. PCL's BEHAVIOUR is correct in both: probed s321, it dies and does NOT call the sub in either case. t3 `goto &{"<utf8 bytes>"}` — perl declines to find the sub because the byte string lacks the UTF-8 flag; PCL declines too (it stores the sub under the CHARACTER name, so the byte string resolves to nothing) — same observable outcome, different mechanism, so do NOT read this row as PCL implementing a per-scalar UTF-8 flag (it has none). t4 `goto &因` on an undefined sub likewise dies. Task #149 category :principle9. NB rows 1-2 PASS: magical goto to a UTF-8-named sub, bareword and softref, both work. QUALITY NIT (not a blocker, no task): t3's message is SBCL's "The function common-lisp:nil is undefined" — an internal error leaking where a Perl-level one belongs (CLAUDE.md 12 in spirit).	0
uni/greek.t	0	0	0	0	NOTAP		0
uni/gv.t	206	0	55	33	DIFF	aborted-forms:1: Died at gv.t line N. 	2
uni/labels.t	10	0	3	5	DIFF	aborted-forms:1: Execution of a form compiled with errors. Form: (go :ここ)	0
uni/latin2.t	0	0	0	0	NOTAP		0
uni/lex_utf8.t	16	0	6	10	DIFF		0
uni/lower.t	11720	0	0	0	DIFF	aborted-forms:2: Can't locate loadable object for module Storable in @INC	0
uni/method.t	62	0	28	21	DIFF	aborted-forms:2: Can't locate object method "nÃ¨w" via package "Føø::Bær" at - line N. 	0
uni/opcroak.t	5	0	0	5	DIFF	aborted-forms:1: PCL: unsupported in string eval: multiple package sections at (eval N) line N. 	0
uni/overload.t	191	2	178	14	DIFF	aborted-forms:1: The function main::pl-todo_skip is undefined.	0
uni/package.t	18	0	8	10	DIFF		0
uni/parser.t	58	0	26	32	DIFF		0
uni/readline.t	7	0	4	1	DIFF		2
uni/select.t	3	2	1	4	DIFF		0
uni/sprintf.t	52	0	49	3	DIFF		0
uni/stash.t	48	1	43	6	DIFF		0
uni/tie.t	9	1	9	1	OK		0
uni/title.t	5936	0	0	0	DIFF	aborted-forms:2: Can't locate loadable object for module Storable in @INC	0
uni/tr_utf8.t	8	0	8	0	OK		0
uni/universal.t	90	0	31	49	DIFF	aborted-forms:2: The function main::pl-Àlìcè is undefined.	0
uni/upper.t	6230	0	1	1	DIFF	aborted-forms:2: Can't locate loadable object for module Storable in @INC	0
uni/variables.t	66880	0	1248	27934	TIMEOUT	timeout; aborted-forms:1: PCL: string literal: code point NxN is not implemented — above SBCL's char-code-limit (U	0
uni/write.t	8	0	0	8	DIFF		0
