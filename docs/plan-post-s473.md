# Plan: the queue after the s472 restart (s473, Fable, 2026-09-06)

**USER (2026-09-06): "Just keep starting new subjobs for the tasks and plan for them."**
Standing rules it runs under: TWO execution subjobs at a time (USER 2026-09-05); put things
LIVE before new work; Fable never executes what an Opus agent can do from a written design;
every round = one perf agent + one or two correctness agents (plan-post-s433 §s452).

Briefs live in `~/pcl-agent-scratch/s473/<label>/prompt.md` (the standing rules for all of them:
`~/pcl-agent-scratch/s473/COMMON.md`); the s470-era briefs still to launch are under
`~/pcl-agent-scratch/s470/` (BU, BZ, s1262).  A brief's `__MAIN__`/`__GEN__`/`__MYGEN__` are
filled at launch into `prompt.launch.md`.  An agent reports by writing `MERGE-READY: <sha>` as the
first line of its `scratch/<label>/STOP.md`; Fable gates the sha in a throwaway worktree
(`git worktree add --detach`), reads the hunks that matter, fast-forwards main, pushes, launches the
successor.

## 1. Where the tree is (2026-09-06 evening)
- main `21f6622c` (gen v2-840) = s470bt (B6 `tools/ir-conform` + the 345-case corpus + B7) and
  s470bx phase 1 (#1042 #1236 #1043 #1231 #1044) merged this session; CI green through `2e39cc67`.
- MERGED after this plan was written: **BX phase 2** (#1237 — the read family's errnos; corpus rows 122-io + 123-io left known-fail at the merge).  In flight: **BU** (perf round 29, `agent-a4ae344882cebc0bc`); **BY** (#1300 #1303 #682 + the
- **s474 (2026-09-06 night, Fable): BY MERGED `0d783d24`** (#1300 #1303 #682 #1338 — the cache surface; `PCL_CACHE_DIR` is a process fact, LAST-USE prune with a daily stamp, 0700 root refused loudly, `pcl --version/--cache-info/--no-cache`, ONE `%p-mtime`; gate 216/7517 xs-only with ZERO write-date signatures = #1338 closed) and **BU MERGED `8a879a57`** (perf round 29: #1250 per-SITE regex/subst/tr op, #1251 the s/// compiled record — json-rt +99 %, subste +62 %, regexg +32 %, textproc +30 %, runtime-only, no gen bump).  In flight: **s473t1** (`agent-af5e2bdefd8089352`) + **s473t2** (`agent-a66c9ca1780b383d5`) — the two slots (t1 was the USER's one-off third while BY/BU finished).
  absorbed **#1338**, `agent-a5d566deb35707e17`).
- Part B of plan-speed-and-ir-s470 is COMPLETE (B1–B7).  Part A continues by rounds.
- The ir-conform corpus's 58 known fails are the correctness worklist's new metric
  (`tools/ir-conform --strict`, the number to watch shrink); their owners by row count:
  #1249 10, #1240 8, #1241 7, #1248 5, #1245 5, #1244 4, #1243 4, #1242 4, #1247 3, #1246 3,
  #1190 3, #221 1, #155 1.
- Found this session: **#1338**, the shared-cache prune race — `p-cleanup-old-cache` stats a file
  a sibling process (or the gate's own `prove -j8` siblings) has just replaced; two Fable gates
  lost 17 rows to it (15/15 and 8/8 alone).  Absorbed by BY.

## 2. The queue (launch order; two slots)
| # | label | what | gen | after |
|---|---|---|---|---|
| 0a | **s473t1** — **MERGED s474 `9e938813`** (gen v2-960; three compiler fixes, five baselines edited row by row, #1431–#1445) | TRIAGE (USER, 2026-09-06 "soon"): the 13 PARTIAL files that stop early UNEXPLAINED — one cause per file, fix fillers, file the rest (`s473/s473t1`) | if a fix | a slot, FIRST after the in-flight pair |
| 0b | **s473t2** — **MERGED s474 `278d5eb8`** (baselines+docs only; 152 causeless rows attributed, #1446–#1460; CAUSES 0) | TRIAGE (USER, "soon"): the 105 blessed failure keys with no cause (pack.t's 47 EXCLUDED — USER: skip pack.t) + the 60 bug-notes without a task — cluster, probe, attribute, fix ≤1h clusters (`s473/s473t2`) | if a fix | a slot, right after s473t1 |
| 1 | **BU** | perf round 29 — **MERGED s474 `8a879a57`** (#1250 + #1251; two levers, round closed early: put things LIVE) | none (runtime-only) | done |
| 2 | **BZ** (IN FLIGHT s474b, launched 2026-09-07 07:30, worktree `agent-a18594e226bec1266`) | #1302 + #1304 install layout + installer tests, podman legs (`s470/s470bz`) | none | BY merged |
| 3 | **#1262** | `docs/caching.md` for users — Sonnet, `model: "sonnet"` (`s470/s1262`) | none | BY merged |
| 4 | **s473a** — **MERGED s475 `bb057f33`** (gen v2-1020; member 0 = #1513 CLOSED: the 64-bit boundary DOCUMENTED, numconvert.t registered XDIFF, guard int-boundary-01.t; #1243(a) `local($x) = LIST` is a LIST assignment, narrowed to scalar targets; #1260 a symbolic `local` assigns its initializer; #1192 + #1190 NOT attempted, each now carries a measured design (#1190 blocked on #1150 = s473b's #1241); filed #1339–#1341) | the `local` family: #1243(a)(b)(c) = #1192 + #1260, #1190 — 7 corpus rows | v2-870 | a slot |
| **00** | **THE PUBLISH PLAN — `docs/plan-publish-s480.md` (USER 2026-09-09: "drum up some interest"; Percolisp is an EXPERIMENT until DBI runs)** — FIRST next session: launch **s481a** (P1 the install wall: vendor cl-ppcre, drop the Quicklisp step, the container image; P3 the license files / release / CONTRIBUTING; IDs 1597–1606) into one slot, s473t4 into the other; Fable writes P2 (the README first screen + two whole-program bench rows); USER does P4 (the docs split) + the GitHub settings; P5 the announcement only after P1–P3 are live | publish | none (runtime registry push) | first slot |
| 5 | **s473b** — **MERGED s477 `3b153d7f` — ALL FOUR MEMBERS SHIPPED** (gen v2-1030; #1151 the nested `=` writes THROUGH the slot; #1241 = #1150 the READ path vivifies every intermediate via the `p-viv-container` marker + the existing chain walker, closing #1456 and the 7 ir-conform autoviv rows; #1152 an element compound assign evaluates container and KEY once, +0.55 % on its own bench row; #1010 a SLICE vivifies where its CONSUMER aliases it, ONE rule and five consumers incl. the `p-viv-slice` marker.  Sweep 0/0/0 TOTAL 18673, for.t 131 → 130 by EDIT because perl's own RT #2166 TODO row now fails as it does under perl.  Filed #1349–#1353) | the nested-element family: #1241 = #1150 (read vivifies), #1151, #1152, #1010 — 7 rows; bench-gated | v2-880 | a slot |
| 6 | **s473p** — **MERGED s475 `bfa11c72`** (gen v2-990; #1200 the string-eval DISK cache: `use JSON::PP; print 1` 1.13 → 0.41 s; #1409 ONE bare array is a foreach run: feread 0.47× → 0.30×; #1182 → `%p-array-grow-discarding`: listcopy +27 %, slices +20 %; #1411 filed; #995 open with one sized lever) | perf round 30 (USER 2026-09-07 "other speed tests first"): #1200, the single-array foreach lever, #995+#1056 loop-body box reuse, #1182 storage adoption — #1187 HELD | v2-890 | BU merged; the next perf slot |
| 7 | **s473c** — **SHIPPED default-OFF, MERGE-READY** (2 commits; the location register `p-line`/`p-file`/`p-loc-save` + five location scopes + ir-spec §6.3b; Kind-A gate `line-track`, **`%DEFAULT_OFF`** — **the flip is ONE line**: move the name to `%NOT_OFF_BY_NONE`, re-run gate+sweep+ir-conform, drop the seven annotated `1240` rows from `ir-conform/known-fail.tsv`.  SIZING: macro PASS (json-rt −0.52, moo-objs +0.23, textproc −1.16 %), micro FAIL on three (intloop= +5.5, cfor +4.2, collatz +5.3 %); one store ≈ 0.25 ns ≈ one cycle per statement, proved by LINEARITY 0/1/2/4 stores = +0/+6.71/+12.81/+19.66 %; (B) the cold-branch constant is unreachable — every message raises INSIDE a runtime function.  With the gate ON 7 of the 8 `1240` corpus rows pass (139-local has a second cause) and 8/8 probes are byte-identical to perl 5.40.3.  Filed #1359 (module cache key ignores `PCL_OPT`), #1360, #1361) | none — the DEFAULT emission is unchanged, corpus-diff IDENTICAL over 111; gen stays v2-1030 | done |
| 8 | **s473d** — **MERGED s477 `0c90e681`** (RUNTIME-ONLY, **no gen bump** — corpus-diff identical over 111; all five members shipped: #1230+#1245 the :DUAL marker, #1191=#1248(a) pp_modulo's two regimes, #1248(b) pp_pow's branch rule + the pack-impl `2**N` dependency, #1012 the %.15g rounded-exponent switch + sprintf's `%g` sibling, #1248(c) read-only storage = adjustable-without-fill-pointer; ten ir-conform rows left known-fail BY EDIT; filed #1369 the IV/NV residue and #1370 the string-side scalar collapse) | numeric representation: the CRASH pair #1245 + #1230, #1248(a)=#1191, #1248(b), #1012, #1248(c) — 10 rows | none | done |
| 9 | **s473e** — **MERGED s478 = main `033043d0` (gen v2-1080; #1161 + #1164 + #1244 CLOSED, #1379 filed; the residue is ONE shape: a direct named call into another compilation unit)** | loop control: #1244(a)=#1161, (b), (c), #1164 — 4 rows | v2-920 | a slot |
| 10 | **s473i** | product bugs: #1060 `--executable`, #1284 `-I` to the module transpile, #1119 content-keyed module cache | v2-930 | BZ + s473p merged (cache + `pcl` regions) |
| 11 | **s473h** | #1249's seven singletons (+ #1290), and #1242/#1117 SIZED (not designed) | v2-940 | a slot |
| 12 | **s473f** — **MERGED s480 (code tip `5b0012df`, gen v2-1220; all eight tasks CLOSED; representation (b) by measurement, +28 ns/op for the glob shape rejected; #1419–#1428 filed; Fable review fix = the hash-element glob-ref arm)** | the handle family: #1309 #1246 #1307 #1308 (representation measured first) #1233 #1220 #1074 #1247(b) — 4 rows | v2-1220 | done |
| 13 | **s473q** — **MERGED s476 `c4304834`** (docs-only: `docs/pcre2-spike-s473q.md`; the stop rule KEPT cl-ppcre — without JIT PCRE2 loses `subste` 1.188×, with JIT it loses `regexg` 1.134×; the strong case is PARITY +311/−7 re_tests rows and PCRE2 answers #196's 18 hangs; BLOCKER = the four images ship PCRE2 10.39/10.42/10.42/10.46 and there is no macOS CI leg → #71 stays PARKED, a USER decision; #1187 CLOSED unbuilt — no non-simple operand, cl-ppcre declaims `*string*` as the union `simple-string`; #1461 filed and RULED s476: a 256-way HASHED BMH skip table, version-pinned shadow, loud refusal; Fable filed #1528 = translator-vs-engine census of the 311) | THE REGEX ENGINE SPIKE #71 (PCRE2 via sb-alien: measurement + binding stop rule + portability record; USER: approved, NOT high priority) | v2-980 (only if member 2 ships) | AFTER s473p and one more non-engine perf round (the remaining S/M levers) |
| 13a | **s473r** — **MERGED s475 `5d59e447`** (perf round 31, RUNTIME-ONLY, no gen bump: #1514 the 68 ns were `int()`/`/`/`%` not `+` — `arith` −53 %, `use integer` −76 % (now FASTER than the plain loop), collatz −26 %; #1189 the package preamble stops re-running `defpackage` on an existing package — moo-objs 2.00 → 1.22 s, 54× → 29.6× perl; #986 SETTLED = #900's overload guard, 15.1 %, filed #1516; #883's arm re-measured 8.5 ns/elem → #1517; #1142 PROMOTED (a read-only grep costs the raw run 30.6 %); #1141 closed by a 65-file scan; #1518 the defclass half; #1515 the >2**53 divergences; #813/#924 closed) | perf round 31 = the LAST non-engine round before s473q: #1514 both halves (`use integer` as a FACT; the 73-vs-5 ns op-dispatch profile), #986 step 2 (the rounds-15–18 bisect), #1141+#1142, #883's open arm, #1189's breakdown instrument → A.2 row 7 sized or filed; #813 + #924 CLOSED as measured-declined | v2-1030 | the PERF slot when s473p merges; s473q after it |
| 13b | **s473s** — **MERGED s478 = main `aa58b3b7` (gen v2-1060; methret −27.5 %, textproc −24.5 %; #1516 not taken; filed #1531 → fixed s478)** (brief `s473/s473s/prompt.md`, written s476) | perf round 32 = THE METHOD-CALL ROUND (USER s476: method calls stay visible on the speed table, #1527): `methret` 1.51× profiled FIRST, then the lever the profile names — the OWN-CLASS half of #582 (an EQ-keyed per-class cache stamped by a global sub-generation; the inherited half stays blocked on the `@ISA` marker) or a fixed-arity `p-method-call-N` Kind-A entry (the per-call-SITE cache stays REJECTED, s444); #1518 the `p-defclass` no-op (moo-objs); #1461 as ruled; #1516 if time | v2-1060 if an emission changes | the PERF slot: the next free slot after s473d launched (s476) |  **DONE 2026-09-08 (5 commits, gen v2-1060).**  Member 1 = the profile (prologue 58.7 %, the LOOKUP inside it; allocation identical to `subret`, so the fixed-arity lever was ruled out unwritten).  Member 2 = #582 own-class half, `methret` **-27.5 %**, no generation counter needed because the cached value is the SYMBOL.  Member 3 = #1518 `p-defclass` SHIPPED but its prize MEASURED ZERO (the eval preamble declares a PACKAGE, not a class: 20 class forms N-independent with zero hits vs 12 package hits per iteration) -- kept for the IR.  Member 4 = #1461 as ruled, `textproc` **-24.5 % (+32.5 % faster)**, hashed == dense, RSS flat.  Member 5 (#1516) NOT taken: a full emission boat for a lever its own filing sized below the round bar.  Filed #1530. |
| 13c | **s473u** — **MERGED s478 = main `bd1e4144` (#1544 profile + lever A ×2, #1545 the transpile server: gate 463 s/2933 CPU-s → 86 s/395 CPU-s; #1546 filed = the next lever)** (brief `s473/s473u/prompt.md`, written s477 — USER: "Try, then see so this really works"; **FIRST LAUNCH next session**) | the GATE is the bug-throughput cost (7.5–8.5 min / ~2900 CPU-s, run 6–9× per batch): profile per file FIRST, then a persistent transpile server per test process (the fixed 190 ms per `pl2cl` spawn × ~2 800 calls), identical verdicts + byte-identical CL as the bar, before/after wall as the number; sweep + companion out of scope | none (tooling) | a slot; IDs 1544–1553 |
| 13b | **#1262** — **MERGED s475 `7c4f302f`** (Sonnet; `docs/caching.md`, the USER-facing page on the four cache layers incl. the s473p eval disk cache; filed #1335 = `pcl --cache-info` does not list `evals/`) | prose | done |
| 0c | **s473t3** | FILLER: the 79 blessed rows whose only cause is a `CATALOG` note get TASKS (one perl-probed reproducer per cluster, ~25 clusters; ≤2 one-hour fixes may ship) — `s473/s473t3/prompt.md`; brief WRITTEN s474b, launches on the next free slot | v2-1000 only if a fix ships | a slot |
| 0d | **#1501** (label s473t4; **ROUND 1 DONE 2026-09-13**, 7 commits, worktree `agent-acd33469931ad06c4`) | the companion suite gets the sweep's two triage checks.  SHIPPED: the CAUSE column + `CAUSES: N of M` through the one reader `tools/lib/PCLCauses.pm` (guard `tools/t/perl-suite-causes.t`, 30 rows / 72 ms); the census + the per-directory rounds t5a-t5f in §5; **CAUSES 0 → 2,269 of 18,836** and the row-shortfall UNEXPLAINED population **124 files / 67,765 rows → **107 / 63,751** (the `t/` half; the file is shared with the sweep population)**; ONE compiler fix (the inner-block `state` refusal, t/op/coresubs.t TRANSPILE → DIFF, guard `Pl/t/state-eval-01.t` 3 → 6) and ONE PPI lexer bug (§29); the test.pl-stub fix MEASURED then REVERTED as #1590 (it tips re/pat_advanced.t over SBCL's 1 GB heap).  Bars: gate 236/8100 xs-only, sweep GATE clean TOTAL 18675 (+0), companion 528 files with op/coresubs.t the ONLY real mover (25-of-26 byte A/B), corpus-diff IDENTICAL, no gen bump.  Filed #1577–#1590.  **OPEN for rounds t5a-t5f** | none (emission IDENTICAL) | rounds t5a-t5f per §5.5 |
| 0e | **#1061** — **MERGED s475 `46ede39d`** (label s1061; gen v2-1010; #1505 `import Foo::Bar` bare designator = a whole file unreadable, #1508 `$ref->{k} =~ s///` wrote to a VALUE; nine down-movers each bisected, #1506–#1512 + #1525 filed; board `cpan-board14-s474.tsv` 79/54/50, 2190/346, exactly four SLU rows moved vs s467, all explained) | FILLER (USER 2026-09-07 "Sounds good"): the CPAN board's nine unattributed DOWN-movers bisected + a fresh board snapshot on main with every mover since s467 attributed | if a fix | the slot after s473t3 |
| 0e′ | **#1513** | FILLER (USER 2026-09-07 "Sounds good"): DOCUMENT the missing 64-bit integer boundary as ONE not-supported entry (folds two old ones), register `op/numconvert.t` as an expected divergence (1446 rows leave UNEXPLAINED), ir-spec paragraph, guard row asserting the documented answers | none | after #1061 (same filler slot; ~1 h) |
| 0f | **#1502** — **DONE s473w** (label s473w; gen v2-1200; the board's ROW-level file `baselines/cpan-board14-fails.tsv` = 389 rows / 317 keys with **CAUSES 0**, `--rows` + `--diff` + `baselines/cpan-board-timeouts.tsv` in `tools/cpan-scoreboard.pl`, one TAP parser `tools/lib/PCLTap.pm`, the registry reader shared as `tools/lib/PCLTimeouts.pm`, guard `tools/t/cpan-scoreboard.t` 25 rows; snapshot `baselines/cpan-board14-s473w.tsv` 84/50/49 and 2213/353 with all SEVEN movers attributed; **#1525 + #1507 shipped**, #1506 + #1512 measured into their tasks, #1567–#1576 filed) | the board gets the sweep's treatment: a ROW-level fail file with a CAUSE column, one probe per cluster, ≤1 h clusters ship, CAUSES line in the board diff | v2-1200 | done |
| 0g | **#1503** | SCORE `cpan-tests/modules/` (Test-Simple, Text-CSV, YAML-Tiny, easy-targets) — a drop-census population today, never scored; decide Test-Simple's standing first | if a fix | any slot (filler) |
| 0h | **#1504** | WIDEN the board with on-disk pure-Perl dists in rounds of 2–3 (Moo + Getopt-Long first; Path-Tiny, Test-Deep; Pod-Simple, Math-BigInt, Memoize, HTTP-Tiny/-Message) + App-cpanminus as a real-program target | if a fix | #1502 merged; one round per slot |
| — | Fable designs | #1117 typeglob slots — **DESIGNED s480 (in the task: (A) exact 3–5 % vs (B) zero-cost side table with a documented read-revivification residue; Fable recommends (B); USER RULED s484: **(B)**, with a stop-rule — document instead if expensive; batch s484b)**; #582 `@ISA`-write invalidation (USER: box magic?); #71 PCRE2 (from s473p's measurement); #1202 extension fasl (USER-parked with pack) | | |

Task ID ranges reserved: BZ 1325–1334, #1262 1335–1337, s473a 1339–1348, s473b 1349–1358,
s473c 1359–1368, s473d 1369–1378, s473e 1379–1388, s473h 1389–1398, s473i 1399–1408,
s473p 1409–1418, s473f 1419–1428, BU 1250–1259 (old range), **#1595–#1596 filed s480; s481a 1597–1606; #1607 = the DBI milestone sizing (next free 1608)**, **s473t1 1431–1445, s473t2 1446–1460, s473q 1461–1470, s473t3 1471–1500, #1501 = the companion triage (s473t4), #1502–#1504 = the board work, s1061 1505–1512, #1513–#1514 filed s474b, s473r 1515–1524 (used 1515–1518; s473r bumped NO gen — v2-1030 is free for s473b), #1525 filed by s1061, #1526 filed s475; s473a used 1339–1341; #1527–#1529 filed s476; s473s 1529 was taken by #1529 → s473s uses 1530–1539**.  Next free after these: **1540** (#1526 = the element-access predicate filler, filed s475; #1527 = USER ask s476: the `methret` + `moo-objs` rows join the README speed table at its next quiet-box refresh; #1528 = the translator-vs-engine census of the 311 parity rows, for the #71 decision; #1529 = USER ask s476: `$ENV{_PCL_RUNTIME_}` so Perl code can detect PCL, synthetic in %ENV so children never inherit it, documented — appended to the s473h brief).

## 3. Standing decisions made this session (also DECIDED §s473)
- A baseline `.tsv` is BINARY: edit through `:raw` handles only.  s470bx's row-removal script
  read the companion fail baseline through a UTF-8 layer and rewrote 25 unrelated rows' Latin-1
  bytes as U+FFFD — the descriptions are join keys, so those rows would have become unpairable.
  Caught by `git diff main -- baselines/*.tsv | grep -a '^[-+]' | cut -f1 | sort | uniq -c`
  (a file that both loses and gains the SAME count of rows is the signature) and a byte `cmp`
  of the untouched rows against main.  Both are now part of Fable's merge read.
- A MERGE-READY sha is gated in a throwaway detached worktree when the agent keeps working in
  its own (a gate over a tree being edited is neither a gate nor safe).
- Rows that die with `Failed to find the WRITE-DATE of ~/.pcl-cache/modules/…` are #1338, not a
  regression: re-run alone, cite the task.  Until BY lands, every gate on this box may lose a
  file to it.
- The perf line's next rounds are ordered by §A.4.1/§A.4.3, not by A.2's guesses (s470bn's
- **s474 (USER): ONE regex engine at run time, never two; the #71 spike BEFORE #1187; approved 2026-09-07 ("Make it so") but NOT high priority — "do other speed tests first" (s473q after s473p + one more perf round); and a platform-touching change (an engine, a foreign library, an installer step) ships only with the install matrix green WITH it plus a macOS leg — "we are not writing software for our laptop".**  Rulings in DECIDED §s474.
  finding); the correctness line's metric is the corpus's known-fail count plus the sweep and
  companion totals.

## 4. Owed to the box (Fable, once quiet — which under continuous subjobs means "in a lull")
- `tools/run-perl-suite.pl --all --quick --jobs 1` over BV + s471a + BT: STARTED this session on
  main `637fc58e` (log in the session scratchpad); read its NOT-RUN and ROW DIFF blocks.
- The quiet-box bench re-measure (BN/BP rows + #1188 constants).
- A full `--all --jobs 4 --bless-stamps` once per round (I4).

## 5. The companion triage census (#1501 / s473t4 member 2, 2026-09-13)

Measured on the `eabff00d` tree from the three blessed baselines — `baselines/perl-suite-run.tsv`
(528 files), `baselines/row-shortfall.tsv` (190 `t/` rows) and `baselines/perl-suite-fails.tsv`
(18,336 rows / 265 files).  Nothing was re-run to build it: the runner already writes the STOP
point into the run baseline's `sig` column (`aborted-forms:N: <the recovery line>`), so the
census is a join, not 188 re-runs.  **`tools/run-perl-suite.pl` does NOT strip the PCL side's
`^;` lines** (the sweep runner does, `tools/sweep-perl-tests.pl:215` — that is what hid the
cause in s473t1), so the companion's causes were there all along and nobody had joined them.

### 5.1 The population
| status | files | | status | files |
|---|---|---|---|---|
| DIFF | 270 | | TRANSPILE | 10 |
| XDIFF | 104 | | TIMEOUT | 9 |
| OK | 101 | | NOT-RUN | 2 |
| NOTAP | 31 | | FIXTURE | 1 |

Shortfall: **190 files / 445,101 rows short, of which 124 files / 67,765 rows UNEXPLAINED.**
Row-level fails: **18,336 rows over 265 files, CAUSES 0 of 18,336** before this round.

### 5.2 The per-directory split (this is the schedule)
| dir | files w/ shortfall | rows short | unexpl. files | unexpl. rows | fail-row files | fail rows | clusters | run statuses |
|---|---|---|---|---|---|---|---|---|
| re | 42 | 352,093 | 23 | 19,458 | 34 | 9,836 | 1,560 | DIFF 34, NOTAP 14, OK 10, TIMEOUT 8, XDIFF 13, TRANSPILE 1 |
| uni | 11 | 84,476 | 7 | 42,520 | 22 | 1,389 | 623 | DIFF 22, OK 4, NOTAP 2, TIMEOUT 1, XDIFF 1 |
| op | 76 | 7,233 | 63 | 4,722 | 138 | 4,707 | 3,427 | DIFF 139, OK 44, XDIFF 21, NOTAP 8, TRANSPILE 6, NOT-RUN 2, FIXTURE 1 |
| comp | 8 | 276 | 7 | 274 | 20 | 1,614 | 373 | DIFF 21, OK 3, TRANSPILE 1 |
| run | 9 | 464 | 9 | 464 | 18 | 499 | 211 | DIFF 18, NOTAP 3, OK 3, TRANSPILE 1, XDIFF 1 |
| io | 15 | 263 | 12 | 204 | 29 | 264 | 162 | DIFF 29, OK 8, XDIFF 4, NOTAP 3 |
| base | 1 | 120 | 1 | 120 | 1 | 15 | 15 | OK 7, DIFF 1, TRANSPILE 1 |
| class | 9 | 100 | 0 | 0 | 0 | 0 | 0 | XDIFF 9, NOTAP 1 |
| mro | 17 | 73 | 0 | 0 | 0 | 0 | 0 | XDIFF 54, OK 16, DIFF 3 |
| cmd / opbasic | 2 | 3 | 2 | 3 | 3 | 12 | 8 | OK 6, DIFF 3, XDIFF 1 |

`re/` + `uni/` hold **98 %** of the rows short and **61 %** of the fail rows, and their shortfall
is dominated by files whose whole body is one engine or one table: the ten `re/uniprops*.t`
(324,976 rows short, every one `Could not run lib/unicore/…`) and the `uni/` case-mapping four
are ALREADY attributed to #1036 and must not be re-probed.  The genuinely open `re/` work is the
six `re/regexp*.t` TIMEOUTs (1,264 each, the same generated `re_tests` corpus) plus `re/reg_mesg.t`
(3,348), `re/charset.t` (2,776), `re/regexp_nonull.t` (2,169) and `re/anyof.t` (1,187).

### 5.3 The early-stopper tail (CHECK 1), banded
| band | files | rows short | UNEXPLAINED files |
|---|---|---|---|
| ≥ 1000 | 28 | 435,229 | 12 |
| 200–999 | 15 | 5,991 | 10 |
| 50–199 | 22 | 2,581 | 18 |
| 10–49 | 44 | 1,010 | 30 |
| 1–9 | 81 | 290 | 54 |

The ten files ≥ 200 short outside `re/` + `uni/` — round 1's target — and the 18 UNEXPLAINED in
the 50–199 band are the tables in §5.5/§5.6 below.

### 5.4 The fail-row clusters (CHECK 2)
The row baseline caps a file at ~500 rows, so a file's *cluster count* is what sizes it, not its
row count.  Clustered by the description with digits/hex/high-bytes normalised (the description
is Test::More's and is the join key):

| file | fail rows | distinct shapes | biggest shape |
|---|---|---|---|
| re/reg_posixcc.t | 7,130 | 727 | 200 (`*extra* chr(NxN) … =~ /[[:^alnum:]]/`) |
| op/write.t | 501 | 164 | 131 (`[at t/op/write.t line N]`) |
| uni/fold.t | 501 | 311 | 7 |
| op/coreamp.t | 501 | 490 | 3 |
| re/charset.t | 501 | 362 | 10 |
| comp/require.t | 500 | 83 | 119 (`(BB) saw expected error`) |
| re/regexp_qr_embed.t | 500 | 7 | 481 (`#N`) |
| re/regex_sets_compat.t | 500 | 56 | 288 (`#N`) |
| comp/utf.t | 500 | 17 | 306 (`UTF-NLE N N '\x{N}' with N spaces before it`) |
| uni/upper.t | 500 | 113 | 61 |
| re/alpha_assertions.t | 495 | 78 | 161 (`skipped. Pattern doesn't contain assertions`) |
| op/decl-refs.t | 360 | 343 | 3 |
| re/regexp_normal.t | 319 | 63 | 166 (`skipped. Test not valid for …`) |
| op/index.t | 311 | 309 | 2 |
| run/runenv_hashseed.t | 269 | 23 | 150 |
| op/tie_fetch_count.t | 212 | 211 | 2 |
| op/cproto.t | 183 | 183 | 1 |
| op/sub_lval.t | 180 | 106 | 71 |
| op/gv.t | 170 | 126 | 14 |
| comp/proto.t | 169 | 54 | 103 |

**The shape of the job:** `op/` has 3,427 clusters over 4,707 rows — a 1.4 rows-per-cluster
population, i.e. mostly ONE row per distinct assertion, so `op/` is attributed by FILE (its
early-stop cause explains the rows that follow it), not cluster by cluster.  `re/`, `comp/` and
`uni/` are the opposite: 9,836 rows over 1,560 clusters in `re/`, and the four biggest `comp/`
and `re/` files collapse to one engine fact each.

### 5.5 The rounds this census schedules (t5…)
| round | population | size |
|---|---|---|
| **t5a** | **DONE (s473t5a, 2026-09-13).**  The population was **eight** files, not ~20: the 18 UNEXPLAINED in the 50–199 band are ALL directories, and the `op/` half of the LIVE `baselines/row-shortfall.tsv` is six (decl-refs.t 195, incfilter.t 153, sub_lval.t 116, gv.t 113, tie.t 95, undef.t 51) plus the two TRANSPILE-FAILs (for.t 149 #1580, goto.t 134 #1581); `op/svleak.t` already carried NS:Regex code blocks, so the "three" is two.  **All eight now carry a CAUSE** (1,004 rows leave the `t/` half's UNEXPLAINED count).  Shipped: **#1581** (a `my` in a `package NAME { … }` BLOCK is not the file lexical — the RENAME's extent count, not the checker), **#1650** (an all-digit container name is an ordinary global; ONE name class for both free-global scans, carrying perl's real digit rule), **#1655** (`$|` desynced the string blanker, so string DATA was read as CODE — found by corpus-diff, both directions).  Filed: #1648 #1649 #1651 #1652 #1654 #1655; #1580 SIZED with its stop-rules.  Bars: gate 237/8165, sweep GATE clean TOTAL 18675 (+0) drops 5 = census, corpus-diff 5 of 111 (all declaration lines), gate-SET scan over both populations = ONE row (op/goto.t's refusal moving on), gen **v2-1360**.  **Leaves for a later round:** op/gv.t's hang (#1651) blocks its 113 rows AND t5b's 170 fail rows; op/for.t's pre-lex repair (#1580, 148 rows) and op/goto.t's forward goto (#1654, 134) are the two biggest single prizes left in `op/`; #1652's `run_multiple_progs` (159 rows over five files) waits on #1590's heap headroom. | 8 files, DONE |
| **t5b** | **DONE (s473t5b, 2026-09-13).**  Six files, **786 rows attributed** and **20 FIXED** — `op/cproto.t` and `op/packagev.t`'s VERSION/BLOCK half were already done by s484a, so the population was six, not seven.  ONE CAUSE PER FILE, each probed once: **op/sub_lval.t 178** = `NS:Lvalue subroutines` (#930, USER-deferred; every row in the file is downstream of a `sub : lvalue`, and the 116 missing ones sit behind 9 aborted forms); **op/gv.t 160 + uni/gv.t 138** = **#1664** (the TYPEGLOB MODEL: a stash slot holding a string is a PROTOTYPE and one holding a scalar ref is a CONSTANT SUB, a glob VALUE in a slot IS `*{name}`, `*x{IO}`/`*x{FORMAT}`/`*$io_ref` missing — and gv.t:586's `or die $@` over that very block is the abort BOTH files take, 111 + 118 rows); 10+10 = #221; **op/attrs.t 125** = `NS:Attributes on a variable declaration` (#322); **op/heredoc.t 44** = **#1666** (a here-doc in an `s///e` replacement whose body is outside the replacement text) + 42 invalid-input; **op/packagev.t 71** = **#1665** (perl's version grammar absent in BOTH strictness levels) + 8 = #1640 — s484a's blanket #1587 on those 79 replaced row by row.  ONE FIX: **PPI §30**, an indented here-doc's indentation is the whitespace before the DELIMITER TEXT, not `^\s*` of the terminator line (`<<~' EOF'`, `<<~''` were SILENT WRONG) — `Pl::Parser::_repair_indented_heredocs`, a fourth in-place token repair; **op/heredoc.t 32/106 → 52/86**, ROW DIFF 20 FIXED / 0 NEW; guard `Pl/t/heredoc-indent-01.t` 15 rows, §30 + 7 rows in `ppi-bug-report.t`.  CAUSES **2,072 → 2,748 of 18,601**; row-shortfall `t/` half UNEXPLAINED **101 files / 63,028 rows → 99 / 62,896**.  Bars: gate **238/8180** xs-only, sweep GATE clean TOTAL **18675 (+0)**, corpus-diff **IDENTICAL over 111**, emission-ab 27 SAME/0/0, **no gen bump** (the trigger shape occurs nowhere in `lib/` or `cpan-tests/`).  Filed #1664 #1665 #1666.  **Leaves:** #1665's `version->new` half (a `lib/` change whose breaking case — an unquoted v-string argument — is in the task); op/gv.t's verdict NOT spliced, rows-unstable three ways on one tree (#1651). | 6 files, DONE |
| **t5c** | **DONE (s473t5c, 2026-09-13).**  `comp/` is **26 files**, of which 3 already read OK and 1 TRANSPILE-FAILs (#1583); the population re-measured on this tree was **1,614 blessed fail rows over 20 files with CAUSES 0** plus **276 rows short in 8 files, 267 in 6 UNEXPLAINED**.  **Every one now carries a cause: 1,590 rows / 1,590 causes** (the count fell because `comp/proto.t`'s 169 blessed rows were replaced by this round's measured 145), and all six UNEXPLAINED shortfall rows are attributed.  Shipped: **#1681** (`\` over a `&`-MENTION is the SUB SLOT, per ELEMENT and in a `\&` prototype slot — `\(&foo)` was CALLING the sub and referencing the result; `comp/proto.t` 47/14 → **78/38**, shortfall 155 → 100, 28 blessed rows fixed).  Filed: #1682 (an undeclared bareword is CALLED — #266's ruled `no` arm, with proto.t's remaining 100 rows priced and #266's stop-rule), #1683, #1684 (`$^P` source retention, 115 rows over two files), #1685, #1686, #1687, #1688 (`require`'s return value and the "did not return a true value" fatal — ~353 of require.t's 500), #1689 + #1690 (use.t's 38), #1691, #1692, #1693 (UTF-16 source encoding, utf.t's 4,192 — its blessed #1115 attribution was STALE), #1694 + #1695 (parser.t's measured three-way split).  `comp/hints.t`'s 3 unregistered diverging rows registered under the reason the file already carries.  Bars: gate **239 files / 8196 rows** (only the 13 pclxs xs rows), sweep **GATE clean TOTAL 18675 (+0) drops 5 = census**, corpus-diff IDENTICAL over 111 and emission-ab lib 27 SAME → **no generation bump**, comp/ emission A/B 24 SAME / 1 DIFF (proto.t, the fix's positive control).  **Leaves for a later round:** the directory's five biggest prizes are all filed and none is a term-grammar problem — #1688 (require's return value, the module-load model), #1693 (source-encoding decode), #1684 (debugger retention), #1689 (version checking), #1682 (the bareword classifier's `no` arm, which also owns proto.t's residue and fold.t's aborts). | 26 files, DONE |
| **t5d** | **DONE (s473t5d, 2026-09-13).**  47 files / 763 blessed fail rows / 373 clusters, all attributed (**695 rows carry a cause** (perl-suite-fails.tsv CAUSES 8,278 -> 8,973 of 12,611); 16 left BY EDIT as fixed and two files were REPLACED from this round's own measurement -- io/utf8.t 39 -> 9, run/switches.t 134 -> 112), **and all EIGHTEEN of the directory pair's UNEXPLAINED CHECK-1 early-stoppers explained** (290 rows leave the t/ half's UNEXPLAINED count; three of them because the shortfall went to ZERO).  SIX FIXES, all `cl/` or harness so the emission is byte-identical: **#1696** the `+>>` (read/append) open mode, missing from ALL FIVE dispatch sites so the shorter `+>` arm won and left the second `>` on the FILENAME (io/tell.t 26/0 -> **36/0 OK**); **#1697** perl's LIST-form pipe open `open($fh,'-|',$prog,@argv)`, a macroexpansion ARITY ERROR that killed the whole top-level form (io/closepid.t 0/0 -> **3/0 OK**); **#1698** `bytes::length`, a `:bytes` package beside `:utf8` -- rule 9a says runtime, since the answer is about the SV's internal form and a Perl shim would have to ask the hardcoded `utf8::is_utf8` (io/utf8.t 23/4 -> 53/9; it is also Text::CSV_PP's `sep_len`/`quo_len`); **#1699** perl's `open` NEVER SIGNALS -- ONE helper `%p-open-file` at the six plain-file arms, catching FILE-ERROR only, plus O_APPEND on an APPEND anonymous temporary (io/perlio_open.t -> **10/0 OK**); **#1700** `-i[ext]` IS `$^I = "ext"`, one prelude line in `pclperl-for-tests` (run/switches.t 6/99 -> 28/112, shortfall 35 -> 11); **#1701** the real `t/test.pl` appends `args` RAW and PCL's stub quotemeta'd them, so `args => ['>', $file]` never redirected (io/inplace.t 2/6 -> 6/2).  A SEVENTH file moved that my own population could not see: the `--all --quick` companion (mandatory for the harness change) found **op/filetest_t.t 0/0 DIFF -> 7/0 OK**, which is #1699 exactly -- a `/dev/tty` open fails ENXIO and the signalled error took the file's only top-level form; eight files differed from the snapshot there and seven of them read IDENTICALLY on the 2ff53f8d extraction in a back-to-back `--jobs 1` pass, so only this one is mine.  FILED: **#1702** (a script's own `#!perl -SWITCHES` line is not honoured -- EIGHT files, ONE mechanism; the rewriting already exists in pclperl-for-tests and belongs in pl2cl), **#1703** (closing a `>&=` fdopen handle closes the shared descriptor and takes STDOUT out mid-file), **#1704** (ungetc needs a pushback STACK), **#1705** (a shipped shim the gating probe cannot see: `$Config{extensions}` empty + no `d_getpbyname` + POSIX with no `locale_h` = 97 rows, although sockets shipped 2026-06-27), **#1706** (the `-d` debugger hook model), **#1707** (`$^F` / fd inheritance across exec), **#1708** (`-I` does not reach the child's runtime `@INC`), **#1709** (perl's switch DIAGNOSTICS -- run/switches.t's 112-row residue), **#1710** (the eleven-file residue bundle, one measured shape per bullet).  Bars: gate **241 files / 8214 rows** xs-only (139 s wall), sweep **GATE clean TOTAL 18676 (+0)** drops 5 = census, corpus-diff **IDENTICAL over 111**, emission-ab 28 SAME/0/0, ir-conform 321/0/24/0, ir-host-leak 31 = main's, companion `--all --quick --jobs 2` 528 files with all EIGHTEEN snapshot differences classified (1 mine, 12 identical on the base extraction, 4 runner-flagged rows-unstable, 2 the #1651 hangs), **no gen bump** (v2-1360 stands).  **Leaves:** #1705's two-line Config half deliberately NOT taken (un-skipping io/socket.t + io/socketpair.t makes 53 rows RUN and is a ~50-row baseline churn to read row by row); #1703's fix needs an fdopen-ALIAS registry in the hot `close` path; ONE row PARKED (io/utf8.t:251, three probes vs perl all matching); FIVE companion files differ from the snapshot and are NOT MINE, each proved on the base extraction (io/pvbm.t the flapper's seventh recording, io/open.t -2 and run/switchM.t -2 both STABLE pre-existing drift, io/iofile.t +1, io/pipe.t +1) -- none spliced. | 47 files, DONE |
| **t5e** | **DONE (s473t5e, 2026-09-13).**  `re/` was **9,836 blessed fail rows over 34 files with CAUSES 0** plus **352,093 rows short over 80 files, 19,409 UNEXPLAINED in 22**.  Now **3,940 fail rows, ALL 3,940 caused**, and the shortfall's UNEXPLAINED half is **0 files / 0 rows**.  THREE FIXES, all three rule-11/rule-12 shaped: **FIX 1** (`[:^class:]` and `[:ascii:]` had no arm in the POSIX table, so a class AGREED with its complement — ONE range table, the negated spelling derived; `reg_posixcc.t` 1544/7646 → **1544/1016**, 6,630 rows); **FIX 2 = #1711** (the transpile server's STRICT `:encoding(utf-8)` substituted the `\x{...}` escape text for perl's non-characters and desynchronised its own char-counted protocol, so `eval "\x{10FFFF}"` died `end of file on string-input-stream` — `:utf8`, the layer `pl2cl`'s own STDOUT already uses; the three `do regexp.t` drivers advance from re_tests line 1859 to line 2030, **+513 rows**); **FIX 3 = #1715** (the INLINE `(?a:…)` group was a cl-ppcre SYNTAX ERROR, not "ignored" as `not-supported.md` claimed; `charset.t` 2776/2776 → **5224/328**, **+2,448 passing rows**).  **The re_tests TIMEOUT question is ANSWERED and it is HANG (#1714)**: `regexp_qr_embed.t` at `--timeout 1200` gives 907 rows against 905 at 90 s, and re_tests lines 906–926 are `.X(.+)+X` at 2^n (PCL n=30 = 64 s, perl 0 s) — no allowance, and do NOT `--quick`-skip them.  Filed #1712 #1713 #1714 #1716 #1717 #1718 **#1719 (a zero-length match does not advance `pos`, so `while ($s =~ /(\w*)/g)` NEVER TERMINATES)** #1720 #1721 #1723 #1724 #1725.  Two STALE attributions corrected: `reg_mesg.t`'s 3,348 are #1590 (`display_rx` is in PERL's test.pl) and the six `fold_grind_*` are XS `Encode` / no-locales, not #1036.  Bars: gate **240 files / 8203 rows** (only the 13 pclxs xs rows), sweep **GATE clean / TOTAL 18675 (+0) / drops 5 = census**, corpus-diff **IDENTICAL over 111** (no `Pl/*.pm` change) → **no generation bump**, ir-conform 321 pass / 0 fail / 24 known / 0 stale of 345, ir-host-leak 31 = main's.  **Leaves for a later round:** #1713 (1,016 rows, needs an in-class scanner), #1719 (the `//g` zero-width rule — a HANG in ordinary code, the biggest single prize left), #1720 (pat.t's 898), #1590 (reg_mesg.t's 3,348 + the heap ceiling), #1714 (registered, engine). | 34 files, DONE |
| **t5f** | **DONE (s473t5f, 2026-09-13) — the LAST t5 round.**  The population re-measured on this tree was **1,389 blessed fail rows over 22 files with 1,236 CAUSELESS**, the five UNEXPLAINED early-stoppers and the two NOTAP files.  **Every row now carries a cause: 1,304 rows / 0 causeless** (the count fell because three files' rows were replaced by this round's own measurement), and the five UNEXPLAINED `uni/` shortfall rows are **ZERO**.  Shipped: **#1736** (the indirect-object invocant shape was `/^[A-Z]/`, ASCII only, so `new Alice` worked and the non-ASCII spelling was a call to an undefined sub that aborted the form — `Pl::Environment::class_bareword_shape`, and the shape is NOT-LOWERCASE because a CASELESS script has no uppercase to require: `uni/universal.t` 31/49 → **86/4** with its 10-row shortfall gone, `uni/method.t` 31/30 → **56/6**), **#1737** (`isa`/`DOES` answer perl's FALSE, which is `""` and not undef — 40 of universal.t's 49-row matrix failed on definedness while truthiness agreed; the UNDEF answers perl does give are `UNIVERSAL::isa`'s own guard from universal.c, and `cl/pcl-xs.lisp`'s bare CL `if` had to change because perl's false is CL's true), **#1741** (a non-ASCII global WRITTEN in a nested `{ package X; … }` block was emitted UNQUALIFIED while every read was qualified — a SILENT WRONG; `uni/package.t` 8/10 → **11/7**, and corpus-diff's SHAPES line caught `interpolation-nonascii.pl` S08/S11 passing BY ACCIDENT with both sides in main).  **The brief's two "#418-family candidates" were both wrong hypotheses and one ASCII probe killed each**: `goto ここ` fails exactly as `goto HERE` (#1747, the nested-block `goto` — the shape #1654 warns about), and universal.t's abort was the indirect-object classifier.  `uni/greek.t` + `uni/latin2.t` owe nothing (perl's own unconditional `skip_all`; PCL's output byte-identical).  **`uni/variables.t` measured and DECIDED: no `perl-suite-timeouts.tsv` allowance** — two 900 s runs stop at EXACTLY row 59,916 while burning CPU (a CUMULATIVE ~60k-string-eval wall; the code point it stops at takes 0 s alone and an isolated 3,000-eval loop is linear at ~270/s), so the registry's promise cannot be met, and 15 min per run would buy 30,734 more rows of ONE fact — `\p{_Perl_IDStart}` answers NO for every character in PCL, so the file's loop always takes its ELSE branch (#1750, two halves).  Filed #1736(done) #1737(done) #1741(done) #1738 #1739 #1740 #1742 #1743 #1744 #1745 #1746 #1747 #1748 #1749 #1750.  Bars: gate **241 files / 8218 rows** (only the 13 pclxs xs rows), sweep **GATE clean / TOTAL 18676 (+0) / drops 5 = census**, corpus-diff emission identical over 111 with SHAPES 1 of 6 explained, emission A/B 571 files 567 SAME / 4 DIFF / 0 RCDIFF, ir-conform 323/0/22/0, ir-host-leak byte-identical to base, generation **v2-1460** with all three artifacts regenerated on the rebased tree.  **The two companion legs were finished by a resumed instance on 2026-09-14** and found one more gain and one more class: `uni/`+`mro/`+`class/` (113 files) is clean -- 0 NEW / 0 FIXED rows, CAUSES exactly the census, and `t/uni/method.t`'s last shortfall row 1 -> 0 because #1736 removed the abort; the `op/` isa-reach leg (38 files) found **#1737 worth 40 MORE rows in `op/universal.t`, 82/62 -> 122/22** (the line-106 `UNIVERSAL::isa` matrix, byte-identical to perl now and UNDEF on an `edd6dc78` extraction; 40 causeless rows left the fails baseline by edit), while `op/coreamp.t` and `op/coresubs.t` read the SAME on the base extraction (stale against MAIN, named not spliced) and both `gv.t` files went to **#1781** (they straddle the 90 s clock; the variable is machine LOAD -- base 5 DIFF / 3 TIMEOUT over eight runs -- so no allowance can be promised and no row was spliced). | 22 files, DONE |

**THE CENSUS'S RESIDUE AFTER ALL SIX t5 ROUNDS** (measured s473t5f from the
live baselines, re-counted 2026-09-14 on the tree rebased over s484c):
`baselines/perl-suite-fails.tsv` holds **11,984 blessed fail rows of which
2,362 are CAUSELESS** — and **2,335 of those are `op/`** (4,930 rows in the
directory), the rest being `base/` 15, `cmd/` 6 and `opbasic/` 6.  `re/`
(3,438), `comp/` (1,590), `uni/` (1,304), `run/` (477),
`io/` (218), `class/` and `mro/` are all at **ZERO causeless**.  The companion
half of `baselines/row-shortfall.tsv` is **47 files / 522 rows UNEXPLAINED, 45
of those files `op/`** — down from the census's own 124 files / 67,765 rows.
So what the six rounds leave is ONE directory plus 27 rows in three small
ones, and the shape of the remaining work is `op/`'s: 3,427 clusters over
4,707 rows at the census, i.e. mostly one row per distinct assertion, which is
why §5.4 says `op/` is attributed by FILE and not cluster by cluster.  Rounds
t5a and t5b took its early-stoppers and its six biggest clusters; the rest is
the long tail.

`mro/` and `class/` need no round: 0 fail rows and 0 UNEXPLAINED shortfall — every divergence is
already file-registered in `baselines/perl-suite-expected.tsv`.

### 5.6 The op/ rounds (t6…) — the census residue, banded by fail-row count

Designed s485 (`~/pcl-agent-scratch/s473/s473t6/plan.md`) from t5f's measurement:
after the six t5 rounds the residue is ONE directory, and it is WIDE rather than
deep (91 files, median 7 rows), so the rounds are cut by band, biggest first.
Recipe unchanged from t5 (CHECK 1 on the shortfall files, CHECK 2 per file,
FIX ≤ 1 h / FILE / CITE, the cause into EVERY row, movers serial + A/B before a
splice, the t5 bar, records); what t6 adds is that the population is RE-MEASURED
on the launch tree first, and that a file's known OWNER is grepped out of
DECIDED + the task store before anything is probed.

| round | band | files | causeless rows | state |
|---|---|---|---|---|
| **t6a** | ≥ 50 | 12 | 1,138 at design | **DONE (s473t6a, 2026-09-17).**  The population re-measured on the launch tree was **1,098**, not 1,138 — `op/universal.t` is 20 rows and not 60, because s473t5f's second companion leg had already fixed 40 with #1737; plus **140 UNEXPLAINED shortfall rows in six files**.  **All 1,098 now carry a cause and all 140 shortfall rows do too** (companion `unexplained` 2,362 → **1,264**; the `t/` shortfall's UNEXPLAINED half 522 → **382**; `cause-census --hygiene` adds ZERO `other` rows).  ONE FIX: **#1813**, an operator reads its operand ONCE — nine operators (`unary -`, `x`, `post ++`, `post --`, `&`, `^`, `|`, `~`, `length`) called a tied scalar's FETCH TWICE because each INSPECTED the operand and then COERCED the same PLACE again; ONE helper `%p-read-operand` whose NARROWING is the fix (a box carrying a class, the is-ref flag or a cached numeric half is still coerced as the box — a bare `unbox` made `~ [1] == ~ [1]` answer "same" where perl gives two addresses).  `t/op/tie_fetch_count.t` 131/10 → **139/2**, `perl-tests/bop.t` 480/29 → **485/24**, sweep TOTAL **18681 → 18686** (rebased over main `5eb87b01`).  The tie trio is ONE decision (#155, with the per-method inventory measured into it).  **#1840** (a compound `OP=` returns its LEFT OPERAND AS AN LVALUE, so consuming it re-FETCHes: op/bop.t is 14 rows FIXED and 9 BROKEN by #1813, net +5, and the 9 are named not blessed — they were passing on PCL's old DOUBLE read).  Filed **#1814** (the ZERO-FETCH half — `@a = ($tied)` pushes the tie PROXY, a silent wrong that also makes the element alias the tied variable), **#1815** (@INC hooks, 93 rows, the round's biggest prize), **#1816** (a `-X` overload under stacking, 47), **#1817** (PPI §31: `sub _ { … }` swallows the next statement — logged in `ppi-upstream-bugs.md` + 5 rows in `ppi-bug-report.t`), **#1818 #1819 #1820 #1821 #1823 #1825 #1826 #1828 #1829**.  Bars (re-measured on the rebased tree): gate **PASS 244/8254** on the tree rebased over s473v (244/8231 on `5eb87b01`; main alone 243/8239), sweep **GATE clean / 0 new / 0 fixed / TOTAL 18686 = baseline / drops 5 = census / CAUSES 480 of 480**, companion `op/` leg **221 files, 7 movers, 2 spliced** (`op/tie.t` 14/81 → 15/80 and `op/bop.t` 469/28 → 474/23, both A/B'd against a `2f9cc300` extraction; the other five read the SAME there — coreamp/coresubs/inc/inccode/gv, #1781 for the last), corpus-diff **IDENTICAL over 111** (no generation bump — `cl/` only), ir-conform 323/0/22/0, ir-host-leak byte-identical.  **Leaves for t6b/t6c:** #1815 is the one to take first (93 rows behind one mechanism); #1433's blast radius now has three data points (`op/method.t`, `op/ref.t`, `op/tie_fetch_count.t`) and its option (c) — narrowing the emitted `p-let` — is the lever that would pay across the directory. |
| **t6b** | 20–49 | 21 | 782 | **DONE (s473t6b, 2026-09-17/18).**  The population on the launch tree was **683** causeless blessed rows in 20 files (not 782 — t6a and s488a had already moved some), and all fifteen files no fix touched re-measured EXACTLY their blessed snapshot verdict, so the band was confirmed before a single cause was written.  **683 → 219**; companion `unexplained` **1205 → 741**.  TWO FIXES: **#403**, a filetest's FALSE is a VALUE — the empty string when the operation that would fill `_` SUCCEEDED and undef when it failed, and ONE value either way where PCL left CL NIL (undef AND zero values in list context); ONE returner `%p--false`, four call sites, no new state, and `t/op/filetest_stack_ok.t` (135 rows of `-X returns single value` and nothing else) went 98/38 → **136/0 OK**, `t/op/filetest.t` +3.  And **#1878**, `die` with no arguments REUSES `$@` (PROPAGATE / a reference unchanged / "\t...propagated at" / "Died" only when `$@` is empty) — the mirror of `p-warn-build-message` — together with the eval ENTRY clear (perl's CLEAR_ERRSV) that its own negatives exposed: `t/op/die.t` 5/15 → 9/11 with `t/op/eval.t` UNCHANGED.  Member 0 shipped **#1850** (the harness stub's `$| = 1`, plus the `%tap-out` autoflush that made it real, plus runpcl streaming); it costs exactly TWO companion rows, both spliced with their cause — a newline-less NON-TAP write now GLUES to the next TAP row, as it does in perl under `$| = 1`, and op/tiehandle.t / uni/stash.t are the only two files that make such a write.  **CHECK 1 is what turned 148 `(missing)` rows into causes** (the runner's own `aborted-forms:` message: lvalue subs in substr.t, the dropped `Tie::Monitor` in gmagic.t, DynaLoader in each.t, pack `P` in bop.t, #1877 in postfixderef.t, the stash in magic.t, the F6 refusal in tr.t, a #138 drop in lex.t).  **An ORPHANED companion run whose tree changed under it is not evidence**: its two biggest movers (io/crlf_through.t −318, io/through.t −262) both re-measure 942/0 OK.  Filed **#1877** + **#1878**; #403 / #1850 / #1878 CLOSED.  Bars: sweep GATE clean / 0 new / 0 fixed / 0 LOST / TOTAL **18686 (+0)** / drops 5 = census; ir-conform 323/0/22/0; gate `Result: PASS`.  **Leaves for t6c: 219 causeless rows** — op/eval.t 29, op/lex.t 29, op/sprintf2.t 24, op/magic.t 22, op/sprintf.t 19, op/sort.t 17, op/die.t 16, op/tr.t 14, op/bop.t 14, op/postfixderef.t 14, op/each.t 8, op/array.t 7, op/lc.t 6. |
| **t6c** | < 20 | 90 | 494 (+ 145 UNEXPLAINED shortfall rows in 24 files) | **DONE (s473t6c, 2026-09-19).**  The population re-measured on the launch tree was **90 files / 494 causeless blessed fail rows / 145 UNEXPLAINED shortfall rows in 24** (the design's "58 / 455" predated t6a and t6b moving rows), and all 90 read their blessed snapshot verdict EXACTLY (`cmp-run` 0 differing of 90, ROW DIFF 0 NEW / 0 FIXED / 0 LOST) before a cause was written.  **494 -> 0 causeless and 145 -> 0 UNEXPLAINED shortfall**; companion `unexplained` **741 -> 247** (t6d's band); `cause-census --hygiene` adds ZERO `other` rows.  THREE FIXES: **#1920** (a child that cannot be started is a VALUE — perl's `system` returns -1 / `$?` -1 / `$!`, `sb-ext:run-program :search t` SIGNALS, and the signal aborted the whole top-level form: `t/op/exec.t` lost 16 rows to one `system { "lskdfj" } "lskdfj"`; `p-exec` already had the handler); **#1921** (a PPI full-quote token keeps its body as a SECTION RECORD and `set_content` leaves it STALE, so a rename that lengthens a name SILENTLY TRUNCATED the body at the ORIGINAL length — `qq{}`, `qq[]`, `s{}{}` both sections, `qx{}` to empty; one re-lex at `_fix_interp_token`, the one site all seven rename callers pass through, and an un-re-lexable record DIES); **#1923** (a kv-slice in SCALAR context is its LAST value, not the pair COUNT — the two kv emitters were the only slice emitters that did not call `_slice_in_context_form`).  Rows: `t/op/exec.t` 24/1 -> **37/4** with its 16-row shortfall gone (14 FIXED ROWS), `t/op/kvhslice.t` 21/18 -> 22/17, `t/op/kvaslice.t` 23/15 -> 25/13; 17 rows leave `baselines/perl-suite-fails.tsv` BY EDIT.  **The tail's value is the CROSS-FILE CLUSTERS**, not 90 investigations: #221 warnings (~95 rows/13 files), rejection of invalid Perl (~80/18), the typeglob-stash model #1664 (~40/9), perl's own internals (~45/7), Unicode semantics (~25/7), DESTROY-at-unwind (~20/7), tie/FETCH #155+#1814 (~20/6), `local` on an element (~15/3), lvalue subs #930 (~9/5), and **#1924** — PCL's `%Config` is a 108-key hand-written subset where perl's is 1,253, so seven files ask for a key that reads UNDEF and `op/lfs.t`'s own `skip_all` fires (a hole only the shortfall bucket can see).  Filed **#1922** (`pos` is kept on a SCALAR only — `pos($h{k})` / `pos($a[0])` / `pos($_[0])` are UNDEF after a `//g` on that place; the whole of op/pos.t), **#1924**, **#1925** (`reset` is a no-op stub where perl CLEARS the package's variables), #1926 #1927 #1928 #1929 #1930 #1931 #1932 #1933 #1934 #1935 #1936 #1937 #1938 #1939 #1940 #1941 #1942.  Bars: corpus-diff **1 of 111** (kvhslice.t, the positive control; `lib/` byte-identical so **no generation bump**), emission A/B **360 files 359 SAME / 1 DIFF / RCDIFF 0**, sweep **GATE clean TOTAL 18,686 (+0) drops 5 = census**, gate `Result: PASS`.  Guards `Pl/t/interp-rename-sections-01.t` (8) + `Pl/t/kvaslice-01.t` (16 -> 18), both inverse-verified. |
| **t6d** | t6b's band residue | 13 (+3 shortfall-only) | 219 (+ ~160 UNEXPLAINED shortfall) | **DONE (s473t6d, 2026-09-20) — the LAST op/ round.**  The population re-measured on the launch tree was **213 causeless blessed fail rows in 15 files** (the design's 219 predated t6c and s491a; `op/override.t`'s 34 belong to the concurrent s492a batch) and **140 UNEXPLAINED shortfall rows in 10 files**, and all 15 files read their blessed snapshot verdict EXACTLY before a cause was written (ROW DIFF 1 NEW / 1 FIXED = the same op/lex.t row re-keyed, #1853).  **213 → 0 causeless and 140 → 0 UNEXPLAINED shortfall**; `cause-census --hygiene` adds ZERO `other` rows.  TWO FIXES: **#1950** (a tie on an ELEMENT — `tie $h{foo}` reached `p-tie` with the element's VALUE and took the announce-and-drop arm, so `tied` read undef and `$obj->init` died; ONE table `%ELEM_BOX_FORM` for `pos`/`tied`/`tie`/`untie`, plus the `p-tie-proxy-p` arm in `%p-hash-unbox-elem` / `p-aref-unbox-elem` that stops a READ of a tied element leaking `#<p-tie-proxy …>` into the program's output: `t/op/gmagic.t` 31/2 → **50/2**, shortfall 45 → 26) and **#1954** (an anonymous CONSTRUCTOR in a foreach list is ONE value — `for (['a','b'])` iterated the REFERENT and ran the block TWICE with `$_` bound to each element, so `@$_` was a symbolic deref of a string; #267's `(vector …)` rule did not list `PPI::Structure::Constructor`, one `elsif`).  **CHECK 1 was MEASURED, not read**: neutralising ONLY `op/substr.t`'s two `:lvalue` sub assignments in a shadow `t/` tree takes the file 354 → **398** produced rows, so its 46 are #930 end to end.  **An earlier round's attribution is evidence, not fact**: `op/postfixderef.t`'s seven never-produced rows were all blamed on #1877 and are **#1436** (3) + DESTROY (4).  **A ROW THAT AGREES IN ISOLATION IS DOWNSTREAM STATE, applied against this round's own filings**: `op/eval.t` 9/10 are `+()=` (an assignment whose LHS is the EMPTY LIST yields the last RHS element here, the count in perl — **#1956**), not `eval`; `op/each.t` 64/65 reproduce perl exactly on their own (**#1964**).  Filed **#1951 #1952 #1953 #1955 #1956 #1957 #1958 #1959 #1960 #1961 #1962 #1963 #1964**; #1950 + #1954 DONE.  Bars: gate `Result: PASS` **250/8,495**, sweep **GATE clean / 0 new / 0 fixed / TOTAL 18,685** (= 18,687 − the two `perl-tests/magic.t` rows #1954 removed, which the double iteration had invented — A/B'd with the sweep's own command on a `268d7e00` extraction and spliced by hand) **/ drops 5 = census / CAUSES 479 of 479**, corpus-diff **5 of 111** (all the intended shapes), lib emission A/B 27 SAME / 0 DIFF / 0 RCDIFF, ir-conform 323/0/22/0, ir-host-leak 31 = main's, generation **v2-1660** with the three artifacts regenerated.  Guards `Pl/t/tie-01.t` 18 → 24 and `Pl/t/foreach-aliasing-01.t` 22 → 30, both inverse-verified.  **COMPANION LEG (member 4, finished s493)**: the targeted 44-file run read `85 NEW / 68 FIXED / 1 LOST`; A/B against a `git archive 268d7e00` extraction, ROW TEXT not counts, says FOUR files moved and they are this round's -- `op/gmagic.t` 31/2 -> **50/2**, `op/tie.t` 15/80 -> **17/78**, `op/inccode.t` 13/26 -> **16/25** (shortfall 50 -> 48), all #1950, and `op/magic.t` 176/31 -> **174/31** (shortfall 1 -> 3), #1954 -- all SPLICED row by row, re-run **0 NEW / 0 FIXED / 0 UNVERIFIED / 0 LOST**.  The other 84 "new" rows are IDENTICAL on BOTH trees and were FILED, not spliced: `op/gv.t` 51 = #1651's hang labelled DIFF instead of TIMEOUT (**#1967**), `re/pat.t` 28 = hash-order row descriptions at pat.t:434 (**#1965**), `re/pat_rt_report.t` 4 + `op/lex.t` 1 = blessed rows in a pre-normalisation SPELLING (**#1966**).  The ONE genuinely new row CORRECTED a member-3 filing: `op/magic.t`'s `[at t/op/magic.t line 619]` is not `${^TAINT}` -- with the two invented rows gone the offset drops from 82 rows to 7 and the failing row is `eval " BEGIN { ok ! defined \$^S } "` at magic.t:615 (**#1968**, `$^S` is UNDEF DURING COMPILATION in perl).  **WHAT REMAINS OF #1501: the op/ directory is at ZERO causeless rows and ZERO unexplained shortfall except `op/override.t`** (34 fail rows + 16 shortfall), which the s492a batch owns as #1870's acceptance.  Across the whole companion the residue is that one file; the census is otherwise complete, so **#1501 closes when override.t lands**. |

## 6. The queue as of s494 (Fable, 2026-09-21) — the EVERYDAY-PERL rounds (#1994) and the s493 proposals the USER ruled

**USER s494 on the ORDER: "don't put too much time deciding order, it can take one week more" — the table below stands as written.**  Briefs for this era live under `~/pcl-agent-scratch/s492/<label>/` (COMMON stays
`~/pcl-agent-scratch/s473/COMMON.md`).  Two slots, as always.

| # | label / task | what | gen | state |
|---|---|---|---|---|
| 1 | **s492b** | the SILENT-WRONG core: #2004 (scalar context leaks into a builtin's LIST arguments) + #2090 + #2102, #2080 `local *GLOB = REF`, #2005 `map` copies per iteration, #2003 TAP names by IMPORT + #2100 runtime-name hijack, #2006 half (a), #2008 exported scalar = alias, #2001 list `m//g` and `pos` | v2-1740 | **IN FLIGHT** (launched s494 on `5ce155cb`; IDs 2030–2049) |
| 2 | **s492c** | the MODULE SURFACE: #1870 → #1992 builtin override + `Time::HiRes` (+ #2002), #1997 POSIX, #2007 File::Spec/`tempdir`, #2081 `flock`, #2050 `\X`, #1361 Carp location, #1743/#1818 `->VERSION`, #2082, #2000, #2085, MIME::Base64, then #2101 → #2094 → #2093; acceptance instrument = the core dists' own t/ | v2-1780 | **IN FLIGHT** (launched s494 on `5ce155cb`; IDs 2052–2079; owns `op/override.t`, which closes #1501) |
| 2a | **#2194 `pcl --check`** | USER-APPROVED s494: run a program under perl and PCL, compare stdout + exit-status class, print the first difference; a TOOL batch (driver + tools/t + docs) | none | brief READY (`~/pcl-agent-scratch/s494/s494k/prompt.md`, label s494k, IDs 2275–2284); **FIRST launch next session** |
| 3 | **#2098** | PERF slot (USER s494: ahead of #1890/#1891): the three QUADRATIC everyday operations — `.=` append loop, `shift` drain, `unshift`; profile FIRST (one cause or three) | v2-1820 (member 3 only) | next free slot; brief READY `~/pcl-agent-scratch/s494/s494p/prompt.md` (label s494p = perf round 35, IDs 2110–2129); the `shift` mechanism is MEASURED in the task — O(1) through the array header window, 200k drain 0.003 s in bare SBCL |
| 4 | **#2104** | the ROSETTA census round: 655 perl-validated programs, 177 differ, **77 SILENT** — narrow to causes, cluster, file; ≤ 1 h fixes may ship | v2-1860 if a fix | next free slot, preferably after s492b or s492c is merged (their fixes explain a share of the 77); brief READY `~/pcl-agent-scratch/s494/s494r/prompt.md` (label s494r, IDs 2130–2189) |
| 5 | **#2106** | the CORE-MODULE LEG for the companion: a second root for `tools/run-perl-suite.pl` = the core dists' own t/ (2,373 files never run before s493), own baselines, on demand | none | brief READY (`~/pcl-agent-scratch/s494/s494m/prompt.md`, label s494m, IDs 2230–2259); AFTER s492c merges (the first bless must measure a tree that is not moving) |
| 5a | **#2006(b) PART 1** | the scope-exit close of a non-escaping lexical handle (DESIGNED s494, in the task): a VarAnnotator sibling verdict + `p-scope-close`; the fd leak dies at i=1021 under the stock `ulimit -n 1024` | v2-1900 | design + brief READY (`~/pcl-agent-scratch/s494/s494h/prompt.md`, label s494h, IDs 2200–2214; one agent-day); AFTER s492b merges (its member 5 is half (a), same seam) |
| 5b | **#2092 PHASE 1** | chars 128–255: case mapping by regime (/u lexical = perl-exact, /d = the data sniff) + `/a` honoured (DESIGNED s494, in the task) | v2-1940 | design + brief READY (`~/pcl-agent-scratch/s494/s494u/prompt.md`, label s494u, IDs 2215–2229); PHASE 2 (regex at /d sites) is a measurement back to Fable |
| 5c | **#2107 SIGNALS** (+ #2108 if time) | only ALRM is wired to `%SIG`: handlers never run, ^C = 32-line backtrace, TERM exits 0, `\| head` never stops; SBCL owns USR2 / URG / the fault signals (MEASURED s494) — a derived deny list; PLATFORM-TOUCHING; ranked 3rd of the newcomer top ten | none (runtime-only) | brief READY (`~/pcl-agent-scratch/s494/s494g/prompt.md`, label s494g, IDs 2260–2274; one agent-day).  **By Q7`s ranking this outranks #2104** — the USER decides the order |
| 5d | fillers from the s494 batteries | **#2108** uncaught-error shape · **#2109** locations · **#2190** eval disk-cache write budget · **#2191** cached string eval re-compiled per execution (perf) · **#2192** textual `use utf8` | — | filed; #2107 is the heavy one and the most user-visible |
| 6 | **#2097** | `pcl` one-liner switches -n -p -l -a -F -i -0 (-s) | if a fix | **LOW PRIORITY** (USER s494: "on the todo, but not so prioritized") — only when nothing above waits |

Fable's own work, in order: the Q4 ENDURANCE and Q5 FAILURE-EXPERIENCE batteries
(`docs/questions-worth-asking.md`), the #2006(b) scope-exit-close design, the #2092
byte-program casing design, #1966/#1967 re-spelled, a quiet-box `tools/bench-exec.pl` run for
the README's pack / moo rows.

## 7. The queue as of s495 (Fable, 2026-09-21) — STEERED BY THE EVERYDAY NUMBER

**USER directive (end of s494): stop opening new test angles, build #2099 early, steer by its
number — goal > 90 % of everyday programs identical to perl.**  #2099 is MERGED (s495, main
`ba30b0f7`): `tools/everyday-smoke.pl` reads **`EVERYDAY: 85 of 122 identical to perl (69.7 %)`**.
90 % of 122 is **110**.  The baseline's cause column (`baselines/everyday-baseline.tsv`, 37 rows)
orders the correctness queue from here; §6's order stands only where this table is silent.

| step | batch | programs it buys (baseline rows) | running total |
|---|---|---|---|
| 0 | **s492b** — MERGED s495 (`87e4e4b3`) | already in the 85 (b2-26, b2-29) | 85 |
| 1 | **s492c** — in flight at the end of s495 (rebased onto s492b; merge bar = combined-tree gate + sweep) | up to **11**: `idioms/posix-funcs` #1997 · `idioms/sig-and-sleep` + `index/proc` #2002 · `index/flock` + `modules/Fcntl-Errno` #2081 · `modules/File-Temp` + `programs/real-dir-walker` #2007 · `modules/IO-Select-Socket` · `modules/Text-Wrap-ParseWords-Abbrev` #2050 · `programs/require-do-file` #1743 · `modules/MIME-Base64` (its plain-Perl shim) | ≤ 96 |
| 2 | **s495f — EVERYDAY ROUND A** (brief `~/pcl-agent-scratch/s495/s495f/prompt.md`, IDs 2340–2369, gen v2-1980) | **10**: #2093 ×5 (`telldir`/`seekdir`, `formline` + `$^A`, `gethostby*`, `getservby*`, `setpriority`/`getpriority`) · #2286 `List::Util::pairs` · #2051 `qr//` is blessed into `Regexp` · #2009 `$?` inside END / END's `$?` is the exit code · #2103 rvalue deref of undef under `strict refs` dies · #2008 an exported SCALAR is an alias (root cause LOCATED by s492b: `p-cast-$`'s symbolic arm) | ≤ 106 |
| 3 | **s494h** #2006(b) PART 1 (brief READY) | 1: `programs/resource-idioms` | ≤ 107 |
| 4 | **s494u** #2092 PHASE 1 (brief READY) | 1: `index/lc-uc-etc` | ≤ 108 |
| 5 | **#2084** module edges (brief OWED) | 2 without tie: (3) Scalar::Util `isweak`/`openhandle`/`readonly`, (4) Archive::Tar::Constant's stash walk; (5) `use Env` waits for #155 | ≤ 110 = **90.2 %** |
| — | what is left after that | #155 tie ARRAY/HASH ×2 (+ Env) — a Fable design · #1361 Carp location (needs the per-frame location stack; s492c's drop-the-newline attempt is a DO-NOT-RETRY) · #2082 open2/open3 · #2083 BigFloat `bdiv` · #2285 Benchmark · #2287 File::Copy `goto` · 4 ruled non-supports (DESTROY at scope exit, `format`, XS digests, Unicode case folding) — so the ceiling without new rulings is 118 | |

Interleaved as before (one perf agent beside a correctness agent): **s494k `pcl --check`**
(USER-approved, tool batch) takes the first slot next session, **s494p #2098** the perf slot;
#2104 (Rosetta census) now measures with the SAME tool (`--corpus DIR --baseline FILE`) and its
brief should say so before it launches; #2107 signals, #2106 and #2097 keep §6's order.

**Todo, LOW PRIORITY (USER s495: "do give stage 0 a low priority") — #2371 DESTROY AT PROGRAM EXIT** (Stage 0 of `docs/scope-exit-destroy-design.md`; about one agent-day; runtime-only; sound by construction: perl destroys survivors at exit too).  It sits beside #2097 at the bottom of the queue — only when nothing above waits, never ahead of a batch that moves the everyday number.  The scope-owned-object stage is PARKED by the USER (#2370): not worth the trouble.  The filehandle stage (s494h #2006(b)) keeps its own place in the table above.

**The denominator must grow.**  122 programs cover ground, they are not a sample; "90 % of 122"
is a milestone, not the goal.  Battery 3 (new programs, each admitted under perl by the runner's
own `--bless-expect`) is Fable's owed work and lands as corpus-only commits between batches —
every addition arrives WITH its baseline row and cause, so the number may go DOWN on the day the
corpus grows, and the history file's M column is what keeps that honest.

### 7a. The table RE-DERIVED from the baseline at s496 (Fable, 2026-09-25) — supersedes §7's running totals

`baselines/everyday-baseline.tsv` at `40da28bd` has **30 rows** (92 of 122 identical).  Grouped by owning cause: **#2093 ×5** (`index/dirpos formline gethost getsrv priority`) · **#2084 ×3** (`modules/Archive-Tar-Compress Scalar-Util Sys-Hostname-Env-English`) · **#155 ×2** (`modules/Tie-Hash-Array-Scalar programs/tied-scalar-hash`) · sixteen singletons (#1361 #1998 #2006 #2008 #2009 #2051 #2056 #2060 #2082 #2083 #2092 #2103 #2107 #2285 #2286 #2287) · four ruled non-supports (`idioms/destroy-scope` DESTROY at scope exit, `modules/Digest-SHA-MD5` XS, `index/format` format/write, `programs/lc-uc-nonascii` case folding).  s492c bought 7 rows, not the 11 §7 assumed (Text-Wrap is #2060, File-Temp is #2056, IO-Select-Socket moved to #1998, index/proc to #2107).

| step | batch | rows it buys | running total |
|---|---|---|---|
| 0 | main `40da28bd` (s494k merged: a tool, no row) | — | **92** |
| 1 | **s495f EVERYDAY ROUND A** (in flight) | 10: #2093 ×5 · #2286 · #2051 · #2009 · #2103 · #2008 | ≤ 102 |
| 2 | **s494g #2107 signals** (launching s496) | 1: `index/proc` | ≤ 103 |
| 3 | s494h #2006(b) | 1: `programs/resource-idioms` | ≤ 104 |
| 4 | s494u #2092 | 1: `index/lc-uc-etc` | ≤ 105 |
| 5 | #2084 module edges (brief OWED) | 3 | ≤ 108 |
| 6 | **s496a #2060 Unicode properties** (brief READY) — needs **#2085** too (the program's line 9 is a `%{{ f() }}` drop; the row moves there otherwise) | 1: `modules/Text-Wrap-ParseWords-Abbrev` | ≤ 109 |
| 7 | #2056 `File::Temp->new` | 1: `modules/File-Temp` | ≤ **110 = 90.2 %** |
| — | after that: #1998 · #155 ×2 · #1361 · #2082 · #2083 · #2285 · #2287 = 8 more → the ceiling without new rulings is **118**; the four ruled rows stay | | |

The denominator still has to grow (battery 3); every number above is "of 122".
